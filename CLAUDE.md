# Klinik Asistanı

## Proje Özeti
Fizyoterapi kliniği için muayene yönetim SaaS'ı. Asistan Merkezi çatısı altında çoklu klinik kullanacak multi-tenant yapıda. Hasta kaydı, randevu takibi, muayene odalarına Android tabletle anlık hasta/işlem gösterimi, terapist performans/maaş takibi, ödeme-paket yönetimi, hasta self-servis portalı ve WhatsApp/Paraşüt entegrasyonlarını kapsar.

## Stack ve Altyapı
Next.js 16 (App Router) + TypeScript + Tailwind + Supabase + Vercel (Frankfurt), Cloudflare DNS — standart Asistan Merkezi stack'i. (Not: ilk sürümde "Next.js 14" olarak yazılmıştı; 2026-07-26'da ilk vertical slice kurulurken güncel latest sürüm (16) ile devam edilmesine karar verildi — middleware→proxy geçişi ve `useActionState`/React 19 kullanıldığını unutma.)
Projeye özgü ek entegrasyonlar:
- **Paraşüt**: resmi fatura kesimi (faturalı işlemde otomatik tetiklenir)
- **WhatsApp**: randevu, paket durumu, bakiye bildirimi — Meta (WhatsApp Business/Graph API) üzerinden kurulacak (`meta-integration` skill'i geçerli)
- **Tablet istemcisi**: Android tabletlerde native yerine Next.js PWA veya kilitli tarayıcı (kiosk mode); Supabase Realtime (WebSocket) ile sıfır gecikmeli güncelleme; offline-first (internet kesintisinde senkronizasyon queue ile çalışır)
- **Zamanlanmış işler**: Supabase Edge Functions + pg_cron (Paraşüt fatura, WhatsApp mesajları, paket expiry kontrolü)
- **UI**: Tablet/resepsiyon panelinde optimistic UI (TanStack Query) — check-in gibi aksiyonlarda gecikme hissettirilmez
- **Güvenlik**: `current_klinik_id()` tüm kritik tablolarda RLS ile zorunlu (klinik_id kolonu + auth.jwt() kontrolü); `super_admin` bypass policy'si audit_log'a yazılır; hassas hasta verileri (TC/pasaport no, adres, acil durum, tıbbi geçmiş) ayrı `hasta_hassas` tablosunda tutulur — pgcrypto kolon şifrelemesi YOK (kullanıcı kararı: izolasyon RLS + Supabase disk-seviyesi şifrelemeyle sağlanıyor, anahtar yönetimi karmaşıklığı MVP'de gereksiz görüldü)
- **Storage**: hasta fotoğrafı/vücut haritası Signed URL + kısa expiry + virus scan (Edge Function) ile saklanır
- **Realtime**: subscription'lar oda bazlı filtrelenir (tüm klinik tek kanalda olmaz); sık sorgulanan tablolarda materialized view/cache kullanılır
- **Ödeme**: kredi kartı için İyzico veya Stripe (Türkiye), 3D Secure
- **Dayanıklılık**: Paraşüt API rate limit/downtime riskine karşı queue+retry (Edge Function + Supabase Queue); WhatsApp template red riskine karşı SMS fallback (Twilio/Turkcell)

## Veri Modeli
- `klinik` — tenant; her klinik ayrı kayıt; subdomain/custom_domain (beyaz etiket), created_at, plan_turu (starter/pro/enterprise), plan_bitis_tarihi, logo_url, marka_renkleri (JSONB)
- `kullanici` — roller kesinleşti: `super_admin` (platform yöneticisi), `klinik_admin` (klinik sahibi — finans/rapor/personel), `resepsiyon` (kayıt/randevu/ödeme/check-in/WhatsApp), `terapist` (kendi randevuları/seans notları/VAS/hakedişleri); last_active_at (aktiflik takibi)
- `personel` — tüm çalışanlar (terapist dahil): görev, maaş, ekstra ücretler, fazla mesai, rol ve yetkilendirme; ayrıca doğum tarihi/yeri, cinsiyet, eposta (görüntüleme amaçlı önbellek — giriş e-postası `auth.users`'ta kalır), departman, çalışma tipi, SGK sicil no, işe giriş tarihi, `ozel_yetki_override` jsonb (şema hazır, henüz hiçbir RLS/UI kullanmıyor — placeholder)
- `personel_hassas` — personel TC kimlik/pasaport no, `pgp_sym_encrypt` ile şifreli (1-1, `personel_id` PK); `hasta_hassas`'ın aksine (orada bilinçli olarak pgcrypto YOK) burada gerçek kolon şifrelemesi var — anahtar `ALTER DATABASE ... SET app.settings.encryption_key` ile DB'ye elle kurulur, migration'a gömülmez; düz metin client'a hiç dönmez, sadece `personel_hassas_maskeli_getir` RPC'siyle son 2 hane
- `personel_acil_kisi` — personel acil durum kişisi (ad/yakınlık/telefon), hassas değil
- `personel_mesleki_belge` — terapist/hekim rolündeki personelin diploma no, uzmanlık belge no, meslek odası sicil no, Sağlık Bakanlığı tescil no, e-imza sertifika seri no (PIN YOK, sadece referans), kaşe görseli (`personel-belge` Storage bucket), mali sorumluluk sigortası poliçe no
- `terapist` — personelin terapist rolündeki alt kümesi; maaş hesaplama parametreleri (`maas_hesaplama_modeli`, `prim_sabit_tutar`, `baraj_seans_sayisi`, `baraj_bonus_tutari`) burada tutulur
- `personel_ekstra_hakedis` — bordroya eklenen yol/yemek/mesai/sgk/diğer ek hakedişler; personel + tarih + tutar (`sgk` türü 2026-08-05'te eklendi, bordro maliyeti görünümünde kullanılır — personelin eline geçen "alacak" hesabına dahil EDİLMEZ, bkz. `personel_odeme`)
- `personel_odeme` — Personel Takip'te "alacak" hesabının ödenen tarafı: personele fiilen yapılan ödeme kayıtları (tutar+tarih+açıklama); hakediş tarafı için ayrı bir ledger yok, maaş/prim/ekstra-hakediş verisinden her sorguda hesaplanır
- `hasta` — hasta kaydı; `kvkk_onay_tarihi`, `ozel_nitelikli_veri_onay_tarihi` (sağlık verisi için ayrı rıza), `ticari_ileti_onay_tarihi` (SMS/e-posta kampanya, `whatsapp_izin_durumu`dan ayrı), `whatsapp_izin_durumu`, `dogum_tarihi`, `cinsiyet`, `kategori` (vita/plus/elit/prime — tedavi fiyatına uygulanacak iskonto kademesi, varsayılan vita), `eposta`, `referans_kanali`, `vergi_no`/`vergi_dairesi`/`unvan` (kurumsal hasta — henüz eklenmedi, Paraşüt turunda), `parasut_contact_id` (cari eşleme — tekrar oluşturulmaz), `kayit_kanali` (`resepsiyon`/`qr_self_servis` — QR ile self-servis kayıtları ayırt eder, `hasta_hassas` anon RLS policy'sinin blast radius kısıtlaması bu koloma dayanır) alanları (ilk kayıtta SMS/QR ile tek tıkla onay)
- `personel_basvuru_taslagi` — QR ile gelen personel başvurusu; onay bekleyen taslak (kullanıcı kararı), klinik_admin onaylayana kadar hiçbir personel/kullanıcı kaydı oluşturmaz; `durum` (bekliyor/onaylandi/reddedildi) + değerlendiren/tarih
- `is_basvurusu` — QR ile gelen basit iş başvurusu (ad soyad/telefon/e-posta/pozisyon/mesaj); `durum` (yeni/incelendi/reddedildi/kabul_edildi); CV yükleme/pipeline kapsam dışı (kullanıcı kararı, "detaylı içeriğini sonra oluştururuz")
- `anket_yaniti` — QR ile gelen memnuniyet anketi/öneri; 1-5 `puan` + `oneri_metni` (en az biri zorunlu), `ad_soyad`/`telefon` opsiyonel (kullanıcı kararı)
- `hasta_hassas` — TC/pasaport no (`kimlik_no`+`kimlik_no_tipi`, klinik bazlı UNIQUE), adres, acil durum kişisi (ad/yakınlık/telefon), tıbbi ön geçmiş — 9 parametrenin her biri ayrı `_var` bayrağı (nullable boolean: NULL=sorulmadı, true/false=hastanın yanıtı) + serbest metin detay alanı olarak tutulur: alerji (`alerji_var`+`alerjiler`), kan sulandırıcı (`kan_sulandirici_kullanimi`+`kan_sulandirici_detay`), kronik hastalık (`kronik_hastalik_var`+`kronik_hastaliklar`), sürekli ilaç (`surekli_ilac_var`+`surekli_ilaclar`), ameliyat (`ameliyat_var`+`gecirilmis_ameliyatlar`), bulaşıcı/enfeksiyöz hastalık, protez/kalp pili/implant, hamilelik-emzirme, sigara/alkol/madde (bu son 4'ü `<parametre>_var`+`<parametre>_detay`); ayrıca `gelis_sebebi` (serbest metin) ve `oncelik_durumu` (normal/oncelikli/acil — triage rozeti, sağlık verisi rızasına bağlı değil); `hasta_id` 1-1 PK, `klinik_id` INSERT trigger ile `hasta`dan türetilir
- `hasta_veli` — 18 yaş altı hasta için veli bilgisi (ad, telefon, yakınlık); onam formunda veli imzası desteklenir
- `hasta_veri_talebi` — KVKK silme/anonimleştirme talebi + durum + tarih; `klinik_ayarlar`'da `veri_saklama_suresi_yil`
- `oda` — muayene odaları; tablet cihaz eşlemesi burada
- `cihaz` — kıt fiziksel ekipman (Reformer, elektroterapi, dry needling seti, lazer vb.); `islem_tanimi`'ne bağlı `gerekli_cihaz_id`; tablet eşlemesi `device_token` + `revoked_at` ile (kayıp/çalıntı tablet uzaktan iptal edilebilir)
- `randevu` — hasta + terapist + oda + cihaz + zaman; durum alanı (planlandı/geldi-içeri alındı/iptal/gelmedi); çakışma kontrolü oda+terapist+cihaz üzerinden yapılır; `is_grup_seansi` + `max_kapasite` (bir zaman dilimine bir odada birden fazla hasta kaydı — Group Pilates, Bel-Boyun Okulu vb.; her hastanın paketinden 1'er hak düşer); `ana_terapist_id` (asıl sorumlu) ile `yapan_terapist_id` (fiilen seansı yapan) ayrımı — prim hangisine yazılacağı klinik ayarına bağlı; `recurring_id` + `recurring_pattern` (JSON) ile tekrarlayan randevu desteği (örn. haftalık Pazartesi 10:00); ana ekran canlı zaman çizelgesi bu tablodan beslenir
- `hasta_onam` — dijital tıbbi onam/rıza formu; tablet üzerinde imzalanır (ilk girişte veya riskli işlem öncesi), imza PNG/PDF olarak Supabase Storage'a (Signed URL) yüklenir
- `klinik_ayarlar` — klinik bazlı tüm konfigürasyon (JSONB veya tek satır tablo): gizlilik_modu, no_show_kural, paket_iade_kesinti_orani, maas_hesaplama_modeli, stok_uyari_esigi, varsayilan_randevu_suresi, grup_seansi_izinli_mi, onam_formu_zorunlu_islemler, features (JSONB — feature flag) — daha önce ayrı ayrı geçen klinik ayarlarını burada toplar
- `terapist_uzmanlik` — terapist ↔ uzmanlık alanı (Bel-Boyun, Spor Yaralanmaları, Nörolojik, Pediatrik vb.) many-to-many; randevu öneri motoru ve resepsiyon aramasında filtre olarak kullanılır
- `bildirim_log` — WhatsApp/SMS/Portal Push/Email gönderim durumu (gönderildi/teslim edildi/okundu/hata); retry ve escalation için
- `randevu_degisiklik_log` — iptal/erteleme/terapist-oda değişikliği tarihçesi; audit ve hasta itirazlarında kanıt olarak kullanılır
- `vardiya_turu` — vardiya şablonları (ad, başlangıç/bitiş saati); `terapist_izin` buna referans verir
- `terapist_izin` — terapist izin/vardiya dışı saatleri; izin_turu (yıllık/hastalık/mazeret/eğitim), onay_durumu (bekliyor/onaylandı/reddedildi), onaylayan_kullanici_id; zaman çizelgesinde kapalı/gri görünür, bu aralığa randevu yazılması veritabanı kısıtıyla (constraint) engellenir; vardiya_turu şablonuyla birlikte otomatik çakışma kontrolü yapılır
- `bekleme_listesi` — slot boşalınca sıradaki bekleyen hastaya otomatik WhatsApp teklifi göndermek için
- `hasta_belge` — hasta MR/röntgen/doktor reçetesi/sevk belgesi; belge_turu, upload_tarihi, yukleyen_kullanici_id, metadata (JSONB), is_encrypted; tablet kamerasından çekilip/yüklenip hasta kartına bağlanır (Supabase Storage + Encrypted/Signed URL, max 10MB + otomatik compress); opsiyonel OCR (Tesseract.js/Google Vision) ile reçeteden işlem önerisi
- `stok_hareket` — islem_recete'nin otomatik düşümü dışında manuel stok girişleri (tedarik, fire, iade); tam envanter tarihçesi
- Materialized view'lar: `mv_terapist_performans`, `mv_klinik_doluluk`, `mv_hasta_aktiflik` (churn riski için)
- `seans_notu` — randevuya bağlı: ağrı skalası (VAS 1-10), yapılan egzersizler, sonraki seans notu, vücut haritası işaretleri (JSON — `{bolge, x, y, severity, not}`, örn. bel_l4_l5); en sık şikayet edilen 5 bölge dashboard'da gösterilir
- `islem_tanimi` — işlem adı + kademeli fiyat (`vita_fiyat` zorunlu/master, `plus_fiyat`/`elit_fiyat`/`prime_fiyat` opsiyonel override — boşsa `iskonto_oranlari`'ndan hesaplanır) + KDV oranı + Paraşüt hizmet kodu + `islem_kategori` (raporlama/filtreleme için); opsiyonel `gerekli_cihaz_id`
- `islem_kategori` — işlem kategorisi (örn. Fizyoterapi ve Rehabilitasyon, Manuel Terapi, Sporcu Recovery, Estetik/Cihaz Bazlı Tedavi); raporlama ve hasta portalı filtresinde kullanılır. Klinikte şu an uygulanan tedaviler: Fizyoterapi ve rehabilitasyon, Manuel terapi, Sporcu Recovery, G8 Terapi, Emscult Terapi, Footbalance ayak taban analizi, 3D Skolyoz Terapi (G8, Emscult, Footbalance ve 3D Skolyoz cihaz bağımlı — `cihaz` tablosunda karşılık gelen kayıtları olacak ve `islem_tanimi.gerekli_cihaz_id` ile bağlanacak)
- `islem_recete` — işlem tanımına bağlı sarf malzeme listesi (kuru iğne, kinezyo bandı, ultrason jeli vb.); her seansta otomatik stok düşümü, klinik bazlı stok uyarı eşiği, SKT kritik olanlarda batch/lot takibi
- `islem_tanimi_fiyat_gecmisi` — kademeli fiyat (vita/plus/elit/prime) + KDV + geçerlilik aralığı, `islem_tanimi` her değiştiğinde DB trigger'ıyla otomatik yazılır; `odeme_satiri`'nda o anki fiyat snapshot olarak saklanır (paket iade hesabı bu snapshot'tan gider)
- `iskonto_oranlari` — klinik başına tek satır; hasta kategorisine (vita/plus/elit/prime) göre tedavi fiyatına otomatik uygulanacak yüzdeler (`islem_tanimi_etkin_fiyat()` fonksiyonu tarafından kullanılır)
- `tedavi_protokolu` / `tedavi_protokolu_adimi` — klinik geneli protokol şablonu (ad+açıklama) + sıralı tedavi adımları listesi (`islem_tanimi_id`+`sira`+opsiyonel not); `hasta_protokol`'den (hastaya atanmış tekil tedavi+tarih aralığı) FARKLI bir kavram — sadece tanım/katalog, hastaya uygulama/atama akışı henüz yok
- `paket` / `paket_satis` — paket tanımı, geçerlilik süresi (expiry), hastaya satılan paket + kalan adet, süre dolunca dondurma/otomatik yakma durumu; iade edilirse kullanılan seanslar tekil fiyattan düşülüp kalan tutar `hasta_bakiye_hareket`'e kredi olarak işlenir (`paket_iade_politikasi`)
- `odeme` — tahsilat kayıtları, iskonto bilgisi, faturalı/faturasız durumu; yöntem: kredi kartı / banka havalesi / nakit; parçalı ödeme desteklenir (bir `odeme` kaydına birden fazla ödeme yöntemi/tutar satırı bağlanabilir — `odeme_satiri` alt tablosu)
- `hasta_bakiye_hareket` — ledger: her satır bir hareket (ödeme/iade/kredi/borç, tutar, referans); hasta bakiyesi bu tablodan türetilir, ayrı iade tablosu yok
- `kasa_kapanis` — günlük kasa kapama/mutabakat: tarih, personel, beklenen/sayılan tutar, fark, onay
- `fatura` — Paraşüt entegrasyon kaydı (fatura no, durum, e-Arşiv PDF linki, ödeme ile ilişki)
- `no_show_kural` — klinik bazlı ayarlanabilir: gelmedi durumunda paketten hak düşsün mü düşmesin mi
- `hasta_kullanici` — hasta portalı girişi; `id` = `auth.users.id`, `hasta_id` 1-1, `aktif` (erişim uzaktan kapatılabilir)
- `randevu_iptal_talebi` — hastanın portaldan gönderdiği randevu iptal talebi; `durum` (bekliyor/onaylandı/reddedildi), `klinik_id` trigger ile randevu'dan türetilir
- `audit_log` — kim, ne zaman, hangi hastanın hangi verisini gördü/değiştirdi (özellikle seans notu ve vücut haritası için kritik, KVKK)
- `sadakat_puan` — seans başına kazanılan puan, indirim/ücretsiz seans dönüşümü
- Not: abonelik modeli (aylık/3 aylık sınırsız veya kotalı) `paket` yapısının bir türü olarak ele alınacak; taksit takibi `odeme_satiri`ye vade alanı eklenerek çözülecek
- Tenant izolasyonu: `current_klinik_id()` helper fonksiyonu ile RLS (OkulCRM'deki desenle tutarlı — varsayım, teyit edilecek)

## İş Kuralları

**Randevu & Tablet**
- Randevu hem resepsiyon/sekreter panelinden hem terapist tarafından girilebilir
- Randevu takviminde sürükle-bırak + çakışma tespiti olacak; çakışma kontrolü oda + terapist + gerekli cihaz üzerinden yapılır (aynı anda iki odada aynı cihaz randevusu çakışmaz); grup seanslarında (is_grup_seansi) aynı slotta max_kapasite'ye kadar hasta kaydı yapılabilir, her hastanın paketinden 1'er hak düşer; terapist izinli/vardiya dışı saatlere veritabanı kısıtıyla randevu yazılamaz
- Randevudan 48s/24s/2s önce kademeli WhatsApp + hasta portalı bildirimi gönderilir
- Resepsiyon "Geldi/İçeri Alındı" (check-in) durumunu seçince ilgili odanın tablet ekranı otomatik güncellenir: hasta adı + yapılacak işlem
- Tablette hasta sıra numarası gösterilir
- QR kod ile hızlı giriş: hasta randevu QR'ını okutunca tablet otomatik hastayı ve işlemi gösterir; QR+NFC kart ile check-in desteklenir
- Resepsiyon panelinde telefon numarasıyla hızlı hasta arama olacak
- Gizlilik modu: tablette hasta soyadı maskeli gösterilebilir (örn. "Ahmet Y.") — KVKK/GDPR uyumlu, klinik/hasta bazlı açılabilir
- Tablet varsayılan olarak kiosk/salt-okunur modda çalışır; terapist not girmek veya çağrı başlatmak için 4 haneli PIN veya kendi QR kodunu okutarak oturum açar; 5 dk hareketsizlikte ekran koruyucuya döner (logo + "sıradaki hasta için hazırız")
- İlk girişte veya riskli işlem (kuru iğneleme, manuel terapi, yüksek yoğunluklu lazer vb.) öncesi tablette dijital onam/rıza formu gösterilir, hasta ekrana imza atar
- İşlem başladığında tablette geri sayım / kalan süre gösterilir
- Terapist tabletten resepsiyona dahili çağrı (malzeme/destek isteği) gönderebilir
- Offline queue çakışması: internet kesintisinde tablet/resepsiyon offline çalışır, bağlantı gelince aynı slot için çakışan kayıtlarda "resepsiyon önceliklidir" veya "ilk oluşturulan kazanır" (timestamp) kuralı uygulanır; çakışan randevular manuel reconciliation ekranında resepsiyona gösterilir
- Gelmedi (no-show) durumunda paketten hak düşülüp düşülmeyeceği `no_show_kural`'a göre belirlenir (klinik bazlı ayarlanabilir); `iptal_politikasi` ile randevudan X saat öncesine kadar ücretsiz iptal tanımlanır, sonrası hak düşer
- Grup seansına paketsiz hasta gelirse `islem_tanimi` fiyatından direkt tekil ödeme alınabilir, paket zorunlu değildir

**Seans & Klinik Notları**
- Her seans sonrası terapist paneline VAS ağrı skalası, yapılan egzersizler ve sonraki seans notu girilir (hızlı not şablonları ve sesli not/dikte ile)
- Şikayet bölgesi 2D vücut haritası üzerinde işaretlenebilir; anonimleştirilmiş "benzer vaka" önerisi sunulur
- Hasta MR/röntgen/doktor reçetesi/sevk belgesi tablet kamerasından çekilip/yüklenip hasta kartına bağlanabilir
- Tüm erişim/değişiklikler `audit_log`'a yazılır

**Ödeme & Fatura**
- Hastada işlem seçiminde: fiyat seçimi → iskonto uygulanabilir → faturalı/faturasız sorgusu → ödeme al butonu
- Ödeme parçalı alınabilir (aynı tahsilatta birden fazla yöntem: nakit + kredi kartı vb.); yöntem seçenekleri kredi kartı, banka havalesi, nakit
- Faturalı seçilirse ödeme sonrası otomatik Paraşüt üzerinden fatura kesilir (hastanın `parasut_contact_id`'si varsa kullanılır, yoksa oluşturulup saklanır — cari tekrar oluşmaz); fatura kesilince e-Arşiv PDF linki otomatik WhatsApp'tan hastaya gönderilir; API hatasında queue+retry devreye girer, kesilemezse "daha sonra kesilecek" uyarısı + manuel tetikleme seçeneği sunulur
- Paket alımında her işlemde paket adedinden düşüm yapılır; paketin geçerlilik süresi dolarsa haklar dondurulur veya otomatik yakılır (klinik ayarına göre); paket biterse veya bakiye azalınca (örn. son 2 seans) yenileme teklif mesajı otomatik gönderilir
- Abonelik modeli (aylık/3 aylık sınırsız veya kotalı paket) desteklenir; taksitli ödeme takip edilir
- Paket iadesinde kullanılan seanslar tekil işlem fiyatından düşülüp kalan tutar (klinik bazlı kesinti oranı düşüldükten sonra, örn. %10) otomatik kredi olarak iade edilir veya bakiyeye aktarılır
- Sadakat puanı: her seans puan kazandırır, puanla indirim veya ücretsiz seans alınabilir

**Hasta Portalı**
- Hasta kendi şifresiyle giriş yapıp randevularını, paket durumunu ve bakiye durumunu görebilir; şifre sıfırlama Supabase Auth + WhatsApp/SMS OTP ile yapılır (email zorunlu değil)
- Hasta randevu alabilir, iptal edebilir, değişiklik talep edebilir; değişiklik/iptal klinik/resepsiyon onayına tabidir (karşı onay olmadan otomatik geçmez)
- Randevu önerme motoru hasta tercihine (cinsiyet, uzmanlık alanı) göre uygun terapist/oda önerir
- Terapistin yüklediği ev egzersiz videoları ve ilerleme takibi (VAS skor grafiği, hareket açıklığı görselleri, iyileşme trendi) portalda gösterilir
- "Belgelerim" bölümünde hasta kendi MR/reçete/sevk belgelerini güvenli şekilde görüntüleyebilir

**WhatsApp**
- Randevu bildirimi etkileşimli butonludur (Geleceğim / İptal-Ertele); hasta iptal ederse zaman çizelgesinde slot boşalır veya resepsiyona uyarı düşer
- Paket durum ve bakiye bilgisi mesajları otomatik gönderilir
- WhatsApp Flows ile interaktif menü sunulur (randevumu gör / paket durumum / ödeme yap); template mesajlar Meta onayına tabidir; red riskine karşı SMS fallback (Twilio/Turkcell) hazır bulunur
- Opt-out: gelen "DUR" mesajı veya portaldan `whatsapp_izin_durumu`'nu false yapan akış olacak; `bildirim_log`'a kaydedilir

**Terapist & Personel**
- Terapist bazında günlük/haftalık/aylık işlem sayısı tutulur
- Maaş hesaplama 3 modelden birini destekler (klinik/terapist bazlı seçilir): sabit maaş; işlem başı prim (%X veya sabit ₺X); barajlı prim (belli seans sayısı üstü için bonus); nöbetçi/devir seanslarında prim ana terapiste mi seansı yapan terapiste mi yazılacağı klinik ayarıyla belirlenir
- Yol/yemek/mesai gibi ekstra hakedişler bordroya otomatik eklenir
- Personel rolüne göre yetkilendirme uygulanır (hangi rol hangi ekrana/işleme erişir)

**Analitik & Raporlama**
- Klinik dashboard: gerçek zamanlı KPI kartları (bugünkü doluluk %, bekleyen check-in, günlük tahmini ciro, aktif grup seansları), oda doluluk oranı, no-show oranı, gelir tahmini, terapist performans karşılaştırması, hasta yaşam döngüsü/churn risk tahmini, klinik sağlık skoru (doluluk+no-show+tekrar oranı), aylık karşılaştırmalı rapor (sektör ortalamasıyla)
- Muhasebe dashboard'unda günlük kasa tahsilatı, bekleyen faturalar, banka bakiyesi entegrasyonu (opsiyonel) ve günlük kasa kapanış/mutabakat ekranı (`kasa_kapanis`) olacak
- Excel/PDF rapor export (aylık ciro, KDV raporu, Paraşüt uyumlu)

## Konvansiyonlar
- Renk paleti: tasarım aşamasında `brand-ui` skill'iyle belirlenecek (klişe olduğu için "yeşil tonları" önerisi sabitlenmedi)
- Typography: Inter veya benzeri sans-serif
- Component library: Shadcn/ui + Tailwind
- Icon set: Lucide React
- Naming: TypeScript'te camelCase, DB'de snake_case

## Test & Kalite Güvencesi
- Unit test: Vitest + React Testing Library
- Integration test: Supabase
- E2E: Playwright — özellikle randevu → check-in → tablet akışı öncelikli
- Performance budget: tabletlerde 60fps hedefi

## MVP Tanımı (Sprint 1-3 sonrası)
- Temel randevu + tablet realtime
- Hasta kaydı + basit seans notu
- Basit ödeme (faturalı/faturasız)
- Temel raporlar

**Operasyonel Detaylar**
- Resepsiyon/terapist panellerinde pg_trgm ile full-text arama olacak (ad+soyad+telefon)
- Yeni klinik onboarding akışı: varsayılan oda/cihaz/işlem tanımı şablonları otomatik yüklenir, ilk terapist ve resepsiyon kullanıcıları oluşturulur
- Paraşüt kuyruğu dolduğunda klinik_admin'e haftalık "bekleyen faturalar" raporu gönderilir; kritik hatalarda (offline sync çakışması, fatura kesilemedi) `bildirim_log` üzerinden super_admin ve ilgili klinik_admin'e Slack/Email bildirimi gider
- Yazdırılabilir PDF çıktılar: randevu kartı, seans özeti, fatura, hasta ilerleme raporu
- Karanlık mod desteği (özellikle tabletlerde uzun kullanım için)
- Her sayfada contextual yardım/bilgi butonu olacak
- Supabase Point-in-Time Recovery + haftalık manuel kritik veri export politikası uygulanacak
- Önemli endpoint'lerde (randevu oluştur, ödeme al) rate limiting olacak (Edge Functions/Vercel Middleware)
- i18n-next altyapısı baştan kurulacak (en az TR+EN — temel disiplin, tam çeviri şimdilik şart değil); WCAG 2.2 AA temel kuralları (özellikle tablet kiosk kontrast/font) baştan gözetilecek, tam denetim kapsam dışı

## Yol Haritası / Durum
Sprint 1 tamamlandı: çekirdek şema Supabase'e uygulandı (klinik, kullanıcı/rol, personel/terapist, hasta, oda/cihaz, randevu +çakışma/kapasite/izin kontrolü, RLS, helper fonksiyonlar); `randevu` tablosu `supabase_realtime` yayınına eklendi (ayrı migration). Panelde çalışan ekranlar: giriş, randevu CRUD (liste/oluşturma/check-in-iptal), hasta CRUD (liste/arama/oluşturma/düzenleme/KVKK onayı), oda-cihaz yönetimi (ekleme/aktif-pasif), oda bazlı tablet realtime ekranı (`/panel/tablet/[odaId]`) — check-in postgres_changes ile sayfa yenilenmeden yansıyor. Tablet ekranı bilinçli olarak MVP kapsamında: staff login arkasında, device_token/kiosk-PIN/gizlilik modu bu turda yok (kullanıcı kararıyla ertelendi, CLAUDE.md'deki "Randevu & Tablet" iş kuralları hâlâ hedef durumu tanımlıyor).
Not (teknik keşif): `@supabase/ssr` browser client'ı realtime bağlantısına oturum JWT'sini otomatik geçirmiyor; RLS'li postgres_changes için `supabase.realtime.setAuth(session.access_token)` manuel çağrılması gerekiyor, yoksa abonelik "SUBSCRIBED" görünür ama hiç event gelmez.
Sprint 2 tamamlandı: ödeme/paket şeması + `odeme_olustur` RPC fonksiyonu (bkz. not) migration'a yazıldı ve uygulandı (`islem_kategori`, `islem_tanimi`, `paket`, `odeme`, `paket_satis`, `odeme_kalemi`, `odeme_satiri`, `hasta_bakiye_hareket` + RLS). Panelde tamam olan ekranlar: İşlem Tanımları (`/panel/islemler`), Paketler (`/panel/paketler`), hasta listesinden erişilen Hasta Detay + Ödeme Al (`/panel/hastalar/[id]`) — tekil işlem veya paket satışı, parçalı ödeme (nakit/kart/havale karışık), iskonto, faturalı/faturasız, ödeme geçmişi ve aktif paket listesi. Ödeme tam tutarda olmalı (kısmi/taksitli ödeme kapsam dışı — vade alanı ileride); `hasta_bakiye_hareket` şimdilik sadece audit-trail (her ödeme `tur='odeme'` yazar), "bakiye" kavramı (kredi/borç netleşmesi) henüz UI'da yok.
Paraşüt fatura kuyruğu kuruldu (`fatura` tablosu + `odeme_olustur`'a faturali=true'da otomatik `fatura` satırı ekleyen genişletme, migration `20260727150000`): Ödeme Geçmişi'nde her faturalı ödemede durum rozeti (bekliyor/kesildi/hata) + "Faturayı Kes" butonu (`faturaTetikle` action). **Henüz gerçek Paraşüt hesabı/API kimlik bilgisi yok** — buton şu an sabit "Paraşüt bağlantısı henüz kurulmadı" mesajı dönüyor, `fatura.durum` 'bekliyor' kalıyor (bilinçli erteleme: resmi API dokümantasyonu bu ortamdan doğrulanamadı — apidocs.parasut.com bot korumasından 403/404 döndü, GitHub wrapper README'leri de endpoint/alan adlarını netleştirmeye yetmedi; doğrulanmamış varsayımlarla client yazmak yerine gerçek hesap/doküman gelince yazılacak). `.env.local`'da `PARASUT_CLIENT_ID`/`SECRET`/`COMPANY_ID` yorum satırı olarak placeholder var. `hasta` tablosuna `vergi_no`/`vergi_dairesi`/`unvan`/`parasut_contact_id` kolonları da bilinçli olarak henüz eklenmedi — gerçek client yazılırken birlikte eklenecek (şimdiden şemayı şişirmemek için). Sıradaki: gerçek Paraşüt client'ı (hesap/dokümantasyon netleşince) + WhatsApp entegrasyonu (o da hesap/numara kurulumu bekliyor).
Not (teknik keşif): Bu sandbox'tan Supabase Postgres'ine doğrudan bağlantı (`supabase db push --db-url`) IPv6 kısıtlaması ve yanlış pooler domaini (`.co` değil `.com` olmalı, örn. `aws-0-eu-central-1.pooler.supabase.com`) yüzünden başarısız oluyordu; doğru pooler host + port 5432 (session mode, 6543 transaction-mode "prepared statement already exists" hatası veriyor) ile migration'lar buradan da push edilebiliyor — artık Dashboard SQL Editor'e elle yapıştırmaya gerek yok.
Not (teknik keşif): Base UI `Select`, değer programatik set edildiğinde (düzenleme formu, submit sonrası) `items` prop'u verilmeden ham value/UUID gösteriyor — yeni her Select kullanımında `items={liste.map(x => ({ value: x.id, label: x.ad }))}` eklenmeli, yoksa etiket bozuk görünür (veri doğru kaydedilir, sadece görünüm bozuk).
Not (mimari): Uygulamada ilk kez çoklu tablo yazan bir işlem (`odeme_olustur`) gerektiği için Postgres RPC fonksiyonu (plpgsql, SECURITY DEFINER) kullanıldı — önceki tüm mutasyonlar tek tablo insert/update'ti (randevu çakışması bile DB exclusion constraint + tek insert ile çözülüyor). Gelecekte benzer çok-tablolu atomik ihtiyaçlarda (örn. paket iade akışı) aynı RPC deseni izlenmeli.
Terapist Maaş + Performans modülü tamamlandı (migration `20260727180000`): `terapist`e prim/baraj kolonları, `personel_ekstra_hakedis` tablosu (yol/yemek/mesai/diğer) + RLS eklendi. Panel: `/panel/personel` (liste) ve `/panel/personel/[id]` (terapist için gün/hafta/seçili-ay tamamlanan seans sayısı, maaş breakdown, maaş ayarları formu ve hakediş ekleme klinik_admin'e açık, kendi verisini görme terapistin kendisine açık). Prim hesabı bilinçli olarak SEANS SAYISI bazlı (sabit ₺X/seans veya baraj-üstü bonus) — `odeme_kalemi` herhangi bir randevu/terapiste bağlı değil (ödeme hasta bazında alınır, seans bazında değil), bu yüzden ciro yüzdesi (%X) prim modeli bu turda kapsam dışı (kullanıcı kararı); gerekirse ileride `odeme_kalemi`ye `randevu_id` eklenerek çözülebilir.
Detaylı Hasta Kaydı tamamlandı (migration `20260727210000` + düzeltme `20260727220000`): `hasta`ya cinsiyet/eposta/referans_kanali/ozel_nitelikli_veri_onay_tarihi/ticari_ileti_onay_tarihi, yeni `hasta_hassas` tablosu (kimlik no+tipi UNIQUE, adres, acil durum kişisi, tıbbi geçmiş) + RLS eklendi. İki aşamalı kayıt (kullanıcı isteği): Hızlı Kayıt (`/panel/hastalar` formu) sadece ad-soyad + telefon + kimlik no/türü + WhatsApp/ticari ileti onayı alır (10 saniyelik akış); Detaylı Bilgiler (adres, acil durum, tıbbi geçmiş) Hasta Portalı'ndan (`/portal/bilgilerim`) hastanın kendisi doldurur — resepsiyon fallback'i olarak Hasta Detay'da da aynı form düzenlenebilir (`klinik_admin`/`resepsiyon`). Sağlık verisi alanları (kronik hastalık/ilaç/alerji/ameliyat/geliş sebebi) `ozel_nitelikli_veri_onay_tarihi` rızası olmadan kaydedilmez — form sessizce atlar, kullanıcıya "rıza kutucuğunu işaretleyin" mesajı döner; rıza hem portaldan (checkbox) hem panelden ("kağıt form imzalandıysa" butonu, KVKK onayıyla aynı desen) verilebilir. **Bulunan/düzeltilen hata**: portal migration'ında (`20260727200000`) `hasta` tablosuna sadece SELECT policy'si eklenmişti, UPDATE unutulmuştu — portaldan cinsiyet/eposta/rıza güncellemeleri RLS tarafından sessizce (hatasız, 0 satır) engelleniyordu; `20260727220000` ile düzeltildi. Ders: RLS INSERT/SELECT/UPDATE/DELETE dörtlüsü her yeni tablo/akışta tek tek kontrol edilmeli, "SELECT var" yeterli sanılmamalı.
Hasta Portalı (temel) tamamlandı (migration `20260727200000`): `hasta_kullanici` + `randevu_iptal_talebi` tabloları, `current_hasta_id()` helper, mevcut tablolara (hasta/randevu/paket/paket_satis/odeme/odeme_kalemi/odeme_satiri/hasta_bakiye_hareket/terapist/personel/oda) additive "kendi verisi" SELECT policy'leri. Giriş yöntemi bilinçli olarak WhatsApp/SMS OTP DEĞİL (sağlayıcı hesabı yok, Paraşüt'teki gibi aynı blokaj): resepsiyon/klinik_admin Hasta Detay'dan "Portal Erişimi Aç" ile geçici şifre üretir (`auth.admin.createUser` + sentetik e-posta `m-{hasta.id}@portal.local`, kullanıcıya asla gösterilmez/gönderilmez), hasta `/portal/giris`de TELEFON + şifreyle girer (`portal_giris_epostasi` SECURITY DEFINER RPC telefonu sentetik e-postaya çevirir, anon çağırabilir). Kapsam bilinçli olarak GÖRÜNTÜLEME + iptal talebi ile sınırlı (kullanıcı kararı): `/portal` — aktif paketler, yaklaşan/geçmiş randevular, ödeme geçmişi, "İptal talep et"; randevu alma/değişiklik talebi bu turda yok. İptal talebi hastadan direkt randevu satırını değiştirmez (RLS'te sütun bazlı kısıt yok) — ayrı `randevu_iptal_talebi` tablosuna INSERT eder, resepsiyon `/panel/randevular`daki "Bekleyen İptal Talepleri" kartından onaylar (randevu.durum='iptal' olur) veya reddeder. Panel ve portal aynı Supabase Auth cookie'sini paylaşır; `/portal/page.tsx` kendi içinde `hasta_kullanici` satırı var mı diye ayrıca doğrular (personel bir şekilde portale girerse veri görmez, RLS zaten engeller, ama sayfa da nazikçe `/portal/giris`e döner). Geçici şifre 6 haneli rakam (Supabase `minimum_password_length=6` ile uyumlu, önceki 10 haneli karışık karakterden daha kolay okunur/yazılır); "Portal Erişimi Aç"/"Şifreyi Sıfırla" sonrası çıkan mesajın altında wa.me tabanlı "WhatsApp'tan Gönder" linki var — gerçek WhatsApp Business API entegrasyonu henüz kurulmadığı için otomatik gönderim değil, resepsiyonun tıklayıp elle gönderdiği click-to-chat linki (`lib/utils.ts`'teki `whatsappLinkOlustur`).
Sprint 3+ fikirleri: çok kanallı bildirim merkezi (WhatsApp/portal/e-posta/SMS), AI destekli seans önerisi, çalışan puantaj+vardiya yönetimi, beyaz etiket tema özelleştirme.
Açık sorular: WhatsApp sağlayıcı hesabı (Meta Business/numara) kurulumu yapılmadı; yüz tanıma ile check-in fikri (opsiyonel) değerlendirme aşamasında; sarf malzeme stok yönetimi kapsamı (fiziksel envanter mi, sadece uyarı mı) netleşmedi; çoklu şube modeli ertelendi — şimdilik tek şube varsayılıyor, ileride karar verilecek (öneri: şube = ayrı klinik kaydı, parent_klinik_id ile gruplama).
Onaylandı: `islem_tanimi`'ye kategori alanı eklendi; klinikte uygulanan tedaviler netleşti (Fizyoterapi ve rehabilitasyon, Manuel terapi, Sporcu Recovery, G8 Terapi, Emscult Terapi, Footbalance ayak taban analizi, 3D Skolyoz Terapi). G8, Emscult, Footbalance ve 3D Skolyoz için `cihaz` kayıtlarının açılması gerekiyor — henüz cihaz modelleri/marka bilgisi netleşmedi.
Geri alındı (2026-07-28): Tedaviler ekranındaki kategori bölümü kaldırıldı — `islem_kategori` için hiç oluşturma ekranı yapılmamıştı, bu yüzden `islem_kategori_id` NOT NULL zorunluluğu yeni tedavi eklemeyi tamamen engelliyordu (migration `20260728140000` ile kolon nullable yapıldı). `islem_kategori` tablosu ileride raporlama/filtre için durabilir ama şu an hiçbir ekrandan yönetilmiyor/gösterilmiyor. Aynı turda "İşlem Tanımları" ekranı "Tedavi Tanımları" olarak yeniden adlandırıldı (başlık, "Tedavi Adı" etiketi, "Yeni Tedavi Tanımı"); Paraşüt Hizmet Kodu alanı formdan kaldırıldı (kolon DB'de duruyor, kullanılmıyor) — muhasebe tarafında artık sadece `muhasebe_hizmet_ismi` kullanılıyor ve bu alan Tedavi Adı yazılırken otomatik aynı değerle dolup, kullanıcı elle değiştirince senkronizasyon kesiliyor (hem ekleme hem düzenleme formunda).

## Rakip Analizi — Özellik Gap Analizi (Medicasimple benzeri diş/klinik yazılımı incelendi)

Zaten karşılanıyor: Takvim/randevu, mobil PWA, onam formları, kademeli WhatsApp hatırlatıcı, e-fatura (Paraşüt), hasta portalı, multi-tenant, RLS+rol bazlı yetkilendirme, hasta profilleri, pg_trgm arama, 3 model prim hesaplayıcı, stok yönetimi, kısmi gider takibi (kasa_kapanis), KPI dashboard, QR/NFC self check-in, online randevu, beyaz etiket, kısmi 3.parti entegrasyon (Paraşüt, WhatsApp).

Gerçek eksikler (henüz sprint'e alınmadı, önceliklendirme kullanıcıyla yapılacak):
- E-Nabız entegrasyonu (mevzuat açısından araştırılmalı, kapsam netleşince `api-integration`)
- Recall otomasyonu: paketi biten/uzun süredir gelmeyen hastaya otomatik mesaj (`bekleme_listesi`den ayrı; `mv_hasta_aktiflik` churn view'ından beslenebilir)
- Seans sonrası memnuniyet anketi otomasyonu
- İtibar yönetimi: memnun hastadan otomatik Google/harita yorumu isteme
- Fırsat raporu: bitmek üzere olan paket/yenileme potansiyeli olan hasta listesi
- Hasta segmentleri (pazarlama amaçlı gruplama — şikayet bölgesi, sadakat, son ziyaret)
- Online ödeme linki / SMS-QR ile ödeme (`payment-integration`)
- Gider/ön muhasebe modülü (şu an sadece kasa_kapanis var)
- Çok dilli tedavi planı/rapor çıktısı (yabancı hasta senaryosu)
- Beyaz etikette özel SMS başlığı/e-posta alan adı
- Dış geliştiriciye API erişimi / Zapier-Make entegrasyonu
- Çağrı merkezi entegrasyonu (düşük öncelik)

İlgisiz: diş hekimliğine özgü maddeler (Diş Şeması, Lab Vaka Takibi) kapsam dışı — fizyoterapi eşdeğeri `seans_notu` vücut haritasıyla zaten karşılanıyor.

## İş Modeli Notları
- SaaS fiyatlandırması klinik büyüklüğüne göre kademeli olacak (1-3 / 4-8 / 9+ terapist); beyaz etiket seçeneği düşünülüyor
- Asistan Merkezi çatısı altında kliniklerden anonimleştirilmiş veri toplayıp pazarlama amaçlı sektör istatistik raporu çıkarma fikri var
- İleride React Native ile terapist+hasta mobil uygulaması düşünülüyor (PWA temelinden native'e geçiş)

Hastalar ekranına elle doldurulacak, yazdırılabilir "Kayıt Formu (PDF)" eklendi (`/panel/hastalar/kayit-formu`): PDF üretimi için ayrı kütüphane eklenmedi (bilinçli — proje henüz hiç PDF üretim altyapısı kurmamıştı), bunun yerine tarayıcı `window.print()` + "PDF olarak kaydet" akışı kullanılan print-ready bir sayfa yapıldı; alanlar `hasta`/`hasta_hassas`/`hasta_veli` şemasıyla birebir (kimlik-iletişim, veli bilgisi, acil durum kişisi, tıbbi ön geçmiş, KVKK/rıza onay kutuları). Sidebar'ı print sırasında gizlemek için paylaşılan `sidebar.tsx`'e dokunmak yerine sayfaya özel inline `<style>` kullanıldı (o dosyada aynı anda başka bir oturumun tema/tasarım değişikliği sürüyordu, çakışmasın diye).

Anamnez formu genişletildi (migration `20260728170000`/`20260728171000`): Hasta Detay'daki "Detaylı Bilgiler & Anamnez" kartı, kullanıcının önerdiği tıklamalı Evet/Hayır UX'iyle yeniden yazıldı — 9 tıbbi ön geçmiş parametresinin her biri artık ayrı bir tri-state bayrak (`_var`, NULL/true/false) + "Evet" seçilince açılan detay metin kutusu; Alerji ve Kan Sulandırıcı kritik/kırmızı vurgulu, Kronik Hastalıklar/Alerjiler'de hızlı seçim etiketleri (chip), Hamilelik/Emzirme sadece cinsiyet=kadın için gösteriliyor. Yeni parametreler: bulaşıcı/enfeksiyöz hastalık, protez/kalp pili/implant, hamilelik-emzirme, sigara/alkol/madde. "Bizi Nereden Duydunuz" serbest metinden seçenekli dropdown'a çevrildi (mevcut özel değerler listeye otomatik eklenip kaybolmuyor). Yeni `oncelik_durumu` (normal/oncelikli/acil) triage rozeti + Hasta Detay başlığında alerji/kan sulandırıcı/acil durumunda otomatik beliren kırmızı uyarı rozetleri eklendi. Kayıt Formu (PDF) elle doldurma sayfası da aynı 9 parametre + öncelik alanıyla senkron.

Hasta Takibi modülü şeması uygulandı (migration `20260729090000` + seed `20260729091000`, pooler üzerinden `supabase db push --db-url` ile push edildi — **sadece DB şeması, panel/portal ekranları bu turda yapılmadı**): risk_bayraklari (hasta'ye JSONB, terapist de ekleyebilir — trigger ile sadece bu kolonla sınırlı) + islem_kontrendikasyon; hasta_hedef + olcek_tanimi (VAS/QuickDASH/Oswestry/Berg/SF-36, klinik_id NULL=platform şablonu) + hasta_olcum; egzersiz_kutuphanesi + ev_egzersiz_programi/kalemi/takip (portal "yaptım" işaretleyebiliyor); seans_checklist_sablonu + randevu_checklist; hasta_iletisim_log (bildirim_log hiç yoktu, tek tabloda birleştirildi) + hasta_anket + randevu_on_form (ikisi de portaldan yanıtlanabiliyor); hasta_iliski + hasta_sigorta; hasta_belge ve hasta_onam (CLAUDE.md'de tanımlıydı ama hiç CREATE edilmemişlerdi, ilk kez bu turda oluşturuldu — hasta_belge versiyonlama/is_guncel ile, hasta_onam append-only) + hasta_foto (onam_id NOT NULL, onamsız kayıt DB seviyesinde engelleniyor); v_hasta_ozet view + mv_hasta_ilerleme materialized view (matview RLS desteklemediği için ham matview client rollerinden REVOKE edildi, uygulama sadece üstündeki `v_hasta_ilerleme` view'ını sorgulamalı). Yeni generic `derive_klinik_id_from_parent()` ve `audit_log_yaz()` trigger fonksiyonları eklendi (önceki tüm klinik_id-türetme triggerleri tek tek yazılıyordu, artık parametrik tek fonksiyon; audit_log'a bu migration'dan önce hiç DB trigger'ı yazmıyordu, sadece app-level insert vardı). Seed'de QuickDASH/Oswestry/Berg/SF-36'nın gerçek soru metinleri bilinçli olarak uydurulmadı (telifli standardize ölçüm araçları) — sadece skorlama iskeleti dolduruldu, gerçek madde metinleri resmi kaynaktan doğrulanıp girilmeli. v_hasta_ozet.bakiye hesabı "kredi - borc" varsayımıyla yazıldı (CLAUDE.md'nin daha önce belirttiği gibi bu kavram henüz UI'da netleşmemişti); yanlışsa view kolayca güncellenebilir. Sıradaki: bu modülün panel ekranları (risk uyarı bandı, hedef/ölçüm trend grafiği, ev egzersiz atama, checklist doldurma, belge/foto yükleme UI'ı).

hasta_belge tüm görsel/dosya türlerini kapsayacak şekilde genişletildi (migration `20260729100000`), hasta_foto tablosu KALDIRILDI: kategori (radyoloji/klinik_foto/dokuman) + kategoriye bağlı kombine CHECK ile belge_turu alt tipleri, bolge (2D vücut haritasıyla aynı kodlar), cekim_tarihi (upload_tarihi'nden ayrı, sıralama buna göre), karsilastirma_grubu_id + asama (tedavi_oncesi/ara_kontrol/tedavi_sonrasi) önce/sonra serisi için, onam_id (kategori='klinik_foto' ise NOT NULL — CHECK ile DB seviyesinde zorunlu), thumbnail_path/dosya_mime/dosya_boyut_byte/exif_temizlendi eklendi; partial unique index `(hasta_id, belge_turu, bolge) WHERE is_guncel=true` oldu. hasta_foto'da 0 satır olduğu teyit edilip (migration veri taşıma adımını yine de idempotent/güvenli yazdı) tablo DROP edildi. **Postgres'te SELECT trigger yok** (donanımsal kısıt) — "kim hangi belgeyi görüntüledi" logu bu yüzden trigger değil, `hasta_belge_goruntule(id)` SECURITY DEFINER RPC'siyle yazılıyor: uygulama belgeyi gerçekten açarken (signed URL istemeden hemen önce) bunu çağırmalı; ham liste/galeri SELECT'leri audit'lenmiyor, sadece somut açma anı. Storage: `hasta-belge` bucket'ı (private, 20MB limit, sadece jpeg/png/webp/pdf) + `storage.objects` üzerinde path deseni (`{klinik_id}/{hasta_id}/{kategori}/{belge_id}.{ext}`) bazlı RLS policy'leri migration'a eklendi — gerçek Signed URL üretimi + 5 dk expiry parametresi uygulama katmanında (`createSignedUrl(path, 300)`) ayarlanacak, henüz UI/server action yazılmadı. `v_hasta_karsilastirma` view'ı eklendi (kesin ikili değil, `karsilastirma_grubu_id` bazlı satır listesi — eşleştirme uygulama tarafında). Sıradaki: panel/portal UI'ları hâlâ yok (bu ve önceki Hasta Takibi migration'ı sadece şema).

Hasta Detay sayfası 7 sekmeli yapıya geçirildi (`/panel/hastalar/[id]`): üst blokta her zaman görünen Risk Bandı (hasta.risk_bayraklari doluysa, terapist de ekleyebiliyor — DB'deki "sadece risk_bayraklari değişebilir" trigger'ıyla korunuyor) + Özet Kart (v_hasta_ozet'ten). Sekme state'i `?tab=` URL query param'da; Base UI Tabs kullanıldı (`components/ui/tabs.tsx`, yeni). TanStack Query eklendi (`@tanstack/react-query`, ilk kez bu turda) — her sekme kendi verisini `app/(app)/panel/hastalar/[id]/queries.ts`'teki hook'larla lazy fetch ediyor; QueryProvider `panel/layout.tsx`'e eklendi. Tam yapılan sekmeler: 1) Genel Bilgiler (kimlik/onam rozetleri, acil kişi, sigorta, hasta_iliski — bağlı kişiye tıklayınca o kartına gidiyor, veli bilgisi modülü henüz yok bu yüzden placeholder metin; Portal Erişimi buraya taşındı), 3) Tedavi & Anamnez (mevcut DetayliBilgilerKarti gömüldü, yeni 2D vücut haritası — gerçek koordinatlarla hasta_vucut_haritasi_isareti'ne yazıyor, Protokoller kartı — hasta_protokol'e yazarken DB trigger'ı blok/uyarı kontrolünü zaten yapıyor UI sadece mesajı gösterip onay alıyor, Hedefler listesi+ekleme+VAS hedeflerinde otomatik ilerleme çubuğu), 4) Gelişim & Ölçümler (VAS gerçek veriyle, ROM veri girildiğinde otomatik görünür — bağımlılıksız `components/hasta/trend-grafik.tsx` SVG grafikle, hedef karşılaştırma tablosu, önce/sonra galeri — Signed URL `hasta_belge_goruntule` RPC'si üzerinden, audit loglu). Kullanıcı kararıyla mevcut çalışan özellikler (Ödeme Al/Ödeme Geçmişi/Aktif Paketler, Periyodik Randevular) ilgili sekmelere (5, 2) taşındı, sadece o sekmelerin GERÇEKTEN eksik kısımları (taksitler, hızlı randevu oluşturma/no-show geçmişi) placeholder kaldı; Sekme 5 (Cari & Ödeme) terapist rolüne hiç gösterilmiyor. Sekme 6 (Belgeler & Medya) ve 7 (İletişim & Bildirimler) tamamen placeholder. Bu turda ayrıca (kullanıcı onayıyla) şema genişletildi: `hasta_vucut_haritasi_isareti`, `hasta_protokol` (+ DB seviyesinde kontrendikasyon trigger'ı) ve `olcek_tanimi`'ye ROM satırı (migration `20260729110000`) — CLAUDE.md'de tarif edilen `seans_notu.vücut_haritası` JSON konseptinin ilk kez ilişkisel karşılığı. Renk paleti: panelin mevcut `.dark` CSS değişkenleri (`--background:#090d16`, `--primary:#00f2fe`) zaten istenen paletle örtüştüğü için aynen kullanıldı — marketing route-group'una özel `tailwind.config.ts`'teki `primary-500`/`canvas` gibi custom class'lar panelde derlenmiyor (content path'i marketing'e scope'lu), o yüzden panelde kullanılmadı; emerald vurgular Tailwind'in stok `emerald-500` (=#10B981, zaten birebir eşleşiyor) ile yapıldı. `npx tsc --noEmit` ve `npm run lint` temiz; dev server'da route'un derlendiği (redirect ile) doğrulandı ama gerçek bir kullanıcıyla tarayıcıda uçtan uca test edilmedi (bu ortamda giriş bilgisi yok).

Belgeler & Medya sekmesi (yükleme hattı + Radyoloji görünümü) tamamlandı. Yükleme hattı Vercel body limitini aşmamak için tamamen client->Storage direkt: `createSignedUploadUrl` server action'da üretilir, dosya browser'dan `uploadToSignedUrl` ile doğrudan Supabase Storage'a yüklenir, DB satırı ayrı bir server action'da (`belgeKaydet`) oluşturulur. Client hazırlığı: HEIC/HEIF `heic2any` ile JPEG'e çevrilir, ardından (PDF hariç) `browser-image-compression` ile max 2000px+~1.5MB'a indirilir (canvas üzerinden yeniden kodlama EXIF/konum verisini doğal olarak siliyor) — iki yeni npm bağımlılığı bu turda eklendi. Thumbnail (300px) server tarafında `sharp` ile üretiliyor (zaten Next.js'in kendi image optimizer'ı için kurulu). **Kullanıcı onayıyla kapsam dışı bırakıldı**: PDF ilk sayfa thumbnail'i — sharp PDF rasterize edemiyor, gerçek render için gereken pdfjs-dist+@napi-rs/canvas native-binary riski nedeniyle bu turda eklenmedi, PDF'ler generic ikonla gösteriliyor. Kategori bazlı form (radyoloji/klinik_foto/dokuman, alt alanlar değişiyor); klinik_foto'da aktif `hasta_onam(onam_tipi='klinik_foto')` yoksa form kilitleniyor — gerçek tablet imza akışı bu projede henüz yok, "Onam Kaydet (kağıt form imzalandıysa)" butonu mevcut `ozel_nitelikli_veri_onay_tarihi` deseniyle aynı şekilde paper-form fallback'i olarak eklendi (migration `20260729120000` ile `hasta_onam.imza_storage_path` bu yüzden nullable yapıldı). Radyoloji görünümü tam: tarih azalan grid + lightbox (zoom/pan, negatif/invert, parlaklık-kontrast CSS filter, watermark'lı indirme — "Klinik adı+tarih" damgası canvas ile sadece indirilen dosyaya basılıyor, ekranda hiç görünmüyor). Signed URL'ler TanStack Query ile 4dk'da bir sessizce yenileniyor (5dk expiry'den önce), liste/galeri thumbnail'leri audit_log'a yazılmıyor (yeni `belgeThumbnailUrlAl` — sadece RLS'li SELECT+sign), gerçek "aç/görüntüle" (`hasta_belge_goruntule` RPC) ve "indir" (`belgeIndir`, ayrı 'indirme' audit satırı) ayrı loglanıyor. Soft delete: `hasta_belge.deleted_at` eklendi (migration `20260729120000`), terapist'in UPDATE (dolayısıyla silme) yetkisi DB RLS'inden kaldırıldı — terapist sadece yükleyip görebiliyor, klinik_admin/resepsiyon soft-delete, sadece klinik_admin kalıcı silebiliyor (hard delete zaten mevcut policy'deydi). **Kullanıcı onayıyla bu turda basit/placeholder bırakıldı**: Klinik Foto görünümü (önce/sonra sürüklenebilir slider + 3+ zaman çizelgesi — "ikinci adım" olarak görevde zaten ayrılmıştı, şimdilik grup bazlı düz grid), Doküman görünümü (PDF inline önizleme + versiyon geçmişi paneli — şimdilik liste + "En Güncel" rozeti), toplu seçim+ZIP indirme. `npx tsc --noEmit` ve `npm run lint` temiz (yeni kodda sıfır hata); dev server'da route derlemesi doğrulandı, gerçek kullanıcıyla uçtan uca test edilmedi (giriş bilgisi yok).

Hasta Detay sayfası mobil hub düzenine geçirildi (masaüstü 7 sekmeli yapı aynen korunuyor, `md` altında sekme barı gizlenip yerine özet kart + 2 sütunlu 7 kartlık grid gösteriliyor — `mobil-hub.tsx`, yeni). Karta tıklayınca `?tab=X`'e `router.push` ile gidiliyor (geri tuşu hub'a dönüyor), sekme değiştirme (masaüstü) hâlâ `router.replace`; her iki görünüm de aynı `sekmeIcerigi()` switch'ini paylaşıyor (JSX iki yerde tekrar etmiyor). Yeni `v_hasta_detay_ozet` view'ı (migration `20260729130000`) `v_hasta_ozet`'i genişletiyor: sonraki randevu, aktif protokol adı (şemada ayrı bir "tanı" alanı yok — kart grid'inde "Aktif tanı" etiketi için en yakın karşılık olarak `hasta_protokol`'ün aktif kaydı kullanıldı), ölçüm/belge sayısı, son iletişim tarihi — mobil hub'daki 7 kartın tamamı bu TEK view'dan `useHastaDetayOzet` (TanStack Query) ile besleniyor, sekme başına ayrı sorgu yok. Üst özet kartı (`ozet-kart.tsx`) sadeleştirildi: metrik satırı 5 kaleme indirildi (Son Seans/Kalan Paket Hakkı/Bakiye/Son VAS/No-show — Aktif Hedef kaldırıldı, zaten Tedavi & Anamnez sekmesinde var), cinsiyet rozeti ve telefonda `tel:` linki eklendi, "Aktif Program"/"Program Yok" rozeti eklendi. Palet kararı: görev tarifindeki hex değerler (#0F172A/#4FACFE vb.) mevcut `.dark` CSS değişkenleriyle (`--background`, `--card`, `--muted-foreground` birebir; `--primary` #00f2fe ile #4FACFE yakın ama tam eşleşmiyor) zaten örtüştüğü ve önceki turda aynı sebeple (route-scoped custom Tailwind class'ları panelde derlenmiyor) stok/semantic class kullanma kararı verildiği için, burada da hardcoded hex yerine mevcut `bg-card`/`text-primary`/`border-border` gibi semantic class'lar kullanıldı. `npx tsc --noEmit` ve `npm run lint` temiz (değişen dosyalarda sıfır hata; lint çıktısındaki 9 hata tamamen ilgisiz, önceden var olan dosyalarda); route derlemesi çalışan dev server üzerinden doğrulandı, gerçek kullanıcıyla/gerçek ekran genişliğiyle uçtan uca test edilmedi (bu ortamda giriş bilgisi yok).

Not (operasyonel keşif, 2026-07-29): Yeni bir deploy Vercel'de "Ready" görünse ve doğru commit'i gösterse bile custom domain'de eski tasarım/kod görünmeye devam edebiliyor — sebep Cloudflare'in (bu projenin DNS/proxy katmanı) sayfayı agresif cache'lemesi, Vercel'in dinamik/oturum bazlı sayfalar (`/panel/*`, `/portal/*`) için gönderdiği cache-control header'larını görmezden gelmesi. Teşhis: aynı deploy'un `.vercel.app` adresi (Cloudflare'i bypass eder) güncel tasarımı gösteriyorsa sorun kesin Cloudflare cache'idir. Çözüm: Cloudflare → Caching → Purge Everything (anlık), kalıcı çözüm için Page Rules/Cache Rules'da `/panel/*` ve `/portal/*` için "Cache Everything" gibi kuralların olmaması veya origin cache-control'e saygı duyacak şekilde ayarlanması gerekiyor. Yeni bir deploy sonrası "hiçbir şey değişmedi" şikayeti gelirse önce bunu kontrol et, kod/build tarafını tekrar tekrar sorgulama.

Adres seçici altyapısı eklendi (`turkey-neighbourhoods` npm paketi, DB tablosu/API yok — tamamen statik/yerel veri): `lib/turkiye-adres.ts` (getIller/getIlceler/getMahalleler — paket il KODU ile çalıştığı için ad→kod eşlemesi burada yapılıyor) + `components/ui/AdresSecici.tsx` (kademeli 3 native `<select>`, üst seviye değişince alt seviyeler sıfırlanır, `prefix` prop'una göre `{prefix}_il/_ilce/_mahalle` name'leri, `defaultIl/defaultIlce/defaultMahalle` ile düzenleme formlarında önceden doldurulabilir). Bilinçli olarak Base UI `Select` DEĞİL native `<select>` kullanıldı (81 il / binlerce mahalle'de Base UI'nin programatik value sorunu, bkz. yukarıdaki not, ve custom listbox performansı risk oluşturuyordu). **Veri modeli kararı (kullanıcı onayı)**: `hasta_hassas.adres` ve `klinik.adres` (mevcut serbest metin kolonlar) yanına ayrı `*_il`/`*_ilce`/`*_mahalle` kolonları eklenecek, `adres` kolonu sokak/cadde/no/daire detayı için kalacak; `personel`de hiç adres alanı yoktu, aynı desenle yeni eklenecek. **Bu turda sadece paylaşılan altyapı (paket+lib+bileşen) yapıldı — migration ve hasta/şirket/personel formlarına bağlama henüz yapılmadı, sıradaki iş bu.** `npx tsc --noEmit` ve eslint temiz; paketin `getDistrictsByCityCode`/`getNeighbourhoodsByCityCodeAndDistrict` fonksiyonları node ile runtime doğrulandı (Kadıköy mahalleleri doğru döndü).

2D vücut haritasına (Tedavi & Anamnez sekmesi) cinsiyete göre figür desteği eklendi: `lib/vucut-bolgeleri.ts` yeniden yapılandırıldı — bölge path'leri (`d`) artık `BOLGE_YOLLARI[cinsiyet][kod]` altında cinsiyet bazlı tutuluyor (kadın/erkek siluetleri farklı olduğu için path'ler de farklı — kullanıcı kadın figür görselleri sağladı), `VUCUT_BOLGELERI` sadece kod/ad/yüz (ortak) listesine indirgendi; yeni `figurGorseli(cinsiyet, yuz)` ve `yuzunBolgeleri(cinsiyet, yuz)` helper'ları eklendi. Görseller `public/vucut/body-{erkek|kadin}-{on|arka}.webp` olarak adlandırıldı (eski `body-front.webp`/`body-back.webp` → `body-erkek-on.webp`/`body-erkek-arka.webp` git mv ile taşındı, kadın görselleri kullanıcıdan eklendi). `VucutHaritasi` bileşeni yeni `cinsiyet` prop'u alıyor (Tedavi & Anamnez sekmesinden `hasta.cinsiyet` geçiliyor); `cinsiyet` null/`belirtilmemis` ise sessizce 'erkek' figürüne düşüyor ve haritanın üstünde düşük vurgulu tek satır bilgi notu gösteriliyor (bilgi bandı değil, sade `text-xs text-muted-foreground`). Bölge kodları cinsiyetler arası ortak olduğu için kayıtlı işaretler (hasta_vucut_haritasi_isareti) cinsiyet güncellemesinde hiçbir migration/dönüşüm gerektirmeden korunuyor. Aktif olmayan yüz `onMouseEnter` ile (`new window.Image()`) prefetch ediliyor, `yuzunBolgeleri` sonucu `useMemo` ile bellekleniyor. `npx tsc --noEmit` ve `npm run lint` temiz (değişen dosyalarda sıfır hata).

Adres seçici hasta tarafına bağlandı (migration `20260730120000`): `hasta_hassas`e `il`/`ilce`/`mahalle` (nullable text) kolonları eklendi, `adres` kolonu artık sadece sokak/cadde/bina no/daire için serbest metin. `AdresSecici` bileşeni Hasta Detay > Kişisel Bilgiler sekmesindeki `DetayliBilgilerKarti` formuna (`prefix="adres"` → `adres_il`/`adres_ilce`/`adres_mahalle` alanları, `hastalar/[id]/actions.ts`'teki `detayliBilgileriGuncelle`'a okunuyor) ve Hasta Portalı `/portal/bilgilerim`deki `BilgilerFormu`'na (aynı desen, `portal/actions.ts`'teki `bilgileriGuncelle`) bağlandı; salt okunur "Acil Durum Kişisi & Adres" kartında da mahalle/ilçe/il + açık adres iki satır olarak gösteriliyor. `klinik.adres` ve `personel` adres alanları bu turda kapsam dışı (CLAUDE.md'nin önceki notundaki plana göre ayrı bir turda aynı desenle eklenecek). `npx tsc --noEmit` ve eslint temiz; gerçek kullanıcıyla uçtan uca test edilmedi (giriş bilgisi yok).

Adres seçici klinik ve personel tarafına da bağlandı (migration `20260730130000`, `20260730140000`): `klinik`e `il`/`ilce`/`mahalle` eklendi (Şirket Bilgileri formu, `sirket-bilgileri-formu.tsx`/`actions.ts`), `personel`e daha önce hiç olmayan `adres`+`il`/`ilce`/`mahalle` eklendi (Yeni Personel formu — `personel-formu.tsx`/`actions.ts`, opsiyonel alan olarak). Personel tarafında şu an sadece oluşturma formu var, sonradan düzenleme için ayrı bir form/action yok (CLAUDE.md'de daha önce not edildiği gibi) — bu yüzden adres sonradan sadece DB'den elle veya ileride eklenecek bir "personel düzenle" ekranından güncellenebilir. `hasta_hassas`/`klinik`/`personel` üçü de artık aynı deseni paylaşıyor: `adres` (sokak/cadde/no/daire, serbest metin) + `il`/`ilce`/`mahalle` (AdresSecici dropdown). `npx tsc --noEmit` ve eslint temiz.

Personel listesi + detay yeniden düzenlendi: `/panel/personel` üstte arama çubuğu (isim/görev/telefon, client-side filtre — personel sayısı büyük olmadığı için hasta listesindeki server-side `q` deseni yerine tamamen client'ta) + göreve göre gruplama (grup adları ve grup içi personel alfabetik) + telefon kolonu (`kullanici:kullanici_id(telefon)` join'i) eklendi; Detay artık terapist olmayan personel için de açık (önceden sadece terapistlerde "Detay" linki vardı). `/panel/personel/[id]` üstte isim+telefon kartı + 2 `ModuleCard` kutucuğu (Kişisel Bilgiler / Ödemeler, `?tab=` ile) yapısına geçti; Ödemeler'deki Ekstra Hakedişler kartı artık terapist olmayan personel için de gösteriliyor (`personel_ekstra_hakedis` zaten terapist'e değil personel_id'ye bağlıydı, sadece UI kısıtlıyordu).

Personel Detay'a Kişisel Bilgiler düzenleme akışı eklendi — kullanıcının yapıştırdığı taslak migration REFERANS ALINDI ama canlıya hiç uygulanmamıştı (information_schema ile doğrulandı) ve ciddi hatalar içeriyordu: `personel_acil_kisi`/`personel_mesleki_belge` RLS policy'leri var olmayan `p.user_id` kolonuna referans veriyordu (doğrusu `p.kullanici_id`) — düzeltilip migration `20260730150000` ile push edildi. Ek düzeltmeler (kullanıcıya bildirilerek): `personel.telefon`/`ev_adresi`/`ise_baslama_tarihi` eklenmedi (sırasıyla `kullanici.telefon`, `personel.adres`+il/ilce/mahalle, mevcut `personel.ise_giris_tarihi` ile aynı bilgiyi ikinci kez tutacaktı); `cinsiyet` check değeri `hasta.cinsiyet` ile tutarlı olsun diye `belirtilmemis` yapıldı (taslakta `belirtilmedi`); `ozel_yetki_override` jsonb kolonu şema olarak eklendi ama hiçbir RLS/UI onu okumuyor — yarım/etkisiz bir yetki checkbox'ı UI'a eklenmedi (kullanıcı onayı gerektirecek ayrı bir iş). **Şifreleme kararı (kullanıcıya soruldu, "gerçek pgcrypto kur" seçildi)**: `personel_hassas` (tc_kimlik_sifreli/pasaport_no_sifreli, pgp_sym_encrypt) eklendi — CLAUDE.md'nin önceki `hasta_hassas` kararının (pgcrypto YOK, RLS-only) TAM TERSİ, bilinçli olarak bu tabloya özel bir istisna. Anahtar migration'a GÖMÜLMEDİ (sır repo'ya yazılmaz) — kullanıcının veritabanında bir kere `ALTER DATABASE postgres SET app.settings.encryption_key = '<güçlü-rastgele-değer>';` çalıştırması GEREKİYOR, yoksa `personel_hassas_kaydet` RPC'si "sifreleme_anahtari_kurulu_degil" hatası döner (Paraşüt'ün "bağlantı henüz kurulmadı" desenine benzer nazik placeholder). Düz metin hiç client'a dönmüyor — `personel_hassas_maskeli_getir` RPC'si sadece son 2 haneyi döner. Mevcut `personel.tc_kimlik_no` (düz metin, önceki turdan) BİLİNÇLİ OLARAK dokunulmadı/taşınmadı — anahtar kurulu değilken canlı veriyi şifreleyip kolonu silmek geri alınamaz bir adım olurdu; yeni form artık SADECE `personel_hassas`'a yazıyor, eski kolon eski kayıtlarda duruyor (anahtar kurulunca istenirse backfill+drop ayrı bir migration'da yapılabilir). **Güvenlik**: rol seçicide `super_admin` BİLİNÇLİ OLARAK yok — bu rol platform yöneticisi (klinik_id NULL) ve klinik_admin'in kendini/başkasını buna yükseltebilmesi multi-tenant izolasyonunu kırardı. Doğrulama sırasında ayrı bir gerçek güvenlik açığı bulundu ve düzeltildi (migration `20260730151000`): her iki RPC'nin `IF NOT (a OR b OR c) THEN RAISE EXCEPTION 'yetkisiz'` deseni (mevcut `musteri_belge_goruntule`'daki gibi) Postgres'in üç değerli mantığında session/`auth.uid()` context'i olmadığında `(a OR b OR c)` NULL'a düşüyor, `IF NOT NULL` de NULL olduğu için RAISE hiç çalışmıyordu (fail-open) — ham psql/superuser bağlantısıyla (session'sız) test edilirken fark edildi, PostgREST üzerinden gerçek authenticated istekte `auth.uid()` hep dolu olduğu için production'da tetiklenemiyordu ama yine de `COALESCE(..., false)` ile fail-closed'a çevrildi ve session'sız çağrıda artık gerçekten `yetkisiz` döndüğü doğrulandı. Personel oluşturma formu 3-4 adımlı wizard'a çevrildi (Kişisel Bilgiler → İş Bilgileri → Sistem Yetkileri → [rol=terapist ise] Mesleki Belgeler), tüm adımlar tek `<form>` içinde `hidden` class'ıyla gizli tutuluyor (state kaybı olmadan, ayrı bir stepper state kütüphanesi gerekmedi); aynı `PersonelFormu` bileşeni `mod="duzenle"` ile Personel Detay'daki yeni "Düzenle" dialogunda da kullanılıyor (klinik_admin'e açık; terapist/resepsiyon self-view hâlâ salt okunur ve kısıtlı alanlarla sınırlı). Kaşe görseli için `personel-belge` Storage bucket'ı (private, klinik_admin-only) eklendi; hasta_belge'deki gibi ayrı signed-upload-url akışı KURULMADI — dosya doğrudan server action FormData'sında alınıp admin client ile yükleniyor (kaşe görselleri küçük olduğu için Vercel body-limit riski hasta belgelerindeki kadar değil, personel_id oluşturma anında henüz yokken bir pre-signed-url akışı da zaten kurulamazdı). TC kimlik doğrulaması `lib/tc-kimlik.ts`'te gerçek checksum algoritmasıyla (node ile bilinen test numaralarına karşı doğrulandı). Görev tarifindeki "code-standards/security-baseline/auth-flow/saas-patterns" skill'leri bu ortamda kurulu değildi (`.claude/skills` yok) — mevcut kod konvansiyonları ve proje geçmişindeki kararlar referans alınarak ilerlendi. `npx tsc --noEmit` ve `npm run lint` temiz; migration'lar CLI ile canlıya push edildi ve information_schema/RPC çağrısıyla doğrulandı; gerçek kullanıcıyla uçtan uca test edilmedi (giriş bilgisi yok).

Detaylı Bilgiler & Anamnez kartına (Hasta Detay > Kişisel Bilgiler) Ad Soyad/Telefon/Doğum Tarihi eklendi ve gerçek bir RLS hatası düzeltildi (migration `20260731090000`): `detayliBilgileriGuncelle` action'ı `terapistDahilYetkiliHastaGetir` ile terapist'in de bu formu kaydedebilmesine zaten izin veriyordu, ama `hasta_hassas_insert`/`hasta_hassas_update` RLS policy'leri hâlâ sadece klinik_admin/resepsiyon'a izin veriyordu — bir terapist bu formdan adres/acil durum kişisi/anamnez girip kaydettiğinde UI "kaydedildi" gösteriyordu ama satır RLS tarafından sessizce güncellenmiyordu (upsert'in UPDATE dalı RLS koşulunu sağlamadığında hatasız 0 satır etkileniyor), bir sonraki ziyarette veriler "silinmiş" gibi görünüyordu — bu, kullanıcının bildirdiği "adres/acil durum kişisi bilgileri siliniyor" şikayetinin kök nedeniydi. Policy'lere `current_rol() = 'terapist'` eklendi (CLAUDE.md'nin daha önceki "RLS INSERT/SELECT/UPDATE/DELETE her akışta tek tek kontrol edilmeli" dersiyle aynı sınıf hata). Yeni alanlar: Ad Soyad (ilk harf otomatik büyük, `isimBasHarfBuyukYap`), Telefon (+90 sabit önek + 10 haneli yerel input, `lib/utils.ts`'teki `telefonYerelHaneleriCikar` ile hızlı kayıttaki mevcut değerden önceden dolduruluyor), Doğum Tarihi — üçü de `hasta` tablosunda zaten var olan kolonlar, hızlı kayıt (`hasta-formu.tsx`) ile aynı kaynağı paylaşıyor (bilerek `hasta-basligi.tsx`'teki mevcut ayrı düzenleme formuna dokunulmadı, iki yerden de düzenlenebilir kalıyor). Doğum tarihi girilince `resitDegilMi` (lib/utils.ts) ile canlı yaş hesaplanıp 18 yaşından küçükse anne/baba adı+telefonu alanları açılıyor (`hasta_hassas`e yeni `anne_adi`/`anne_telefon`/`baba_adi`/`baba_telefon` kolonları, aynı migration) — CLAUDE.md'nin veri modelinde tarif edilen daha genel `hasta_veli` (yakınlık alanlı, tekil) tablosu yerine kullanıcının özellikle istediği anne/baba ayrımıyla, mevcut `hasta_hassas` deseniyle tutarlı basit text kolonlar olarak eklendi; "Kişisel Bilgiler" sekmesindeki "Veli Bilgisi" kartı da bu veriyi gösterecek şekilde güncellendi (önceden "modül henüz eklenmedi" placeholder'dı). TC Kimlik No client'ta canlı uyarı gösteriyor (11 haneden farklıysa). **Düzeltilen ikinci regresyon (aynı gün, kullanıcı bildirdi)**: bu alan ilk yazıldığında server'da da `superRefine` ile 11 hane zorunlu kılınmıştı (sert red) — üretimde `hasta_hassas`'ta 10 haneli (11 değil) `kimlik_no_tipi='tc'` ile kayıtlı 2 gerçek hasta bulundu (uzunluk kontrolü hiç yokken girilmiş legacy veri, `SELECT length(kimlik_no), count(*) FROM hasta_hassas WHERE kimlik_no_tipi='tc' GROUP BY 1` ile teyit edildi), bu hastalarda formun HİÇBİR alanı (kullanıcı kimlik_no'ya hiç dokunmasa bile — 18 yaş altı anne/baba dahil) artık kaydedilemiyordu, `ayristirma.success` en baştan false dönüyordu. superRefine kaldırıldı, sadece client uyarısı kaldı (orijinal istek zaten "uyarılsın" diyordu, "reddedilsin" değil) — ders: mevcut/legacy veriye yeni bir sert validasyon eklemeden önce üretimde o validasyonu ihlal eden satır var mı kontrol edilmeli. Aynı turda "Veli Bilgisi" ayrı kartı kaldırıldı, anne/baba özet verisi "Aile / Yakın Bağlantıları" kartının üstüne taşındı (kullanıcı anne/baba bilgisini orada aradı, ayrı bir kart beklemiyordu). `npx tsc --noEmit` ve `npm run lint` temiz (değişen dosyalarda sıfır hata); migration pooler üzerinden push edilip information_schema/pg_policies ile doğrulandı; gerçek kullanıcıyla uçtan uca test edilmedi (giriş bilgisi yok).

**Üçüncü regresyon (aynı özellik, kullanıcı yine bildirdi)**: "Tıbbi Ön Geçmiş girilirken kayıtta kalmıyor" — kök neden, Detaylı Bilgiler & Anamnez'in TEK bir form/action olmasıydı: `detayliBilgileriGuncelle` her submit'te `hasta.telefon`'u dokunulmasa bile "+90..." formatına yeniden yazıyordu (bkz. yukarıdaki telefon notu), bu terapist için hasta tablosunda GERÇEK bir değişiklik sayılıp `hasta_terapist_sadece_risk_guncelle` trigger'ını (terapist sadece risk_bayraklari değiştirebilir) tetikliyor ve TÜM formu (tıbbi geçmiş dahil) reddediyordu. Kalıcı çözüm (migration `20260731100000` + `actions.ts`/`detayli-bilgiler-karti.tsx` yeniden yapılandırma): form ikiye ayrıldı — **Temel Bilgiler formu** (Ad Soyad/Telefon/Doğum Tarihi/Cinsiyet/E-posta/Referans/Kimlik/Adres/Acil Durum/Veli — `temelBilgileriGuncelle`, sadece klinik_admin/resepsiyon, `duzenlenebilir` prop'una göre gösteriliyor/gizleniyor) ve **Anamnez formu** (Tıbbi Ön Geçmiş/Geliş Sebebi/Öncelik — `anamnezGuncelle`, terapist + klinik_admin/resepsiyon, hasta tablosuna hiç dokunmuyor, hasta_hassas'a sadece anamnez kolonlarını yazıyor). Aynı ayrım artık DB seviyesinde de zorunlu: `hasta_hassas_terapist_kisitla` (UPDATE) ve `..._ekleme_kisitla` (INSERT) trigger'ları terapist'in bu tabloda SADECE anamnez kolonlarını değiştirebilmesini garanti ediyor (`hasta.risk_bayraklari` için kullanılan desenle aynı) — API doğrudan çağrılsa bile kimlik/adres/veli değiştirilemiyor. Aynı turda kullanıcının istediği "Diğer Yakını" (ad soyad + telefon + yakınlık derecesi) alanı anne/baba'nın yanına eklendi (`hasta_hassas.diger_yakini_*`), tüm veli telefonları (anne/baba/diğer yakını) artık hasta'nın kendi telefonuyla aynı ortak `TelefonGirisi` bileşenini (+90 sabit önek + 10 hane) kullanıyor — bu alanlarda server-side format zorunluluğu YOK (sadece hasta'nın kendi telefonunda var), bir önceki iki regresyonun dersiyle (ikincil/opsiyonel alanlara sert validasyon eklememe) tutarlı. **Genel ders (üç kez tekrarladı)**: terapist gibi kısıtlı bir role açılan tek bir form/action'a idari alanlar (isim/telefon/kimlik) eklemek, o alanlara bağlı HERHANGİ bir kısıt (RLS, trigger, validasyon) anamnez gibi ilgisiz alanların da kaydını sessizce engelleyebiliyor — klinik/idari veri girişleri baştan ayrı action'larda tutulmalı. `npx tsc --noEmit` ve `npm run lint` temiz; migration pooler üzerinden push edilip information_schema/pg_trigger ile doğrulandı; gerçek kullanıcıyla uçtan uca test edilmedi (giriş bilgisi yok).

"Kişisel Bilgi Giriş Formu" (Detaylı Bilgiler & Anamnez kartı yeniden adlandırıldı) başına 3 onay bloğu eklendi (migration `20260731111500`): KVKK/Sağlık Verisi İşleme/Ticari İleti onayı — her biri ⓘ ikonuyla açılan pop-up açıklama (`Dialog`, sabit metin `ONAY_ACIKLAMALARI`), onay durumu+tarihi, **onaylayan kişi** ve (onaylanmadıysa, sadece klinik_admin/resepsiyon'a `duzenlenebilir` prop'una göre görünen) "Onayla" tuşu içeriyor. Onaylayan iki türden biri: `'personel'` (panelden onaylayan `kullanici.id` + join'li `ad_soyad`) veya `'hasta'` (hasta.ad_soyad gösterilir, ayrı id saklanmaz) — **"QR kodla hasta girişi" bu projede henüz ayrı bir self-servis form akışı değil** (CLAUDE.md: QR şu an sadece randevu check-in için, tablet kiosk MVP'de ertelendi), bu yüzden "hasta kendisi doldurdu/onayladı" senaryosu mevcut Hasta Portalı (`/portal/bilgilerim`) akışıyla eşleştirildi — kullanıcıya bildirilen bilinçli bir yorumlama kararı. `hasta`'ya 3 onay için `*_onaylayan_tip`+`*_onaylayan_kullanici_id` (6 kolon), `hasta_hassas`'a formu dolduran/son değiştiren için `olusturan_tip`/`olusturan_kullanici_id`/`son_guncelleyen_tip`/`son_guncelleyen_kullanici_id` eklendi — ikincisi, `hasta.risk_bayraklari` deseniyle aynı şekilde client'tan gelen değere göre DEĞİL, `trg_hasta_hassas_denetim` trigger'ıyla sunucu session'ından (`current_rol()`/`current_hasta_id()`) OTOMATİK türetiliyor (spoof edilemez, app kodu bu kolonları hiç göndermiyor). Bu yeni trigger'ın her UPDATE'te `son_guncelleyen_*`'i değiştirmesi, terapist kısıtlama trigger'ını (üç kez art arda bulunan aynı sınıf hata) YİNE tetikleyebilirdi — bu yüzden aynı migration'da `hasta_hassas_terapist_sadece_anamnez_guncelle` güncellenip bu iki denetim kolonu karşılaştırma allowlist'ine eklendi (trigger adı alfabetik sırayla "denetim" < "terapist_kisitla" olduğu için NEW zaten atanmış oluyor, sıralama sorunsuz). "Formu dolduran" + "son değişiklik" (varsa) bilgisi kartın en altında, her iki alt-form'un (Temel Bilgiler + Anamnez) DIŞINDA tek bir footer'da gösteriliyor (`hasta_hassas` tek satır olduğu için iki formdan hangisi son dokunduysa onu yansıtıyor). Kullanıcı isteğiyle "Öncelik / Aciliyet Durumu" alanı Anamnez formundan kaldırıldı (`anamnezGuncelle`/`anamnezSemasi`'den `oncelik_durumu` tamamen çıkarıldı — upsert'e dahil edilmediği için mevcut değer korunuyor, DB kolonu ve onu okuyan Hasta Detay başlığı rozeti/Kayıt Formu PDF'i dokunulmadı). **Operasyonel not**: migration push sırasında BAŞKA bir (bu oturumdan bağımsız, eşzamanlı çalışan bir oturumun) migration dosyasıyla (`20260731110000_randevu_gelis_bakiye_isleme.sql` — bkz. bir alttaki not) aynı timestamp'te çakışma yaşandı; ilk push denemesi o dosyanın DDL'ini kısmen uyguladı ama `schema_migrations` kayıt adımı hataya düştü (idempotent DDL olduğu için zararsız), bu migration `20260731111500`'e kaydırılarak çakışma çözüldü — diğer dosyaya/dosyalarına dokunulmadı. `npx tsc --noEmit` ve `npm run lint` (dokunulan dosyalarda) temiz; migration pooler üzerinden push edilip information_schema/pg_trigger ile doğrulandı; gerçek kullanıcıyla uçtan uca test edilmedi (giriş bilgisi yok). **Takip düzenlemesi (aynı gün, kullanıcı isteğiyle)**: onay satırlarındaki bordered kutu + ⓘ pop-up tasarımı kaldırıldı — artık satır etrafında çerçeve yok, açıklama metni yanında sabit görünen bir "Açıklama:" kutusunda düz metin olarak yazıyor (tıklama gerekmiyor), "Onayla" butonu yerine gerçek bir `<input type="checkbox">` ("onay kutusu") kullanılıyor — işaretlenince `onayAction` tetikleniyor, onaylandıktan sonra işaretli+disabled kalıyor (geri alma UI'ı yok, mevcut KVKK/sağlık verisi onaylarıyla tutarlı).

Randevu check-in'de otomatik bakiye/paket işleme eklendi (migration `20260731110000`, kullanıcı kararı): Randevular'da "Geldi" butonuna basıldığında artık düz bir durum güncellemesi değil, yeni `randevu_gelis_isaretle` RPC'si (SECURITY DEFINER, `odeme_olustur` ile aynı atomik-fonksiyon deseni) çalışıyor — randevunun `islem_tanimi_id`'sine göre: hastanın o işlem için geçerli (aktif, `kalan_adet`>0, süresi dolmamış) bir paketi varsa `paket_satis.kalan_adet` 1 azalır ve `randevu.paket_satis_id` (yeni kolon, izlenebilirlik için) o pakete işaret eder; paket yoksa `islem_tanimi.fiyat` tutarında `hasta_bakiye_hareket`'e `tur='borc'` satırı eklenir (yeni `randevu_id` kolonuyla randevuya bağlı) — Hasta Detay > Cari & Ödeme > "Bakiye Hareketleri" kartında görünür. **Kullanıcı kararıyla bilinçli olarak append-only**: randevu sonradan iptal edilse/durum değişse bile bu borç kaydı veya paket düşümü OTOMATİK GERİ ALINMAZ, gerekirse resepsiyon elle iade/kredi hareketi girer (mevcut ödeme/paket_satis akışlarıyla aynı "hiç güncellenmez/silinmez" ledger felsefesi). Yetki kontrolü RLS'e değil fonksiyon içindeki manuel kontrole dayanıyor (yine `odeme_olustur` deseni): klinik_admin/resepsiyon serbest, terapist sadece kendi randevusunu işaretleyebiliyor (randevu tablosundaki `randevu_update_terapist_kendi` policy'siyle aynı sahiplik kontrolü fonksiyon içinde tekrarlandı). `randevuDurumGuncelle` action'ı artık `{success, message}` döndürüyor (öncesinde void'di) ve `DurumButonlari` bu sonucu (paketten mi düşüldü, ne kadar borç eklendi) buton altında gösteriyor — tek çağıran yer olduğu için imza değişikliği güvenliydi. Migration pooler üzerinden push edildi, RPC'nin canlıda var olduğu service-role çağrısıyla doğrulandı; `npx tsc --noEmit` ve `npm run lint` temiz (lint'teki 9 hata önceden var olan ilgisiz dosyalarda). Gerçek kullanıcıyla uçtan uca test edilmedi (giriş bilgisi yok).

Randevu sonuç seçenekleri genişletildi (migration `20260731120000`/`20260731130000`, kullanıcı kararı): `randevu_durum_tipi` enum'ına `gecikmeli_geldi` ve `ertelendi` eklendi (`ALTER TYPE ... ADD VALUE`, aynı transaction'da kullanılamadığı için kullanan mantık ayrı bir sonraki migration'a bölündü), `randevu`ya `gecikme_dakika` (nullable) kolonu eklendi. Randevu Çizelgesi'nde bir randevuya tıklanınca açılan panelde artık her zaman 5 sonuç butonu var: Geldi, Gecikmeli Geldi (tıklayınca dakika giren mini form açılır), Gelmedi, Ertelendi (tıklayınca yeni tarih+saat giren mini form açılır), İptal — **hiçbiri varsayılan olarak seçili/vurgulu görünmez** (hepsi eşit ağırlıkta outline buton, kullanıcı isteği) ve **randevunun mevcut durumu ne olursa olsun hepsi her zaman tıklanabilir** (önceki "sadece planlandi'dan çıkış" kısıtlaması tamamen kaldırıldı — resepsiyon yanlış işaretlemeyi serbestçe düzeltebilsin, kullanıcı kararı). "Gecikmeli Geldi" fiilen bir geliştir: bir önceki turda eklenen check-in paket/bakiye mantığıyla (`randevu_gelis_isaretle` RPC, artık `p_gecikme_dakika` parametresi de alıyor) BİREBİR aynı şekilde ya paketten düşer ya borç yazar (kullanıcı kararı). "Ertelendi" girilen yeni tarih/saati DOĞRUDAN randevunun `baslangic`/`bitis`'ine işler (aynı süre korunarak, ayrı bir randevu OLUŞTURULMAZ — kullanıcı kararı) — yeni `randevuErtele` action'ı, oda/terapist/cihaz çakışma kontrolü (23P01) yeni zaman için normal şekilde çalışıyor. Artık TÜM 5 seçenek her zaman tıklanabilir olduğu için RPC'ye idempotency kontrolü eklendi: aynı randevu için `paket_satis_id` zaten doluysa veya `hasta_bakiye_hareket`te bu `randevu_id` için zaten `borc` satırı varsa, tekrar "Geldi" tıklansa bile paket/borç mantığı İKİNCİ KEZ ÇALIŞMAZ (sadece durum güncellenir) — çift ücretlendirme/çift paket düşümü riskine karşı, önceki turdaki "sadece planlandi'dan" kısıtlaması kaldırılınca gerekli hale geldi. Renklendirme (kullanıcı isteği): Geldi/Gecikmeli Geldi yeşil (emerald), İptal kırmızı (destructive), Ertelendi açık mavi (sky) — bu arada **gerçek bir görsel hata da düzeltildi**: `randevu-kutusu.tsx`'te "iptal" durumu daha önce hiç `kutuRenk` tanımlamıyordu, bu yüzden iptal edilen randevular kırmızı DEĞİL, tedavi rengini (soluk) gösteriyordu — hem çizelgedeki kutu hem Hasta Detay > Gelişim & Ölçümler'deki seans zaman çizelgesi (`seans-gecmisi-zaman-cizelgesi.tsx`, orada da "iptal" `bg-muted` gösteriyordu) için düzeltildi. `randevuDurumGuncelle` artık sadece gelmedi/iptal için (basit güncelleme); "Geldi"/"Gecikmeli Geldi" yeni `randevuGelisIsaretle`, "Ertelendi" yeni `randevuErtele` action'ında — `DurumButonlari` artık `randevuId`+`durum` yerine tüm `randevu` satırını prop alıyor (erteleme formunun varsayılan tarih/saatini `randevu.baslangic`'ten dolduruyor). Terapist prim/seans sayısı hesaplamaları (`lib/raporlar/hesaplamalar.ts`, Personel Detay günlük/haftalık/aylık seans sayısı) ve tablet ekranının "içeride olan hasta" tespiti artık `gecikmeli_geldi`'yi de `geldi` ile birlikte sayıyor. `npx tsc --noEmit` temiz; `npm run lint` temiz (9 hata önceden var olan ilgisiz dosyalarda, aynı baseline). Migration'lar pooler üzerinden push edildi, yeni enum değerleri/kolonlar/RPC service-role çağrısıyla canlıda doğrulandı. Gerçek kullanıcıyla uçtan uca test edilmedi (giriş bilgisi yok) — özellikle "Ertelendi" akışının gerçek bir çakışma senaryosunda denenmesi öneriliyor.

Randevu Çizelgesi'ndeki "Tamamlandı" otomatik durumu kaldırıldı (kullanıcı kararı, DB/migration değişikliği yok — sadece `randevu-kutusu.tsx`): `gorunumDurumuHesapla` daha önce randevunun bitiş saati geçince (staff'ın hiçbir işlem yapmasına gerek kalmadan) kutucuğu otomatik "Tamamlandı" (soluk gri) gösteriyordu — bu, check-in'i unutulmuş "Planlandı" bir randevuyu bile yanıltıcı şekilde "tamamlanmış" gibi gösteriyordu. Artık kutucuk sadece DB'deki gerçek `durum`u gösteriyor, süre geçmesiyle KENDİLİĞİNDEN değişmiyor — resepsiyon kutucuğa girip Geldi/Gecikmeli Geldi/Gelmedi/Ertelendi/İptal'den birini elle seçmediği sürece görünüm aynı kalıyor. Tek istisna korundu: "Seansta" (yeşil nabız rozeti) — bu, zaten elle "Geldi" işaretlenmiş bir randevuya seans saati gelince eklenen salt görsel bir vurgu (durumu değiştirmiyor, sadece üstüne biniyor), kaldırılması istenmedi ve kaldırılırsa `canli-cizelge.tsx`'teki "Şu an seansta" duyurusu/footer sayacı da kırılırdı. `GorunumDurumu` tipinden ve `DURUM_STIL` map'inden artık hiç üretilmeyen "tamamlandi" girdisi de temizlendi (ölü kod bırakılmadı). `npx tsc --noEmit` ve `npm run lint` temiz (aynı 9 önceden var olan ilgisiz hata baseline'ı).

Tedaviler sidebar öğesi 2 kutucuklu bir gruba (Tedaviler / Tedavi Protokolleri) çevrildi — mevcut Muhasebe/Ayarlar hub deseni (`MENU_GRUPLARI` + `MenuGrubuSayfasi` + `ModuleCard`) aynen tekrar kullanıldı, yeni bir hub bileşeni yazılmadı: `lib/panel/menu-gruplari.ts`'e `tedaviler` grubu eklendi, sidebar'daki statik "Tedaviler" linki kaldırılıp `/panel/tedaviler` (`MenuGrubuSayfasi`) hub sayfası eklendi. Yeni "Tedavi Protokolleri" (`/panel/tedavi-protokolleri`, migration `20260731140000`) kullanıcı kararıyla "ad + sıralı tedavi adımları" olarak tanımlandı: yeni `tedavi_protokolu` (ad/açıklama/aktif) + `tedavi_protokolu_adimi` (`islem_tanimi_id`+`sira`+opsiyonel not, `UNIQUE(tedavi_protokolu_id, sira)`) tabloları, `islem_tanimi`/`paket` ile birebir aynı RLS deseni (herkes görür, sadece klinik_admin yönetir) ve `klinik_id` yine `derive_klinik_id_from_parent()` generic trigger'ıyla türetiliyor. Ekran, Tedaviler (`/panel/islemler`) sayfasıyla aynı "üstte başlık+Yeni Ekle butonu, altta liste" iskeletini birebir tekrar ediyor (kullanıcının "çalışma listesi aynı, liste üstü yeni ekle" isteği) — `IslemFormu`/`IslemSatiri` kopyalanmadı, aynı görsel/etkileşim deseni yeni `ProtokolFormu`/`ProtokolSatiri` olarak protokol+adım verisine uyarlandı. Adım editörü (`adim-editor.tsx`) dinamik ekle/sil/yukarı-aşağı taşı destekliyor; adım listesi FormData'nın çoklu-aynı-isimli-input deseniyle (`getAll`) DEĞİL, tek bir `adimlar_json` hidden input'una serialize edilerek gönderiliyor — Base UI Select'in her adım için ayrı `Select.Root` hidden input'u (aynı `name`) üretip üretmediği/DOM sırasını koruyup korumadığı bu projede hiç denenmemiş bir varsayım olacağından, JSON serileştirme daha güvenilir görüldü. Düzenlemede adımlar tamamen silinip yeniden ekleniyor (transaction/RPC yok — bu bir fiyat kataloğu/tanım ekranı, `odeme_olustur` gibi parasal atomiklik gerektirmiyor). Protokolün hastaya uygulanması/atanması (mevcut `hasta_protokol` ile ilişki) bu turda KAPSAM DIŞI, sadece tanım/katalog CRUD'u yapıldı. `npx tsc --noEmit`, `npm run lint` (dokunulan dosyalarda sıfır hata, mevcut 9 hata ilgisiz/önceden var) ve `next build` temiz; migration pooler üzerinden push edilip information_schema ile doğrulandı; gerçek kullanıcıyla uçtan uca test edilmedi (giriş bilgisi yok).

Hasta Detay > Tedavi & Anamnez > "Seans / Tedavi Geçmişi" zaman çizelgesinde bir seansa tıklanınca, kullanıcının paylaştığı referans görsele (başka bir klinik yazılımından "Tedavi (tarih saat) / Fizyoterapist / Antrenör / Tanı / Tedavi Türü / Tedavi Protokolü" kartı) birebir uyan bir detay kartı eklendi (migration `20260731150000`, kullanıcı kararı): `randevu`ya 3 yeni nullable kolon — `tani` (serbest metin), `antrenor_id` (→ `personel`, terapistten bağımsız; yeni bir `kullanici_rol_tipi` DEĞİL, `personel.gorev` zaten serbest metin olduğu için "Antrenör" iş unvanı yeni bir enum'a gerek kalmadan giriliyor) ve `tedavi_protokolu_id` (→ bir önceki turda eklenen `tedavi_protokolu` kataloğu). Bilinçli tasarım kararı: bu 3 alan `hasta_protokol`'e (hasta bazlı tek tedavi kursu ataması) DEĞİL doğrudan `randevu`ya eklendi — "Tedavi Türü" (`islem_tanimi_id`) zaten aynı desende randevu'da tutuluyordu ve görseldeki kart yapısı da seans/randevu bazlı, kurs bazlı değil. Kart, `seans-gecmisi-zaman-cizelgesi.tsx` içine gömüldü (ayrı bir bileşen/sekme değil) — bir seans seçilince `<li>` içinde `TedaviBilgisiKarti` inline açılıyor, `useHastaSeansGecmisi`'nin zaten çektiği satırdan besleniyor (ekstra sorgu yok). Düzenleme, Randevu Çizelgesi'ndeki mevcut Randevu Detay paneline (`randevu-detay-paneli.tsx`) eklendi — Tanı (metin), Antrenör ve Tedavi Protokolü (ikisi de opsiyonel Select, boş bırakılabilir) — kullanıcı kararıyla SADECE düzenleme panelinde, randevu OLUŞTURMA formuna eklenmedi (bu 3 alan seans sırasında/sonrasında doldurulacak klinik detaylar olarak ayrıldı, randevu.terapist_id/oda_id/islem_tanimi_id gibi zorunlu planlama alanlarından bilinçli olarak ayrı tutuldu). Yetki: kullanıcı kararıyla mevcut `randevu_update_terapist_kendi` RLS policy'si (satır bazlı, kolon kısıtı yok) zaten kendi randevusunu düzenleyen terapist'i kapsıyor — bu 3 nullable kolon için ayrı bir RLS/trigger YAZILMADI (önceki turlardaki 3 art arda hasta_hassas regresyonunun aksine, burada idari/kimlik alanı yok, kısıtlı bir role yeni bir çakışma riski taşımıyor). Antrenör seçenek listesi TÜM aktif personelden geliyor (kullanıcı kararıyla `gorev='Antrenör'` filtresi YOK — görev serbest metin olduğu için yazım tutarsızlığı yanlış filtrelemeye yol açabilirdi). Veri çekme/prop threading iki randevu çizelgesi call-site'ına da (`/panel/randevular`, ana ekran `/panel`) eklendi — bu turda aynı anda çalışan başka bir oturumun `CanliCizelge`'ye eklediği `rol`/`kendiTerapistId` (seansta hastasına git linki) props'larıyla çakışmadan yan yana durdu, birbirine dokunulmadı. `npx tsc --noEmit` temiz; `npm run lint` temiz (9 hata önceden var olan ilgisiz dosyalarda, aynı baseline). Migration pooler üzerinden push edilip dry-run ile doğrulandı; gerçek kullanıcıyla uçtan uca test edilmedi (giriş bilgisi yok).

Arşiv İçe Aktarma eklendi — Ayarlar altında yeni bir bölüm (migration `20260804090000`, kullanıcı kararı: "önceden kullanılan programdan arşiv çekme ve yükleme"): `/panel/ayarlar/arsiv-ice-aktarma` mini-hub'ından 3 sıralı alt bölüme gidiliyor — **Hastalar**, **Randevu/Seans Geçmişi**, **Ödeme & Paket Geçmişi** (Belgeler bilinçli olarak KAPSAM DIŞI bırakıldı — gerçek dosya+metadata gerektiriyor, spreadsheet satırından farklı bir akış). Kaynak program adı/format detayı netleşmediği için en evrensel seçenek olan Excel/CSV hedeflendi; her bölüm aynı 3-4 adımlı sihirbaz iskeletini paylaşıyor (Dosya Yükle → Sütun Eşleştir → [Randevu/Ödeme'de: dosya başına bir kere terapist/oda/paket eşleştirme] → İçe Aktar & Sonuç), paylaşılan bileşenler `components/panel/ice-aktarma/` ve `lib/ice-aktarma/`'da (`dosya-oku.ts`, `sutun-esleme.tsx`, `onizleme-tablosu.tsx`, `normallestir.ts`, `hasta-eslestirme.actions.ts`). **Mimari karar**: bu, projenin ilk TOPLU + satır bazlı hata toleranslı yazma akışı — `odeme_olustur`/`randevu_gelis_isaretle` tek kayıt için tasarlanmıştı (bir hata = tüm işlem reddedilir), burada yüzlerce satır tek RPC çağrısında işleniyor ve her satır kendi `BEGIN/EXCEPTION` alt bloğunda (plpgsql örtük savepoint) işlenip `{satir_no, durum: eklendi|atlandi|hata, sebep}` olarak biriktiriliyor — randevu tablosundaki oda/terapist/cihaz exclusion constraint'i gibi nedenlerle bazı satırlar başarısız olsa bile diğerleri etkilenmiyor. 4 yeni SECURITY DEFINER RPC (`hasta_arsiv_ice_aktar`, `randevu_arsiv_ice_aktar`, `hasta_bakiye_hareket_arsiv_ekle`, `paket_satis_arsiv_ekle`) — hepsi bilinçli olarak **sadece `klinik_admin`**'e kilitli (resepsiyon dahil değil; hassas kimlik verisi + finansal ledger + geri alınamaz toplu insert, mevcut Yetkilendirme/Şirket Bilgileri ile aynı sınıf), canlıda service-role ile "yetkisiz" hatası döndüğü doğrulandı. `randevu` ve `hasta_bakiye_hareket`'e yeni `kaynak` kolonu (`'uygulama'|'arsiv'`, default `'uygulama'`) eklendi — ileride canlı KPI/no-show/ciro raporlarının eski verilerle karışmaması için düşük maliyetli bir iz bırakma kolonu (henüz hiçbir rapor sorgusu bunu filtrelemiyor, ileride eklenebilir). **Ödeme/Paket importu bilinçli olarak `odeme_olustur`'u ÇAĞIRMAZ** — ayrı bir RPC, `fatura` kuyruğuna asla satır eklemez (retroaktif veri girişi, gerçek bir tahsilat değil). **Bilinçli KAPSAM DIŞI**: KVKK/sağlık verisi/ticari ileti onay tarihleri bu akıştan set edilmiyor (eski bir satırdan gerçek rıza kaydı üretmek hukuki bir varsayım olurdu, klinik bu onayları kendi akışlarıyla ayrıca almalı). Hasta importunda telefon/kimlik_no dedup var (aynı dosya güvenle tekrar yüklenebilir); Randevu/Ödeme importunda YOK (bilinçli, sonuç ekranında "tekrar yüklemeyin" uyarısı var). Randevu/Ödeme sihirbazları hasta eşleştirmesini **telefon numarasıyla** otomatik yapıyor (`hastaTelefonlariniEslestir`, TanStack Query ile — CLAUDE.md'nin daha önce belgelediği yeni `react-hooks/set-state-in-effect` lint kuralına takılmamak için raw `useEffect`+`setState` yerine bilinçli olarak `useQuery` kullanıldı, terapist/oda/paket eşleştirmesi de aynı sebeple useEffect DEĞİL üç adımlı öneri/override `useMemo` deseniyle çözüldü); terapist/oda/işlem eşleştirmesi dosya başına bir kere (satır satır değil) yapılıyor. **Yeni bağımlılık kararı**: `xlsx` (SheetJS) npm paketi KULLANILMADI — son npm sürümü (0.18.5) 2 yüksek önemli düzeltilmemiş güvenlik açığı taşıyor (prototip kirliliği + ReDoS, tam da bu özelliğin tehdit modeli olan "kullanıcı yüklediği dosyayı parse etme" senaryosunda doğrudan istismar edilebilir), SheetJS npm'de yama yayınlamıyor; onun yerine aktif bakımı süren `exceljs` kullanıldı (moderate bir `uuid` transitive bulgusu var ama exceljs'in bizim kullanım yolumuzda tetiklenmeyen dar bir edge-case, `xlsx`'inkinden çok daha düşük risk). CSV için ayrı bir kütüphane eklenmedi, elle yazılmış küçük bir ayrıştırıcı kullanıldı (Türkçe Excel'in `;` ayracını otomatik algılıyor — `,` ondalık ayracı olduğu için). Migration pooler üzerinden push edilip 4 RPC + 2 `kaynak` kolonu service-role çağrısıyla canlıda doğrulandı; `npx tsc --noEmit`, `npm run lint` (yeni kodda sıfır hata, 9 önceden var olan ilgisiz hata aynı baseline) ve `next build` temiz (4 yeni route dahil derlendi). Gerçek bir eski-program export dosyasıyla uçtan uca test edilmedi (örnek dosya yok) — özellikle Randevu bölümünün terapist/oda eşleştirme adımı ve exclusion-constraint çakışma raporlama akışının gerçek veriyle denenmesi öneriliyor.

Ayarlar altına QR Kodları eklendi (migration `20260804100000` + düzeltme `20260804110000`, kullanıcı kararı: 4 kare kod — Hasta Ön Kayıt/Personel Başvurusu/İş Başvurusu/Anket ve Öneriler): `/panel/ayarlar/qr-kodlar` (klinik_admin-only) her biri için client-side üretilen (yeni `qrcode` npm paketi — üçüncü parti bir QR API'ye veri gönderilmiyor) bir kare kod kartı gösteriyor; her kart okutulunca `/kayit/hasta/[klinikId]`, `/kayit/personel/[klinikId]`, `/basvuru/is/[klinikId]`, `/anket/[klinikId]` altında GİRİŞSİZ (anon, `middleware.ts`'teki `/panel`+`/portal` koruması bu path'leri kapsamıyor) bir form açıyor. **Kullanıcı kararları**: Hasta Ön Kayıt mevcut hızlı-kayıt alan setiyle birebir (ad soyad/telefon/kimlik no opsiyonel/doğum tarihi + KVKK onayı ZORUNLU) doğrudan `hasta`+`hasta_hassas`'a yazıyor — ayrı bir onay ekranı yok, Hastalar listesinde direkt görünür. Personel Başvurusu **"onay bekleyen taslak"** (yeni `personel_basvuru_taslagi` tablosu) — hiçbir hesap/personel kaydı OTOMATİK oluşmuyor, klinik_admin `/panel/ayarlar/qr-kodlar/personel-basvurulari`'dan onaylayınca sadece `durum='onaylandi'` işaretleniyor, gerçek personel+giriş kaydını admin mevcut Personel sayfasından elle açıyor (PersonelFormu'nun karmaşık 3-4 adımlı wizard'ına prefill/otomatik-dönüştürme eklenmedi — kapsam/risk tradeoff'u, admin taslak kartındaki bilgiyi kopyalayıp giriyor). İş Başvurusu **"basit form, detay sonra"** — yeni `is_basvurusu` tablosu, CV yükleme/pipeline YOK, sadece ad-soyad/telefon/e-posta/pozisyon/mesaj + `/panel/ayarlar/qr-kodlar/is-basvurulari`'da durum güncelleme (yeni/incelendi/kabul/reddedildi). Anket ve Öneriler **"isim/telefon opsiyonel"** — yeni `anket_yaniti` tablosu (1-5 yıldız + serbest metin öneri, ikisinden en az biri zorunlu), `/panel/ayarlar/qr-kodlar/anket-yanitlari`'da salt-okunur liste + ortalama puan.

**RLS mimarisi (proje için ilk kez)**: bu 4 akışın hepsi `auth.uid()` NULL olan gerçek anonim (`anon` Postgres role) istekler — projede ilk kez bir tabloya doğrudan `anon`'a `GRANT`lı `TO anon` INSERT policy'si eklendi (önceki tek anon-erişimi `portal_giris_epostasi` salt-okunur bir RPC'ydi). Blast radius'u daraltan iki kritisch karar: (1) `hasta`ya yeni `kayit_kanali` kolonu (`'resepsiyon'|'qr_self_servis'`, default `'resepsiyon'`) — `hasta` için anon INSERT policy'si `WITH CHECK (kayit_kanali='qr_self_servis' AND kvkk_onay_tarihi IS NOT NULL AND kvkk_onaylayan_tip='hasta')`, `hasta_hassas` için anon INSERT policy'si SADECE `kayit_kanali='qr_self_servis'` olan bir hasta'ya referans veren satırlara izin veriyor — yani QR'ı ele geçiren biri bile keyfi/mevcut bir hastaya sahte kimlik verisi iliştiremiyor, sadece kendi oluşturduğu satıra. (2) anon'a HİÇBİR yeni SELECT policy'si eklenmedi (`hasta`/`hasta_hassas`/3 yeni tabloda) — dedup/mevcut-kayıt kontrolleri (panelin authenticated `hastaOlustur` action'ındaki gibi) bilinçli olarak YAPILMADI, çünkü anon'a "bu kimlik no zaten var mı" sorgusu açmak PII probe/enumeration açığı olurdu; bunun yerine mevcut UNIQUE(klinik_id, kimlik_no) constraint'ine güvenilip 23505 hatası kullanıcı dostu bir mesaja çevriliyor (hasta satırı geri alınmıyor — anon'a DELETE grant'i de verilmedi, aynı gerekçe). **Canlıda bulunan ve düzeltilen gerçek bir RLS hatası** (migration `20260804110000`): `hasta_hassas` policy'si başta `hasta`ya doğrudan bir `EXISTS` alt sorgusuyla bakıyordu, ama bu alt sorgu da current role'ün (anon) `hasta` üzerindeki RLS SELECT policy'lerine tabi — anon'un `hasta`da HİÇ SELECT policy'si olmadığı için (yukarıdaki PII kararı) `EXISTS` her zaman false dönüp meşru `hasta_hassas` insert'lerini de reddediyordu; `current_klinik_id()` ile aynı desende yeni bir `SECURITY DEFINER` helper (`hasta_qr_self_servis_mi`) RLS'i bypass ederek asıl kontrolü yapacak şekilde eklendi. **İkinci gerçek hata** (aynı turda, kod yazılırken fark edildi, migration'a gerek kalmadan düzeltildi): Supabase'de `INSERT ... RETURNING` (yani `.insert().select()`) her zaman SELECT policy'sine de tabi — anon'un `hasta`da SELECT policy'si olmadığı için `.select("id").single()` zincirlemek "new row violates row-level security policy" hatası veriyordu (WITH CHECK doğru olsa bile); çözüm RETURNING'e hiç ihtiyaç duymamak — `hasta.id` sunucu tarafında `crypto.randomUUID()` ile üretilip insert payload'ına konuyor, `hasta_hassas.hasta_id` için aynı değer kullanılıyor. **Bu iki hata da canlı veritabanına karşı (test klinik + admin/anon key'lerle) gerçek insert/select denemeleriyle bulundu ve doğrulandı** — sadece dry-run/tsc/lint değil, önceki turların "gerçek kullanıcıyla test edilmedi" notundan farklı olarak bu turda anon-role senaryosu fiilen çalıştırılıp KVKK-onaysız insert reddi, yabancı-hasta'ya hassas veri iliştirme reddi ve anon'un kendi/başka hiçbir satırı SELECT edememesi tek tek doğrulandı (test satırları temizlendi).

**Bilinen/kabul edilen kısıt (dokümante edildi, çözülmedi)**: bu 4 public route'ta rate limiting YOK (CLAUDE.md'nin "Önemli endpoint'lerde rate limiting olacak" hedefi bu turda uygulanmadı) — QR linkini/URL'sini ele geçiren biri teorik olarak toplu sahte kayıt/başvuru gönderebilir; `kayit_kanali` ayrımı sadece hasta_hassas tarafında blast radius'u daraltıyor, spam'i engellemiyor. Aynı kimlik no ile iki kez QR'dan kayıt olunursa (veya kişi zaten resepsiyon kaydındaysa) ikinci `hasta_hassas` insert'i 23505 ile reddedilip kullanıcıya nazik mesaj dönüyor ama YARIM (`hasta_hassas`sız) `hasta` satırı silinmiyor (rollback anon'a DELETE grant'i gerektirirdi, bilinçli tercih edilmedi) — nadir, düşük risk, resepsiyon elle temizleyebilir. `npx tsc --noEmit` ve `npm run lint` temiz (9 hata önceden var olan ilgisiz baseline, `react-hooks/set-state-in-effect`'e takılmamak için `QrKart` da `useQuery` kullanıyor — Arşiv İçe Aktarma turundaki aynı desen); `next build` 8 yeni route dahil temiz derlendi. Personel/İş Başvuruları/Anket review ekranlarının gerçek kullanıcı arayüzüyle uçtan uca (tarayıcıda) testi yapılmadı (giriş bilgisi yok) — sadece DB seviyesinde doğrulandı.

Hasta Detay > Randevu & Seans sekmesindeki "Randevu Listesi" placeholder'ı gerçek veriyle dolduruldu: `/panel/hastalar/[id]/randevu/page.tsx` artık `randevu` tablosundan hastanın TÜM randevularını (gelecek+geçmiş, tüm durumlar, `baslangic` DESC, limit 100 — hem "randevu arşivi" hem "Randevu Ekle" ile yeni randevu oluşturma aynı kartta) çekip `RandevuSeansSekmesi`'ne prop olarak geçiyor; liste tarih+saat, tedavi, terapist, oda, durum rozeti (gecikmeli geldiyse dakika ekli) ve arşivden içe aktarılmış kayıtlarda (`kaynak='arsiv'`, bkz. Arşiv İçe Aktarma notu) "Arşiv kaydı" etiketi gösteriyor. Tekrarı azaltmak için `RANDEVU_DURUM_TONLARI` (StatusBadge ton eşlemesi) `types/hasta-detay.ts`'e taşınıp export edildi — daha önce sadece `seans-gecmisi-zaman-cizelgesi.tsx` içinde yerel bir sabitti, şimdi ikisi de aynı map'i paylaşıyor. **Ayrıca gerçek bir bağlantı hatası bulunup düzeltildi**: Günlük Çizelge'de "gelen hasta"ya tıklayınca hasta detayına giden link (`components/panel/canli-cizelge.tsx`, bir önceki turda "seansta"dan "geldi/gecikmeli geldi/seansta"ya genişletilmişti) `/panel/hastalar/{id}?tab=tedavi` query-param deseniyle yazılmıştı — ama hasta detay sayfası bir süre önce (bu CLAUDE.md'de belgelenmemiş bir turda) `?tab=` okuyan tek-sayfa Tabs yapısından, her sekmenin kendi route'u olduğu bir yapıya (`/panel/hastalar/[id]/kisisel|randevu|tedavi|cari`) geçmiş; `?tab=tedavi` artık hiçbir yerde okunmadığı için link sessizce hub sayfasına düşüyordu (hata vermiyordu, sadece yanlış yere gidiyordu). Link `/panel/hastalar/{id}/tedavi`ye düzeltildi — bu, dokümante edilmemiş bir mimari değişiklikten kaynaklanan, önceden fark edilmemiş gerçek bir regresyondu. `npx tsc --noEmit` ve `npm run lint` temiz (9 hata önceden var olan ilgisiz baseline); gerçek kullanıcıyla uçtan uca test edilmedi (giriş bilgisi yok).

Sidebar'daki "Personel" öğesi Tedaviler/Tedavi Protokolleri ile aynı hub deseniyle 2 sayfaya ayrıldı — kullanıcı kararı: `MENU_GRUPLARI`'ye yeni `personel` grubu eklendi (Muhasebe grubundan "Personel" linki kaldırılıp taşındı), `/panel/personel` artık `MenuGrubuSayfasi` hub'ı (2 kart: Personel / Personel Takip); asıl bordro listesi `/panel/personel/liste`'ye taşındı, yeni `/panel/personel/takip` eklendi (ikisi de `/panel/personel/[id]` detay route'unun kardeşi — `/panel/hastalar/kayit-formu` ↔ `/panel/hastalar/[id]` ile aynı önceden kanıtlanmış statik/dinamik route birlikte-yaşama deseni). **Personel (liste)**: kullanıcının istediği gibi personel gruplarına (görev) göre gruplu listede artık her satırda Maaş + Fazla Mesai + Yol/Yemek + SGK dökümü var, grup altında bordro toplamı satırı eklendi; ay bazlı gezinme (Önceki/Sonraki, `[id]` sayfasındaki Ödemeler tab'ıyla aynı `ayAraligi` deseni). SGK için `personel_ekstra_hakedis.tur` CHECK'ine `'sgk'` eklendi (migration `20260805090000`, yol/yemek/mesai ile birebir aynı desen — kullanıcı kararıyla MVP'de manuel giriş, otomatik yüzde hesaplama YOK). **Personel Takip (yeni, klinik_admin-only — `notFound()` ile diğer roller kapalı, terapist kendi maaş bilgisini zaten `[id]` sayfasından görüyor)**: "alacak" = personelin klinikten eline geçmesi gereken hakediş (maaş + terapistse prim + ekstra hakedişler — **SGK hariç**, çünkü SGK personelin eline geçmiyor, devlete yatırılan işveren gideri) eksi o ay fiilen ödenen tutar; yeni `personel_odeme` tablosu (migration aynı dosyada, `personel_ekstra_hakedis` ile birebir aynı RLS: görüntüleme admin+kendisi, yönetim sadece klinik_admin) SADECE ödenen tarafı tutuyor — hakediş tarafı için ayrı bir ledger AÇILMADI, mevcut `maasHesapla` helper'ı + prim hesabı için terapist başına (Promise.all ile bulk) seans sayısı sorgusu kullanılarak her istekte yeniden hesaplanıyor (tek kaynak korunuyor, `odeme_kalemi`/hakediş arasında çakışma riski yok). Her personel satırında "Ödeme Ekle" dialog'u (yeni `odemeEkle` action, klinik_admin-only) ile fiili ödeme girilebiliyor; kalan alacak >0 ise kırmızı (rose), ≤0 ise yeşil (emerald) gösteriliyor. Migration pooler üzerinden push edilip service-role ile gerçek insert/select denemeleriyle (sgk türü kabul ediliyor, personel_odeme çalışıyor) doğrulandı; `npx tsc --noEmit`, `npm run lint` (9 hata önceden var olan ilgisiz baseline) ve `next build` (41 route, yeni 3 route dahil) temiz. Gerçek kullanıcıyla uçtan uca test edilmedi (giriş bilgisi yok).

Tedavi Tanımları'na kademeli fiyatlandırma (Vita/Plus/Elit/Prime) + hasta kategorisine göre otomatik iskonto eklendi (migration `20260805100000` + `20260805110000`, kullanıcı isteği). **Kademeli fiyat**: `islem_tanimi.fiyat` kolonu tamamen kaldırılıp `vita_fiyat` (zorunlu, master fiyat — eski "Fiyat (₺)" alanı aynen buraya yazıyor) + `plus_fiyat`/`elit_fiyat`/`prime_fiyat` (opsiyonel override) ile değiştirildi; boş bırakılan kademe fiyatı, aşağıdaki iskonto oranından hesaplanıyor. CLAUDE.md'de tarif edilip hiç CREATE edilmemiş olan `islem_tanimi_fiyat_gecmisi` ilk kez bu turda oluşturuldu (kademe bazlı, `AFTER INSERT/UPDATE` trigger'ıyla tamamen otomatik yazılıyor — `audit_log_yaz` deseniyle aynı SECURITY DEFINER yaklaşımı; bu turda sadece şema+trigger, görüntüleme ekranı yok). **İskonto Oranları**: Tedaviler hub'ına yeni üçüncü kart (`/panel/iskonto-oranlari`, klinik_admin-only) — Vita/Plus/Elit/Prime % alanları, yeni `iskonto_oranlari` tablosunda (klinik başına tek satır, `klinik_ayarlar`'ın kullanılmayan JSONB deseni yerine tipli/kısıtlı ayrı tablo tercih edildi) tutuluyor. **Hasta kategorisi**: görev tanımındaki "musteri_kategori" — proje `musteri`'yi daha önce `hasta`'ya yeniden adlandırmıştı, o yüzden `hasta.kategori` (vita/plus/elit/prime, varsayılan vita) eklendi; Hasta Detay > Kişisel Bilgiler > Temel Bilgiler formuna (klinik_admin/resepsiyon-only, terapist'e kapalı — mevcut idari-alan ayrımıyla tutarlı) bir Select olarak eklendi. **Fiyat hesaplama merkezi bir SQL fonksiyonunda**: yeni `islem_tanimi_etkin_fiyat(islem_tanimi_id, hasta_id)` — kademe override'ı varsa onu, yoksa `vita_fiyat * (1 - ilgili_kategori_pct/100)` döner; hem `odeme_olustur` (Ödeme Al) hem `randevu_gelis_isaretle` (paketsiz check-in'de borç yazma) RPC'leri artık ham `fiyat` yerine bu fonksiyonu çağırıyor — istemci hiçbir zaman fiyat göndermiyor (sadece `ref_id`+`miktar`), bu yüzden kademe/iskonto mantığı tamamen sunucu tarafında ve spoof edilemez. Ödeme Al ekranındaki toplam önizlemesi (`cari/page.tsx`) aynı formülü TS tarafında (`lib/fiyat/etkin-fiyat-hesapla.ts`) tekrarlıyor — sadece görüntüleme için, RPC zaten bağımsızca yeniden hesaplıyor. Ödeme Al kartında hastanın kategorisi rozet olarak gösteriliyor. **Tedavi Tanımları listesi accordion'a çevrildi** (kullanıcı isteği): "Düzenle" ile bir satır açıldığında artık SADECE o satır görünüyor, diğerleri tamamen gizleniyor — state `IslemSatiri`'nin kendi local'inden yeni bir client wrapper'a (`tedavi-listesi.tsx`, `duzenlenenId` state'i) taşındı, `IslemSatiri` artık `duzenleniyor`/`onDuzenleBaslat`/`onDuzenleBitir` prop'larıyla controlled bir bileşen. Migration'lar pooler üzerinden push edilip service-role ile gerçek RPC çağrılarıyla (yüzde bazlı hesap, override, eski `fiyat` kolonunun gerçekten kalktığı) doğrulandı; `npx tsc --noEmit`, `npm run lint` (9 hata önceden var olan ilgisiz baseline, dokunulan dosyalarda sıfır) ve `next build` (yeni `/panel/iskonto-oranlari` dahil) temiz. Gerçek kullanıcıyla uçtan uca test edilmedi (giriş bilgisi yok).

Personel Detay > Ödemeler'deki "Ekstra Hakedişler" kartı "Ödemeler" olarak yeniden adlandırıldı, ödeme türü seçenekleri değişti (migration `20260809090000`, kullanıcı kararı): `personel_ekstra_hakedis.tur`'dan `'sgk'` kaldırıldı, yerine `'avans'` eklendi — artık yol/yemek/mesai/avans/diğer. SGK işveren maliyeti zaten ayrı bir yoldan (Muhasebe > Kamusal Giderler, `klinik_harcama` kategori `'vergi_sgk'`) takip ediliyordu; `'sgk'` türü kısa süre önce (20260805090000) bordro maliyeti görünümü için eklenmişti ama `lib/raporlar/hesaplamalar.ts`'teki `hesaplaSabitPersonelMaliyeti` hiç filtrelemiyordu — yani SGK tutarı hem Muhasebe raporuna hem (yanlışlıkla) Sabit Personel Maliyeti'ne karışıyordu; `'sgk'` seçeneğinin tamamen kaldırılması bu tutarsızlığı da çözdü. `personel/takip` sayfasındaki artık gereksiz `if (h.tur === "sgk") continue` filtresi kaldırıldı (yeni `HakedisTuru` tipinde `"sgk"` olmadığı için zaten derlenmezdi). Canlıda `'sgk'` türünde hiç kayıt olmadığı PostgREST ile doğrulandı (migration'da veri dönüşümüne gerek kalmadı); yeni CHECK constraint'in `'sgk'` insert'ini gerçekten reddettiği canlıda test edildi. `npx tsc --noEmit`, `npm run lint` ve `next build` temiz.

Personel Detay > Maaş Ayarları'na "Geçerlilik Tarihi" alanı eklendi, maaş değişiklikleri artık tarihli kayıt altına alınıyor (migration `20260809100000`, kullanıcı kararı: "hangi tarihte hangi maaşı alıyor kayıt olsun"): yeni `personel_maas_gecmisi` tablosu (personel_id/maas/gecerlilik_tarihi/ekleyen_kullanici_id), `personel_ekstra_hakedis` ile birebir aynı RLS deseni (görüntüleme admin+kendisi, yönetim sadece klinik_admin). `personel.maas` hâlâ "güncel maaş" tek kaynağı (maasHesapla vb. her yerde ona bakmaya devam ediyor) — yeni tablo salt geçmişe dönük kayıt/audit amaçlı, hesaplama mantığına dokunulmadı (geçmiş aylar için o tarihte geçerli olan maaşla yeniden hesaplama YAPILMIYOR, kapsam dışı bırakıldı). `maasAyarlariGuncelle` action'ı geçmiş satırını sadece maaş değeri GERÇEKTEN değiştiğinde ekliyor (prim/baraj ayarı tek başına değişirse, veya form aynı maaş değeriyle tekrar kaydedilirse gereksiz tekrar satır eklenmiyor) — eski `personel.maas` değeri update öncesi ayrıca sorgulanıp karşılaştırılıyor. Personel Detay'da yeni "Maaş Geçmişi" kartı (son 24 kayıt, tarihe göre azalan) Maaş Ayarları'nın altına eklendi; hakediş listesiyle aynı görünürlük kuralını izliyor (RLS'e bırakıldı — hem admin hem personelin kendisi görebiliyor, formu doldurma yine sadece klinik_admin'e açık). `npx tsc --noEmit`, `npm run lint` ve `next build` temiz; migration pooler üzerinden push edilip tablo service-role ile canlıda doğrulandı.

Personel Listesi kutucuklarına Giriş/Çıkış/Ödeme hızlı aksiyonları eklendi (kullanıcı kararı, klinik_admin-only): "Giriş"/"Çıkış" sayfa açmadan tek tıkla o anki saati bugünün `personel_puantaj` satırına yazıyor (yeni `hizliPuantajKaydet` action'ı, `app/(app)/panel/personel/actions.ts`) — Çalışma Çizelgesi'ndeki `gunKaydet` ile aynı RLS/yetki sınırı (personel_puantaj INSERT/UPDATE zaten DB'de klinik_admin-only), var olan bir saati asla sessizce ezmiyor (zaten kaydedilmişse mevcut saati gösteren bir uyarı döner, düzeltme gerekiyorsa admin Çalışma Çizelgesi'nden elle değiştirir); "çıkış" girişi olmadan çalıştırılamaz. "Ödeme" ise bir server action değil, doğrudan Personel Detay > Ödemeler sekmesine (`?tab=odemeler`) giden bir link. Üç buton da `personel-satiri.tsx`'te kart tıklamasının (detaya gitme) üstüne binmesin diye kendi satırında `stopPropagation` ile izole edildi — kart artık `flex-row` değil `flex-col` (üstte avatar/isim/telefon/rozet satırı, altta admin'e özel buton satırı). Canlıda service-role ile giriş/çıkış insert+update akışı (generated `net_calisma_dakika` dahil) test edilip temizlendi. `npx tsc --noEmit`, `npm run lint` ve `next build` temiz.

Tablet bölümüne "Tablet Görünümü" ayar ekranı eklendi (`/panel/tablet/ayarlar`, klinik_admin-only, kullanıcı kararıyla Ayarlar hub'ına değil doğrudan Tablet bölümünün içine — oda seçim sayfasındaki `/panel/tablet` header'ına "Görünüm Ayarları" butonu eklendi). Hasta adı/terapist adı/işlem adı/oda durumu renk kodu (meşgul=kırmızı, müsait=yeşil) için 4 checkbox, yanında sabit örnek veriyle (`"Ahmet Yılmaz"`/`"Fzt. Ayşe Kaya"`/`"Manuel Terapi"`) canlı güncellenen bir tablet önizlemesi (`tablet-onizleme.tsx`) — checkbox durumu üstteki `tablet-ayarlari-istemci.tsx`'te tutulup hem forma hem önizlemeye prop olarak geçiyor. **Migration gerekmedi**: `klinik_ayarlar` tablosu (`klinik_id` PK + `ayarlar jsonb`, RLS'i zaten klinik_admin-only yazıma kilitli) ilk kez bu turda kullanıldı, `ayarlar->'tablet'` altında namespaced saklanıyor; yazma tarafı düz `.upsert()` değil önce mevcut `ayarlar`'ı okuyup `{...mevcut, tablet: yeni}` olarak birleştiren bir read-modify-write (gelecekte aynı kolona eklenebilecek başka ayar gruplarını ezmemek için — iskonto_oranlari gibi tek-satır ayarlarda zaten RPC/transaction kullanılmıyor, aynı konvansiyon). Ayarların gerçek etkisi olması için asıl tablet ekranı da genişletildi: `randevu` sorgusuna `islem_tanimi(id, ad)` join'i eklendi (tip zaten `RandevuSatir`'de vardı), `tablet-ekrani.tsx`'e işlem adı satırı ve `StatusBadge` ile meşgul(rose)/müsait(emerald) rozeti eklendi — "meşgul" `mevcut` (durum geldi/gecikmeli_geldi) doluyken, "Sıradaki" ve "Oda boş" durumlarında "müsait" gösteriliyor. `npx tsc --noEmit`, `npm run lint` (9 hata önceden var olan ilgisiz baseline, dokunulan/yeni dosyalarda sıfır) ve `next build` (`/panel/tablet/ayarlar` dahil) temiz. Gerçek kullanıcıyla uçtan uca (checkbox↔önizleme canlı güncelleme, gerçek tablet ekranı) test edilmedi (giriş bilgisi yok).

Gerçek tablet ekranının header'ına (`[odaId]/page.tsx`) ve "Tablet Görünümü" ayar sayfasındaki önizlemeye (`tablet-onizleme.tsx`) Şirket Bilgileri'nde yüklenen klinik logosu (`klinik.logo_url`, `klinik-logo` Storage bucket'ından public URL) eklendi — oda adının solunda küçük bir logo olarak, `sirket-bilgileri-formu.tsx`'teki logo önizlemesiyle aynı `<img>` deseni (next/image değil, dinamik Storage URL'i için). Logo klinikte tanımlıysa gösteriliyor, tanımlı değilse sessizce atlanıyor; ayrı bir checkbox/ayar eklenmedi (kullanıcı sadece "uygun bir yere eklensin" dedi, toggle istemedi). `npx tsc --noEmit`, `npm run lint` (aynı ilgisiz baseline) ve `next build` temiz.

Personel Listesi kutucuklarındaki Giriş/Çıkış/Ödeme butonları renklendirildi (Giriş emerald, Çıkış amber, Ödeme sky — outline variant üstüne soft-tint className, `durum-butonlari.tsx`'teki AKTIF_SINIFI deseniyle aynı fikir).

"Personel" artık ayrı bir hub sayfası değil (kullanıcı kararı: "personel bölümüne girilince direk personel listesi çıksın... personel maaş, personel takip iptal, personel detayına girilince zaten takip ediliyor"): sidebar'da `MENU_GRUPLARI`'den çıkarılıp Hastalar/Randevular gibi doğrudan tek bir üst seviye linke (`ANA_OGELER`, `/panel/personel` → `UserCog` ikonu) dönüştürüldü. `/panel/personel/page.tsx` artık `MenuGrubuSayfasi` hub'ı render etmiyor, doğrudan `/panel/personel/liste`'ye redirect ediyor (Personel Listesi zaten "Yeni Personel" butonunu içeriyor, ayrıca bir giriş noktasına gerek yok). Personel Maaş (`/panel/personel/maas`) ve Personel Takip (`/panel/personel/takip`) route'ları SİLİNMEDİ, sadece navigasyondan (hub kartları) kaldırıldı — CLAUDE.md'nin daha önceki "islem_kategori" kararındaki gibi geri alınabilir bir gizleme, kalıcı silme değil; ikisine de artık uygulama içinden hiçbir linkle ulaşılamıyor (sadece URL'i bilen biri elle gidebilir). `npx tsc --noEmit` temiz; `npm run lint`'teki tek hata (`sidebar.tsx`'teki `react-hooks/set-state-in-effect`) bu değişiklikten önce de vardı (git stash ile doğrulandı, önceden var olan ilgisiz baseline). `next build` temiz (maas/takip route'ları hâlâ derleniyor, sadece linksiz).

Personel Listesi kutucuklarındaki Giriş/Çıkış artık tek tıkla anında kaydetmiyor (kullanıcı kararı: "o anki saat gözüksün, onayla ve düzenle çıksın"): butona tıklanınca kartın altında o anki saat (client'ta `formatTime(new Date().toISOString())` ile İstanbul saatine çevrilip donduruluyor, panel açıkken canlı akmıyor) + "Onayla"/"Düzenle" gösteriliyor; "Düzenle" bir `<input type=time">`'a geçiyor ("Kaydet" ile onaylanıyor). `hizliPuantajKaydet` action'ı artık üçüncü bir `saat` (HH:mm) parametresi alıyor — sunucu tarafında regex ile doğrulanıp `toUTC` ile o günün tarihiyle birleştiriliyor (`personel_id`/`tur` gibi hep sunucuda yeniden kontrol edilen alanlarla aynı güven modeli, saat sadece client'tan geliyor çünkü kullanıcı bilinçli olarak değiştirebiliyor). Çıkış saati giriş saatinden önce girilirse artık `personel_puantaj_cikis_giristen_sonra` CHECK constraint'i (23514) yakalanıp "Çıkış saati giriş saatinden önce olamaz." mesajına çevriliyor (önceden bu senaryo hiç oluşamıyordu çünkü ikisi de ayrı ayrı "şimdi" anlarında yazılıyordu; artık saat serbestçe düzenlenebildiği için gerçek bir hata yolu oldu). Canlıda constraint ihlali servis-role ile tetiklenip satırın gerçekten oluşmadığı doğrulandı. `npx tsc --noEmit`, `npm run lint` ve `next build` temiz.

Şirket Bilgileri sayfasına "Araçlar" kartı eklendi (migration `20260809110000`, kullanıcı isteği: marka/model/plaka, tek satır hep açık): yeni `klinik_arac` tablosu — `klinik_harcama` ile aynı basit klinik-scoped desen (klinik_id doğrudan tutulur). RLS bilinçli olarak asimetrik: SELECT klinikteki HERKESE açık (hassas/finansal veri değil, sayfanın geri kalanının aksine bilgi amaçlı), ekleme/silme sadece `klinik_admin`. UI dialog'a gizlenmedi — liste + altında her zaman görünen inline marka/model/plaka giriş satırı ve "Araç Ekle" butonu (`araclar-karti.tsx`, kullanıcının "tek satır hep açık olsun" isteği), silme `radyoloji-grid.tsx`'teki gibi iki adımlı inline onay ("Silinsin mi? Evet, sil / Vazgeç") — projede `window.confirm` hiç kullanılmıyor, aynı deseni takip etti. Migration pooler üzerinden push edilip service-role ile gerçek insert/select/delete denemeleriyle doğrulandı; `npx tsc --noEmit`, `npm run lint` (9 hata önceden var olan ilgisiz baseline) ve `next build` temiz.

Kapı tableti (`/panel/tablet/[odaId]`) kurumsal görünüme geçirildi — tek renk kaynaklı 3 durumlu (Müsait/Hazırlanıyor/Seans sürüyor) durum sistemi, statik SVG doku katmanı, marka logosu, ekran koruyucu, çevrimdışı göstergesi. Yeni `lib/tablet/oda-durumu.ts` (`ODA_DURUMU` sözlüğü: etiket/renk/metinSinifi/auraOpacity — aura gradient, sol 5px şerit, alt ilerleme çubuğu hepsi bu tek kaynaktan besleniyor) ve `oda-durumu-hesapla.ts` (`mevcut`/`sonraki`'den durum türetir: sıradaki randevu ≤15 dk içindeyse "Hazırlanıyor", veri modelinde bu ayrımı taşıyan bir alan olmadığı için kullanıcı onayıyla eklenen bir eşik). 5 yeni bileşen: `components/tablet/{TabletBackground,TabletLogo,TabletSaat,DurumRozeti,EkranKorucusu}.tsx` — `TabletBackground` `React.memo`'lu statik SVG (nokta grid + 3 topografik eğri + durum renkli aura, animasyon yok), `TabletSaat` izole kendi interval'ıyla parent'ı re-render etmiyor, `EkranKorucusu` `lib/tablet/use-idle.ts`'teki `useIdle(5dk)` hook'uyla tetikleniyor (mousemove/touchstart/keydown ile sıfırlanır). Realtime `.subscribe()` artık status callback'i dinliyor — `CHANNEL_ERROR`/`TIMED_OUT` olduğunda üst barda amber "Çevrimdışı · son güncelleme HH:mm" rozeti çıkıyor, ekran boşalmıyor (son bilinen durum kalıyor). **Migration `20260809140000`**: `klinik.logo_url_koyu` (nullable) — koyu zeminde açık logo kaybolmasın diye, Şirket Bilgileri formuna ikinci opsiyonel yükleme alanı eklendi (`logo-koyu.<uzanti>` path, aynı `klinik-logo` bucket). `TabletLogo` kaynak zinciri: tema koyuysa `logo_url_koyu ?? logo_url`, açıksa `logo_url`; ikisi de yoksa veya `onError` tetiklenirse `marka_renkleri.primary` (klinik tablosunda ilk migration'dan beri var ama hiç kullanılmıyordu) zeminli baş harf kutusuna düşüyor. **Spek'ten bilinçli sapmalar**: logo için `next/image` değil `<img>` (projede 7/7 dinamik Storage görseli aynı deseni kullanıyor, `next.config.ts`'de `remotePatterns` hiç yoktu); "gizlilik_modu" yerine bir önceki turda zaten kurulan `tablet ayarları.soyad_maskele` kullanıldı (paralel alan açılmadı); "Bugünkü program" listesi kaldırıldı (yeni tasarımın 2-3 metreden okunabilir tek-odaklı hedefiyle çelişiyordu). **Gerçek bir hidrasyon hatası bulunup düzeltildi**: alt ilerleme çubuğunun genişliği `simdi = useState(() => new Date())`'den türetiliyordu — sunucu render anı ile istemci hydration anı farklı milisaniyeler ürettiği için React "hydration mismatch" hatası veriyordu (Playwright ile konsol logu okunarak yakalandı). Çözüm: kendi state'i yerine projede zaten var olan hidrasyon-güvenli `lib/hooks/use-minute-tick.ts` (`useMinuteTick`, SSR'da null döner) kullanılacak şekilde değiştirildi; `simdi` null iken merkez blok iskelet (skeleton) gösteriyor, tıpkı gerçek yeniden-yükleme durumunda olduğu gibi. **Ayrıca bulunan/düzeltilen bir WCAG kontrast hatası**: durum rozeti metni ve "text-slate-300/500" ile yazılmış terapist/işlem satırları koyu tema için seçilmiş sabit renklerdi, açık temada okunmuyordu — rozet metni artık `components/ui/status-badge.tsx`'teki tone deseniyle aynı yaklaşımla `text-{renk}-800 dark:text-{renk}-300` (600/700 gerçek kontrast hesabıyla WCAG 4.5:1'i geçmiyordu, node ile manuel hesaplanıp doğrulandı), diğer metinler `text-foreground/80`/`text-muted-foreground` gibi tema-duyarlı semantik token'lara çevrildi. Auth'suz kalıcı QA rotası eklendi: `/tablet-onizleme?durum=&tema=` (kullanıcı kararıyla kalıcı bırakıldı, nav'a bağlı değil) — sahte randevu verisiyle 3 durum × 2 tema render eder, `TabletEkrani`'a yeni `canliBaglanti` prop'u (false ise realtime aboneliği hiç kurulmaz) eklendi. Bu rotada global `ThemeToggle` FAB'ının (ve gerçek tablet ekranında zaten gizli olan) görünmemesi için `GIZLI_ROTALAR`'a eklendi. Playwright (proje bağımlılığı değil, `npx playwright@latest` ile tek seferlik) kullanılarak 1280×800/1024×600 × 3 durum × 2 tema = 12 ekran görüntüsü alındı, bir Artifact galerisinde incelendi. `npx tsc --noEmit`, `npm run lint` (aynı 9 ilgisiz baseline hata) ve `next build` temiz.

Tablet Görünümü ayar sayfasındaki (`/panel/tablet/ayarlar`) önizleme, checkbox'lara tepki veren sade/gerçeğe uymayan bir kutu yerine gerçek kapı tableti tasarım bileşenlerini (`TabletBackground`/`TabletLogo`/`DurumRozeti` — bir önceki turda kurulanların aynısı) küçük ölçekte kullanan sabit bir 2x2 örnek galerisine (`tablet-tasarim-ornekleri.tsx`) çevrildi: açık/koyu tema × Müsait/Seans sürüyor durumu, sabit örnek veriyle (form state'ine bağlı değil, amaç canlı önizleme değil "tablet gerçekte böyle görünüyor" referansı). Eski `tablet-onizleme.tsx` silindi. Bunun için `page.tsx`'teki klinik sorgusu `logo_url` yerine `ad, logo_url, logo_url_koyu, marka_renkleri` çekecek şekilde genişletildi (TabletLogo bileşeni bunları gerektiriyor) — `TabletAyarlariIstemci` artık `logoUrl` yerine tam `Klinik` objesi prop alıyor. `npx tsc --noEmit`, `npm run lint` (9 hata önceden var olan ilgisiz baseline) ve `next build` temiz.

Hasta Kayıt Formu (PDF, `/panel/hastalar/kayit-formu`) tarayıcı `window.print()`'e bağımlılıktan çıkarıldı (kullanıcı isteği: sayfa üstünde çıkan tarayıcı yazdırma başlığı/tarih/URL/sayfa no'dan kurtulmak): yeni `KayitFormuPdfButonu` (`components/panel/kayit-formu-pdf-butonu.tsx`) `html2canvas`+`jsPDF` (yeni npm bağımlılıkları) ile her `.pdf-sayfa` elementini ayrı ayrı canvas'a çekip kendi (içerik boyutuna göre hesaplanan, A4 genişliğinde ama değişken yükseklikte) PDF sayfası olarak ekliyor — böylece bir bölüm ortadan ikiye bölünerek sayfa geçişi yapamıyor, sayfa sınırları tamamen kod tarafından (üç açık `<Sayfa>` bloğu) belirleniyor, artık tarayıcının doğal taşma bazlı sayfalamasına bırakılmıyor. Eski `YazdirButonu` (Muhasebe Raporları'nda hâlâ kullanılıyor) bu sayfadan kaldırıldı, bu forma özel yeni bir buton yazıldı. Aynı turda 3 sayfaya açık bölünme yapıldı: Sayfa 1 = Kimlik & İletişim + Veli Bilgisi + Acil Durum Kişisi, Sayfa 2 = Tıbbi Ön Geçmiş, Sayfa 3 = Geliş Sebebi & Klinik Notları (önceden Tıbbi Ön Geçmiş ile aynı sayfadaydı, kullanıcı isteğiyle Onaylar'ın üstüne, 3. sayfaya taşındı) + Onaylar + imza satırı. Bölüm başlıklarının üstündeki tüm ayırıcı çizgiler kaldırıldı (`header`'ın `border-b`'si ve paylaşılan `Bolum` bileşeninin `border-t`'si, hangisi first-child sayılırsa sayılsın hepsi kaynak koddan tamamen çıkarıldı) — kod incelemesinde header'ın kendi alt çizgisiyle `Bolum`'un üst çizgisinin üst üste bindiği (Bolum, header'dan sonra geldiği için Tailwind'in `first:` varyantı hiç eşleşmiyordu) gerçek bir CSS hatası bulundu, bu da "Kimlik & İletişim" üstünde 2, diğer bölüm başlıklarının üstünde 1 çizgi görünmesini açıklıyordu. **Netleştirilen nokta**: "2. sayfadaki alt bölümler arasında silik çizgiyi kaldır" talebi netleştirildi (kod incelemesinde tarif edilen "iki çizgi" yapısı yoktu, tek çizgi vardı — kullanıcıya soruldu, "silik çizgileri kaldır" onayı geldi) — Tıbbi Ön Geçmiş'teki 9 `EvetHayirSatiri` satırının her birindeki `border-b border-black/10` (satırlar arası tek ayırıcı) kaldırıldı, sadece `pb-3` boşluğu kaldı. Sayfa 1'in sol üstüne klinik logosu eklendi (`klinik.logo_url`, sadece 1. sayfada) — `crossOrigin="anonymous"` + html2canvas'a `useCORS:true` ile CORS-güvenli yükleniyor (Storage bucket'ı zaten public). Ekrandaki "sayfa" görünümü (rounded border + shadow) `.pdf-sayfa` elementinin DIŞINA sarılan ayrı bir wrapper'a kondu — o border/shadow sadece ekran önizlemesi için, PDF'e gömülen görüntüye karışmıyor. Print'e özel `print:` varyantları ve `@page`/`aside { display:none }` stili tamamen kaldırıldı (artık kullanılmıyor); form kartı artık tema-bağımsız sabit açık/beyaz doküman görünümünde (kağıda basılacak form olduğu için koyu tema ile birlikte değişmiyor). `npx tsc --noEmit`, `npm run lint` (aynı 9 ilgisiz baseline hata, dokunulan dosyalarda sıfır) ve `next build` (`/panel/hastalar/kayit-formu` dahil) temiz; gerçek kullanıcıyla tarayıcıda uçtan uca test edilmedi (giriş bilgisi yok) — özellikle logo CORS davranışının gerçek Supabase Storage URL'iyle denenmesi öneriliyor.

**Bulunan/düzeltilen gerçek hata (aynı gün, kullanıcı "PDF oluşturulamadı" hatası bildirdi)**: `html2canvas` (v1.4.1, klasik paket) Tailwind v4'ün ürettiği modern CSS renk fonksiyonlarını (`color-mix(in oklab, ...)` — Tailwind v4'te `text-black/70` gibi opaklık modifier'lı her renk sınıfı buna derleniyor, `.next` derlemesinde doğrulandı) parse edemiyor ve PDF üretimi sessizce (jenerik catch mesajıyla) başarısız oluyordu. Çözüm: `html2canvas` paketten tamamen kaldırılıp, tam olarak bu uyumluluk sorunu için var olan drop-in fork `html2canvas-pro` (oklch/color-mix/lab/lch destekli) ile değiştirildi — `kayit-formu-pdf-butonu.tsx`'teki tek satırlık import değişti, kalan kod (API birebir aynı) dokunulmadı. Ayrıca catch bloğuna `console.error` eklendi (önceden hata sessizce yutuluyordu, sadece jenerik kullanıcı mesajı gösteriliyordu — ileride benzer bir sorun çıkarsa konsoldan teşhis edilebilsin diye). **Ders**: bu projede Tailwind v4 kullanıldığı için (bkz. `--border: oklch(...)` gibi CSS custom property'ler), DOM'u canvas'a "fotoğraflayan" her yeni araç (html2canvas, muhtemelen ileride benzer bir ihtiyaç çıkarsa dom-to-image vb.) seçilirken oklch/color-mix desteği önceden kontrol edilmeli — klasik/eski sürümler bu konuda genelde uyumsuz. `npx tsc --noEmit`, `npm run lint` ve `next build` temiz; gerçek bir Supabase oturumuyla tarayıcıda uçtan uca hâlâ test edilmedi (giriş bilgisi yok) — kullanıcının bir sonraki denemede PDF'in gerçekten indiğini teyit etmesi gerekiyor.

**Bulunan/düzeltilen ikinci gerçek hata (aynı gün, kullanıcı "3. sayfa diğer sayfalardan daha geniş render ediliyor, KVKK metni/imza satırı kesiliyor" bildirdi)**: kullanıcının hipotezinin aksine 3. sayfanın kök wrapper'ı (`<Sayfa>`, `.pdf-sayfa` class'ı) 1. ve 2. sayfayla BİREBİR aynıydı (üçü de aynı paylaşılan bileşeni kullanıyor) — gerçek neden yalnızca 3. sayfada bulunan `Onay` bileşeninin (KVKK/sağlık verisi/WhatsApp/ticari ileti açıklama paragrafları, formdaki en uzun kesintisiz metinler) iç içe iki flex konteynerinde (`flex items-start gap-2` > `flex flex-col gap-0.5`) `min-width: 0` olmamasıydı — flexbox'ın varsayılan `min-width: auto` davranışı, uzun paragrafın satır kaydırmadan (unwrapped/max-content genişliğinde) render edilmesine izin veriyor, bu da o flex item'ı ve dolayısıyla `.pdf-sayfa`'yı sayfanın geri kalanından daha geniş büyütüyordu (Tailwind'in `grid-cols-2` utility'si zaten `minmax(0,1fr)` kullandığı için grid seviyesinde bu sorun yoktu, sorun ONAY bileşeninin İÇİNDEKİ ekstra flex katmanındaydı). 1. ve 2. sayfalardaki `Alan`/`EvetHayirSatiri` etiketleri kısa tek satırlık metinler olduğu için bu ekstra flex sarmalayıcıya hiç ihtiyaç duymuyordu, bu yüzden sorun sadece 3. sayfada görünüyordu. Çözüm: `Onay` bileşenindeki her iki flex konteynerine `min-w-0` (dıştaki `flex-1` ile birlikte) ve metin span'larına `break-words` eklendi — artık uzun paragraflar konteyner genişliğini büyütmek yerine normal şekilde satır kaydırıyor. `npx tsc --noEmit`, `npm run lint` ve `next build` temiz.

**Üçüncü gerçek hata (aynı gün, kullanıcı ikinci fix'ten sonra da PDF gönderip "hâlâ kesiliyor" bildirdi — bu fix'in de yetersiz olduğu ORTAYA ÇIKTI)**: bir sonraki oturumda önce sayfalara sabit `w-[794px]` (A4 96dpi) genişlik verildi (dış container `max-w-3xl`→`max-w-[850px]`), bu da yetmedi — kullanıcının gönderdiği yeni PDF'de KVKK/onay paragrafları HÂLÂ hiç satır kaydırmadan tek satır halinde sağ kenardan (İmza satırı dahil) kesiliyordu. Gerçek kök sebep ikinci fix'in teşhisinden farklı çıktı: **`html2canvas`'ın (aynı şekilde `html2canvas-pro` fork'unun da) CSS Grid'deki `grid-column: span 2` (Tailwind `col-span-2`) öğelerini doğru genişlikte hesaplamaması** — bilinen bir html2canvas kısıtı, grid track sizing algoritmasını tam desteklemiyor, `col-span-2` öğesini auto/max-content genişlikte (sınırsız) render ediyor; ekranda (gerçek tarayıcı grid motoruyla) hiçbir sorun görünmüyordu, sadece html2canvas'ın kendi (CSS grid'i taklit eden) layout motorunda ortaya çıkıyordu — bu yüzden `min-w-0`/sabit sayfa genişliği gibi CSS-doğru düzeltmeler ekranda mantıklı görünse de PDF çıktısını değiştirmiyordu. Çözüm: "Tıbbi Ön Geçmiş", "Geliş Sebebi & Klinik Notları", "Onaylar" bölümlerinin ZATEN tüm elemanları `col-span-2` (tam genişlik) olduğu için 2 sütunlu grid'e hiç ihtiyaçları yoktu — `Bolum` bileşenine `tekSutun` prop'u eklenip bu 3 bölüm `grid grid-cols-2`'den `flex flex-col`'a çevrildi (html2canvas flex'i grid'den çok daha güvenilir destekliyor), `col-span-2` kullanımları kaldırıldı. **Metodolojik ders (üç başarısız denemeden sonra)**: bu sefer düzeltme varsayıma bırakılmadı — gerçek `html2canvas-pro`, projenin production Tailwind CSS çıktısıyla (`next build`'den alınan `.next/static/chunks/*.css`) birlikte Playwright ile headless çalıştırılıp, aynı DOM üzerinde hem "hangi eleman sayfa sınırının dışına taşıyor" (`getBoundingClientRect` karşılaştırması, 0 taşan eleman bulundu) hem de gerçek `canvas.width` (beklenen 1584px'e tam eşit çıktı) ölçülerek, ayrıca capture edilen canvas PNG olarak diske yazılıp görsel olarak da incelenerek doğrulandı — üç kez "ekranda mantıklı görünüyor ama PDF'te hâlâ bozuk" yaşandığı için, html2canvas gibi DOM'u kendi motoruyla yeniden yorumlayan bir araca güvenilirken CSS'in "doğru" görünmesi yeterli kanıt değil, aracın GERÇEK çıktısı ölçülmeli. `npx tsc --noEmit`, `npm run lint` ve `next build` temiz.

**İş Başvurusu, Personel bölümüne taşındı ve genişletildi (2026-08-10, kullanıcı kararı).** Personel Listesi'ndeki "Yeni Personel Ekle" butonu "İş Başvurusu Ekle"ye dönüştürüldü (klinik_admin-only, artık bir dialog değil `/panel/personel/basvurular`'a giden bir link — doğrudan/boş personel oluşturma bu ekrandan kaldırıldı, tek giriş noktası artık bir başvuruyu onaylamak). **Kullanıcı kararıyla mevcut ayrı "Personel Başvurusu" akışı (QR, `personel_basvuru_taslagi` tablosu, `/panel/ayarlar/qr-kodlar/personel-basvurulari`) bu yeni "İş Başvurusu" akışıyla BİRLEŞTİRİLİP KALDIRILDI** — migration öncesi service-role ile her iki tablonun da canlıda 0 satır olduğu doğrulandı (veri kaybı riski yoktu), `personel_basvuru_taslagi` `DROP TABLE ... CASCADE` ile silindi, `/kayit/personel/[klinikId]` public rotası ve `/panel/ayarlar/qr-kodlar/personel-basvurulari` review ekranı kaldırıldı. **Şema (migration `20260810090000`)**: `is_basvurusu`, İş Başvuru Formu PDF'iyle (bir önceki turda eklenen `lib/pdf/is-basvuru-formu-sablon.ts`) BİREBİR aynı alanlarla genişletildi — kişisel bilgiler (doğum_tarihi, tc_kimlik_no, adres — hasta_hassas/personel_hassas'taki gibi ayrı şifreleme YOK, bu bilgi işe alım öncesi/geçici bir aday kaydı, personel_hassas'a taşınması ancak gerçek personel oluşunca oluyor), başvuru bilgileri (bizi_nereden_duydunuz, calismaya_baslama_tarihi, calisma_sekli, beklenen_ucret), eğitim (okul_bolum/mezuniyet_yili/sertifikalar), `is_deneyimi`/`referanslar` (jsonb array, PDF'teki 3/2 satırlık sabit bölümlerle eşleşiyor), `kvkk_onay`+tarihi, `degerlendiren_kullanici_id`+tarihi, ve yeni `personel_id` (→ hangi personelin hangi başvurudan geldiğini izlemek için). **`durum` 4'ten (yeni/incelendi/reddedildi/kabul_edildi) 3'e indirildi** (`beklemede`/`olumlu`/`olumsuz`, kullanıcının istediği tam bu 3 kelime) — eski değerler idempotent bir UPDATE ile eşlendi (canlıda 0 satır olduğu için pratikte devreye girmedi). Anon INSERT policy'si `personel_basvuru_taslagi`'deki desenle aynı: sadece `durum='beklemede' AND degerlendiren_kullanici_id IS NULL AND degerlendirme_tarihi IS NULL AND personel_id IS NULL` — anon'un değerlendirme/aktarım alanlarını spoof edememesi **gerçek anon key ile** test edildi (zengin alanlı insert başarılı; `durum:'olumlu'` veya `personel_id` eklenmiş insert'ler RLS tarafından gerçekten reddedildi; anon hâlâ hiçbir satırı SELECT edemiyor). Yeni `v_personel_bilgi_durumu` view'ı (`security_invoker=true`, `v_hasta_ozet` ile aynı desen) Personel Listesi kutucuklarındaki **"Bilgiler Tamam" / "Bilgiler Eksik"** rozetini besliyor — kriter (bilinçli varsayılan, CLAUDE.md'de dokümante edildi): doğum tarihi + adres(il veya serbest metin) + `personel_acil_kisi` satırı + `personel_hassas`'ta TC/pasaport (var/yok, düz metin okunmuyor) + rol=terapist ise ayrıca `personel_mesleki_belge` (diploma veya uzmanlık belge no). **Yeni `/panel/personel/basvurular` sayfası**: üstte İş Başvuru Formu PDF butonu (buraya taşındı) + `İş Başvurusu (Web)` QR kartı (mevcut `QrKart` bileşeni yeniden kullanıldı, hedef URL değişmedi — `/basvuru/is/[klinikId]`), altta "Bekleyen Başvurular" (durum=beklemede, düz liste) ve **"Arşiv"** (kullanıcı kararı: "olumsuz ve beklemede tıklanırsa da arşivde bilgiler dursun, ay/yıl olarak" — `beklemede`+`olumsuz` başvuruların TAMAMI `created_at`'e göre ay/yıl gruplu, katlanır bölümlerde; hiçbir satır silinmiyor, `beklemede` olanlar hem üstteki aktif kuyrukta hem arşiv taramasında görünüyor — iki ayrı state değil, aynı verinin iki görünümü). Her satırda 3 buton (Beklemede/Olumlu-Onayla/Olumsuz, `is-basvurulari` eski ekranındaki "her zaman 3 buton, mevcut durum devre dışı" deseniyle aynı) + tıklanınca eğitim/deneyim/referans/adres gibi ayrıntıları açan bir akordeon. **"Olumlu" bir durum-güncelleme çağrısı DEĞİL** — başvuru bilgileriyle önceden doldurulmuş `PersonelFormu` (mod=`olustur`) sihirbazını bir Dialog'da açıyor (yeni `basvuru` prop'u — ad soyad/telefon/doğum tarihi/TC/adres/pozisyon önceden dolduruyor, kurumsal e-posta/rol/şifre gibi başvuruda hiç olmayan zorunlu alanları admin BURADA tamamlıyor); `personelHesabiOlustur` action'ı artık `personelBilgileriGuncelle`'daki gibi bound bir `basvuruId: string | null` parametresi alıyor — personel satırı oluşur oluşmaz (terapist alt kaydı/yardımcı kayıtlar başarısız olsa bile ÖNCE) `is_basvurusu`yu `durum='olumlu'`+`personel_id`+`degerlendiren_kullanici_id`+tarih ile güncelliyor; fonksiyon başında da aynı başvurunun `personel_id`'sinin zaten dolu olup olmadığı kontrol ediliyor (aynı başvurudan iki kez personel oluşturulamaz). `PersonelFormu`'nun `mod=olustur` başarı callback'i (`onBasarili`) daha önce BİLİNÇLİ OLARAK hiç çağrılmıyordu (geçici şifre gösterimi + dialog'un kapanmaması için) — artık `basvuru` doluyken de çağrılıyor, ama YİNE dialog'u kapatmıyor (o hâlâ caller'ın kararı); `basvuru-satiri.tsx` bu callback'i sadece satırın görünür içeriğini "personele aktarıldı" notuna çevirmek için kullanıyor, Dialog (ve içindeki geçici şifre) elle kapatılana kadar açık kalıyor — **ilk yazımda bu ayrım kaçırılıp satırın tamamı (Dialog dahil) erken `return null` ile kaldırılmıştı, gözden geçirilirken düzeltildi.** QR Kodları hub'ından (`/panel/ayarlar/qr-kodlar`) "İş Başvurusu" (taşındı) ve "Personel Başvurusu" (kaldırıldı) kartları çıkarıldı, sadece Hasta Ön Kayıt + Anket ve Öneriler kaldı. Public web formu (`/basvuru/is/[klinikId]`) da PDF ile birebir aynı alanlarla genişletildi (kullanıcı kararı: "webteki formla pdf aynı olsun") — `KamuFormKarti`'ye yeni opsiyonel `genis` prop'u eklendi (bu form için `max-w-md`→`max-w-2xl`, diğer QR formları etkilenmedi). Artık hiçbir yerden kullanılmayan `yeni-personel-dialog.tsx` silindi. **Doğrulanan**: anon RLS (yukarıda), `is_basvurusu_durum_check`/`is_basvurusu_calisma_sekli_check` CHECK'lerinin geçersiz değerleri gerçekten reddettiği, `v_personel_bilgi_durumu`'nun gerçek veriyle sorgulandığı; `npx tsc --noEmit`, `npm run lint` (aynı 9 önceden var olan ilgisiz baseline) ve `next build` (46 route, silinen 3 route'un kaybolduğu + yeni `/panel/personel/basvurular`'ın derlendiği) temiz. **Doğrulanamayan**: klinik_admin'in gerçek bir oturumla (giriş bilgisi yok) Bekleyen/Arşiv listelerini görüntülemesi, "Olumlu" ile açılan Personel sihirbazının uçtan uca (gerçek `personelHesabiOlustur` çağrısı, kullanıcı hesabı+personel+`is_basvurusu` bağlanması) çalıştığı tarayıcıda denenmedi — sıradaki gerçek kullanıcı testinde özellikle bu akışın izlenmesi öneriliyor.

**QR Kodları genişletildi: Personel Puantaj (PIN ile giriş/çıkış) + Seans Sonu Anket ve Puanlama (2026-08-10, kullanıcı isteği).** İki yeni QR akışı eklendi. **1) Personel Puantaj**: QR Kodları sayfasına "Personel Puantaj — Giriş/Çıkış" kartları eklendi (`/puantaj/[klinikId]/giris|cikis`, anonim) — personel isim seçmeden sadece 6 haneli PIN girer, sistem PIN'i klinikteki aktif personele eşleştirir (migration `20260810100000`: `personel.puantaj_pin_hash` — pgcrypto `crypt()`/`gen_salt('bf')` ile TEK YÖNLÜ hash, `personel_hassas`'taki tersine çevrilebilir `pgp_sym_encrypt` deseninden BİLİNÇLİ OLARAK ayrı tutuldu; `personel_puantaj.kaynak` CHECK'ine `'self_qr'` eklendi). PIN'i personel KENDİSİ belirler (Personel Detay > Kişisel Bilgiler'e yeni "Puantaj PIN'i" kartı, `kendisi` görür/değiştirir, `yonetici` sadece sıfırlayabilir — unutma durumunda); 3 yeni RPC: `personel_puantaj_pin_belirle` (klinik-içi PIN çakışması reddi dahil), `personel_puantaj_pin_sifirla`, `personel_puantaj_pin_ile_kaydet` (anon+authenticated, `hizliPuantajKaydet`'teki giriş/çıkış idempotency kurallarını SQL'e taşıdı). **Güvenlik kararı**: isim seçilmediği için PIN "klinikteki tüm personel" havuzuna karşı deneniyor (doğum günü paradoksu etkisi) — bu yüzden 4 değil **6 haneli** PIN zorunlu kılındı; ayrı bir rate-limit/kilitleme KURULMADI (mevcut 4 anonim QR akışıyla aynı "bilinen kısıt, dokümante edildi" ölçüsü — bu kodlar dış dünyaya değil sadece klinik binasına asılıyor). **2) Seans Sonu Anket ve Puanlama**: randevuya özel dinamik QR (migration `20260810110000`: yeni `seans_degerlendirme` tablosu, `randevu_id UNIQUE` — ayrı bir token kolonu YOK, randevu.id zaten UUID v4 olduğu için doğrudan URL'de kullanılıyor `/anket/seans/[randevuId]`; `randevu_anket_uygun_mu`/`seans_degerlendirme_baglam_getir` RPC'leri `hasta_qr_self_servis_mi` ile aynı "anon'un ana tabloda SELECT policy'si yok, SECURITY DEFINER ile bypass" dersini tekrarlıyor). Tetikleyici mevcut "Seansı Tamamla" mekanizması (`randevu.durum='tamamlandi'`) — önceden SADECE Hasta Detay > Seans Geçmişi'nde (client-side `useSeansTamamla` hook'u ham update yapıyordu) vardı, şimdi yeni bir server action'a (`randevuSeansiTamamla`, `app/(app)/panel/randevular/actions.ts`) taşınıp Randevu Detay Paneli'ne de 6. buton ("Seansı Bitir", açıklama zorunlu) olarak eklendi — iki UI artık TEK fonksiyona iniyor. Tablet bilinçli olarak salt-okunur/kiosk kalmaya devam ediyor (CLAUDE.md'nin önceki "PIN'siz, bilinçli ertelendi" kararına dokunulmadı) — `tablet-ekrani.tsx`'e SADECE görüntüleme eklendi: oda boşken (`mevcut===null`) o günün en son biten `tamamlandi` randevusu varsa (`sonTamamlanan`) QR gösterilir, bir sonraki hasta check-in olunca (`mevcut` dolduğunda) OTOMATİK kaybolur — `mevcut` zaten `sonTamamlanan`'a her zaman öncelikli olduğu için ek bir "kaybolma" tetikleyicisi yazılmadı (kullanıcı kararı: "seans tamamlanınca gözüksün diğer hasta gelene kadar", sabit bir timeout yok). Seans Geçmişi zaman çizelgesindeki seans detay kartına da (varsa) puan yıldızları + öneri metni rozeti eklendi (`seans_degerlendirme` randevu listesine EMBED edilmeden, ayrı bir sorguyla çekilip client'ta eşleniyor — PostgREST'in 1:1 ters-FK embed'lerinde dizi/obje belirsizliğine girmemek için). **Gerçek bir hata bulunup düzeltildi**: bu veritabanında pgcrypto uzantısı `public` şemaya DEĞİL `extensions` şemasına kurulu (`SELECT extnamespace::regnamespace FROM pg_extension` ile doğrulandı) — yeni PIN RPC'leri ilk yazımda `SET search_path = public` kullanıyordu, bu da `crypt()`/`gen_salt()` çağrılarının "function does not exist" hatası vermesine yol açıyordu; gerçek bir psql oturum simülasyonuyla (yetki kontrolünü geçen bir admin/personel session'ı `SET LOCAL role authenticated` + `request.jwt.claims` ile taklit edilerek) test edilirken yakalandı, `search_path = public, extensions` olarak düzeltildi. **Aynı sınıf bir hatanın mevcut `personel_hassas_kaydet`'te de UYKUDA olabileceği not edildi** (aynı `SET search_path=public` ile `pgp_sym_encrypt` çağırıyor) — bu ortamda şifreleme anahtarı hiç kurulu olmadığı için erken `sifreleme_anahtari_kurulu_degil` guard'ı devreye girip bu hatayı hiç tetiklemiyor, gerçek anahtar kurulan bir ortamda aynı "does not exist" hatasını verebilir; bu migration'ın kapsamı dışında bırakıldı, sadece dokümante edildi. **Doğrulama metodolojisi**: migration'lar pooler üzerinden push edildi (`supabase db push --db-url`); tüm RPC'ler gerçek bir psql oturum simülasyonuyla (BEGIN + `SET LOCAL role authenticated` + `set_config('request.jwt.claims', ...)` + ROLLBACK/COMMIT) uçtan uca test edildi — PIN format reddi, admin/kendisi/yetkisiz üçlü yetki matrisi, klinik-içi PIN çakışma reddi, PIN ile giriş/çift-giriş/çıkış/çift-çıkış idempotency, PIN sıfırlama sonrası eski PIN'in çalışmaması, `randevu_anket_uygun_mu`'nun uygun/uygun-olmayan durumlarda doğru dönmesi, ikinci kez doldurmanın `23505` vermesi — hepsi gerçek verilerle denenip test satırları temizlendi (kalıntı kontrolü ayrıca doğrulandı, 0 satır). `npx tsc --noEmit`, `npm run lint` (9 hata önceden var olan ilgisiz baseline, dokunulan/yeni dosyalarda sıfır) ve `next build` (`/puantaj/[klinikId]/[tur]` ve `/anket/seans/[randevuId]` dahil, statik `seans` segmentinin `/anket/[klinikId]` dinamik segmentiyle aynı seviyede çakışmadan derlendiği doğrulandı) temiz. **Doğrulanamayan**: gerçek bir klinik_admin/personel oturumuyla tarayıcıda uçtan uca (PIN formu, tablet QR'ının gerçek bir seans sonunda belirmesi, telefonla okutup anket doldurma) test edilmedi (bu ortamda giriş bilgisi yok) — özellikle tablet ekranındaki QR geçişinin gerçek bir realtime check-in ile denenmesi öneriliyor.

**QR Kodları — yönetim listesine çevrildi, aktif/pasif kontrolü eklendi (2026-08-10, kullanıcı isteği: "orada qr kullanım listesi olması lazım, qr kodları oradan yönetecez").** `/panel/ayarlar/qr-kodlar` sayfasındaki 4 sabit `QrKart`'lık 2 sütunlu grid, veri-güdümlü tek-sütun bir **liste**ye çevrildi ve her satıra bir "Aktif" checkbox'ı eklendi — kapsam SADECE 4 statik/kapı QR'ı (Hasta Ön Kayıt, Anket, Personel Puantaj Giriş/Çıkış); `/anket/seans/[randevuId]` (randevuya özel, tablette anlık üretilen dinamik QR) bilinçli olarak bu listenin DIŞINDA, aç/kapa kavramı ona uymuyor. **Yeni tablo/migration şeması gerekmedi** — Tablet Görünümü ayarlarındaki (`tabletAyarlariGuncelle`) read-modify-write deseni birebir tekrarlandı: `klinik_ayarlar.ayarlar` jsonb'sine yeni bir `qr_kodlari` anahtarı (`{ [tip]: { aktif: boolean } }`) eklendi. Yeni `lib/qr/qr-kod-tanimlari.ts` (`QR_KOD_TANIMLARI` — 4 QR'ın icon/başlık/açıklama/yol/dosyaAdı merkezi kaydı, önceden `page.tsx` içine gömülü JSX'in veri hâli) ve yeni `qrKoduDurumGuncelle` server action'ı (`qr-kodlar/actions.ts`, klinik_admin-only). **Aktif/pasif sadece kozmetik değil**: kapatılan bir QR'ın anonim formu artık gerçekten "Kullanım Dışı" gösteriyor, kayıt/gönderim kabul etmiyor — bunun için migration `20260811090000` ile `klinik_qr_bilgisi_getir` ile birebir aynı desende yeni bir SECURITY DEFINER RPC (`qr_kodu_aktif_mi`) eklendi (anon'un `klinik_ayarlar`'da SELECT policy'si yok, `current_klinik_id()` authenticated oturum gerektiriyor — aynı "anon için public bilgi RPC'si" gerekçesi tekrar geçerli oldu). **Opt-out varsayılan bilinçli seçildi**: `qr_kodlari` anahtarı hiç yoksa (migration öncesi hiçbir klinikte set edilmemiş) RPC `true` döner — böylece migration hiçbir mevcut QR'ı aniden kapatmıyor. 3 anonim route'a (`/kayit/hasta/[klinikId]`, `/anket/[klinikId]`, `/puantaj/[klinikId]/[tur]`) aynı gating deseni eklendi: `klinikAdGetir` + `qrKoduAktifMi` paralel (`Promise.all`) çağrılıyor, klinik yoksa mevcut `KamuFormBulunamadi`, klinik var ama pasifse mevcut `KamuFormKarti` yeniden kullanılarak "Kullanım Dışı / Lütfen resepsiyon ile iletişime geçin." mesajı gösteriliyor — yeni bir bileşen gerekmedi. Checkbox anlık kaydediyor (Kişisel Bilgi Giriş Formu'ndaki KVKK onay checkbox'larıyla aynı UX — ayrı bir "Kaydet" butonu yok), yeni `components/panel/qr-kart-yonetilebilir.tsx` client wrapper `useTransition` ile `qrKoduDurumGuncelle`'ı çağırıp `QrKart`'ın (artık opsiyonel `aktifDurumu` prop'u alan) header'ındaki checkbox'ı besliyor. **Eşzamanlı oturum notu**: bu turda `components/panel/qr-kart.tsx`'in `icon` prop'u (bu konuşmanın DIŞINDA, eşzamanlı çalışan başka bir oturum tarafından) `LucideIcon`'dan `ReactNode`'a (`icon={<Icon .../>}` şeklinde çağrılan hale) çevrilmiş hâlde bulundu — `aktifDurumu` prop'u bu güncel API'ye göre eklendi, `QR_KOD_TANIMLARI`'deki `icon: LucideIcon` alanı ise `QrKartYonetilebilir` içinde render edilip `QrKart`'a JSX olarak geçiliyor (registry'nin kendisi bilinçli olarak component-referansı tutmaya devam ediyor, sadece tüketici tarafı yeni prop şekline uyarlandı). **Doğrulama**: migration pooler üzerinden push edildi; RPC gerçek bir admin oturum simülasyonuyla (hiç ayar yokken varsayılan `true`, bir tipi kapatınca `false`, diğer 3 tipin etkilenmediği, tekrar açınca `true`'ya dönmesi, farklı klinik/tip kombinasyonlarının birbirini etkilememesi) test edilip test verisi temizlendi. `npx tsc --noEmit`, `npm run lint` (9 hata önceden var olan ilgisiz baseline, dokunulan dosyalarda sıfır) ve `next build` temiz. **Doğrulanamayan**: gerçek bir klinik_admin oturumuyla tarayıcıda checkbox'a tıklayıp anlık kaydın ve karşılığında ilgili public formun gerçekten kapanmasının uçtan uca denenmesi (bu ortamda giriş bilgisi yok).

**Hasta Portalı genişletildi: kişisel giriş QR'ı, randevu talebi, kişisel bilgi değişiklik denetimi (2026-08-11, kullanıcı isteği).** Üç parça. **1) Kişisel QR**: Hasta Detay > Portal Erişimi kartına (`portal-erisim-karti.tsx`, portal erişimi aktifken görünür) `QrKart` ile bir "Portal Giriş QR'ı" eklendi — `yol="/portal/giris?telefon=..."`. **Netleşen karar (kullanıcıya soruldu)**: tam otomatik/şifresiz giriş DEĞİL — QR sadece `/portal/giris`'i telefon numarası ÖNCEDEN DOLU açıyor, hasta yine kendi şifresini giriyor; bu yüzden migration/yeni bir token sistemi GEREKMEDİ, sadece `giris/page.tsx` `searchParams.telefon` okuyup `PortalLoginForm`'a `defaultTelefon` geçiriyor. **2) Randevu Talebi**: portalda tam self-servis DEĞİL, mevcut randevu iptal talebi ile aynı "talep + resepsiyon onayı" modeli (kullanıcıya soruldu, bu seçildi) — yeni `randevu_talebi` tablosu (migration `20260811100000`, `randevu_iptal_talebi`'nin ikizi ama var olan bir randevuya değil doğrudan hastaya bağlı: `islem_tanimi_id`+`tercih_tarih`+`tercih_saat`+opsiyonel `terapist_id`/`not_metni`, `klinik_id` `hasta`dan trigger'la türetiliyor). Yeni `/portal/randevu-talep` sayfası (tedavi Select + tarih/saat + not) + `randevuTalebiOlustur` action (`iptalTalebiOlustur` ile aynı iskelet); `/portal/page.tsx`'e "Randevu Talep Et" butonu + kendi taleplerinin durumunu gösteren "Randevu Taleplerim" kartı eklendi. **Portal'ın mevcut `islem_tanimi` RLS policy'si SADECE hastanın geçmişte ödediği tedavilerle sınırlıydı** (odeme_kalemi join'i) — talep formunda hiç alınmamış bir tedavi de seçilebilmeli diye additive yeni bir policy (`islem_tanimi_select_portal_katalog`: kendi klinik + aktif=true) eklendi, mevcut dar policy'ye dokunulmadı (permissive OR ile birleşiyor). Panel tarafında `/panel/randevular`'a mevcut "Bekleyen İptal Talepleri" kartının yanına "Bekleyen Randevu Talepleri" kartı (`bekleyen-randevu-talepleri.tsx`) — "Reddet" basit bir durum güncellemesi (`randevuTalebiReddet`), "Randevu Oluştur" ise YENİ bir form YAZILMADAN mevcut `RandevuFormu`'nun yeni bir `talep` prop'uyla (hasta/tedavi/tarih/saat önceden dolu, gizli `talep_id` input'u) açılması — resepsiyon gerçek terapist/oda/cihaz/kesin saati seçip gönderir, mevcut `randevuOlustur` action'ı genişletilip (`talep_id` opsiyonel alan) randevu başarıyla oluşunca `randevu_talebi`yi de `onaylandi`+`olusturulan_randevu_id` ile güncelliyor (iki ayrı update, `iptalTalebiOnayla` ile aynı güven modeli — DB transaction'a sarılmıyor). **3) Kişisel bilgi değişiklik denetimi**: `hasta` tablosuna `hasta_hassas`'takiyle BİREBİR aynı 4 denetim kolonu (`olusturan_tip/_kullanici_id`, `son_guncelleyen_tip/_kullanici_id`) eklendi — YENİ bir trigger fonksiyonu YAZILMADI, var olan `hasta_hassas_denetim_bilgisi_ata()` (migration `20260731111500`, tabloya özel kolon adı içermeyen genel bir fonksiyon) aynen `hasta` tablosuna da bağlandı; bu sayede hem portal'ın `bilgileriGuncelle`'ı hem panelin `temelBilgileriGuncelle`'ı hiç dokunulmadan otomatik denetleniyor. Portal Bilgilerim (`/portal/bilgilerim`) formunun altına "Son değişiklik: {tarih/saat} — Siz / Klinik personeli" satırı eklendi (`hasta` ve `hasta_hassas`'ın `updated_at`'inden daha yeni olanı seçiliyor) — **personel adı/ID'si BİLİNÇLİ OLARAK gösterilmiyor** (panelin staff-facing footer'ının aksine, hastaya başka bir çalışanın kimliğini göstermek uygunsuz), sadece hasta/personel ayrımı yapılıyor. **Doğrulama**: migration pooler üzerinden push edilip gerçek oturum simülasyonuyla (admin/hasta ayrımının trigger'da doğru işlediği, hastanın sadece kendi randevu_talebi'ni oluşturabildiği — başka hasta adına denemenin RLS'ten gerçekten reddedildiği, portal'dan artık hiç alınmamış bir tedavinin de görünür olduğu, adminin talebi onaylayabildiği) test edilip test verisi temizlendi. `npx tsc --noEmit`, `npm run lint` (9 hata önceden var olan ilgisiz baseline) ve `next build` (`/portal/randevu-talep` dahil) temiz. **Doğrulanamayan**: gerçek kullanıcıyla tarayıcıda uçtan uca (QR okutup telefonun dolu geldiği, randevu talebi gönderip panelden "Randevu Oluştur" Dialog'unun doğru prefill ile açıldığı) test edilmedi (bu ortamda giriş bilgisi yok).

Hasta Detay > Cari & Ödeme > "Bakiye Hareketleri" düz liste yerine gerçek bir tablo oldu (`/panel/hastalar/[id]/cari`, kullanıcı isteği: tarih/saat/işlem türü/terapist/kategori iskonto/iskonto/işlem bakiye sütunları). Sütun kaynağı hareketin `tur`üne göre değişiyor — `randevu_id` dolu (paketsiz check-in borcu) satırlarda İşlem Türü/Terapist `randevu.islem_tanimi.ad`/`randevu.terapist.personel.ad_soyad`'dan; `odeme_id` dolu satırlarda İşlem Türü `odeme_kalemi` (işlem/paket adlarının birleşimi) listesinden geliyor, terapist "—" kalıyor (bir ödeme birden fazla kaleme/seansa bağlı olabilir, tek terapiste indirgenemez). **İki ayrı iskonto kavramı netleştirildi (kullanıcıya soruldu)**: "Kategori İskonto" = hastanın Vita/Plus/Elit/Prime kademesinden OTOMATİK gelen indirim — `islem_tanimi.vita_fiyat` ile fiilen tahsil edilen/borçlandırılan tutar (`odeme_kalemi.birim_fiyat` veya check-in'de yazılan `tutar`) arasındaki fark olarak hesaplanıyor (istemci `islem_tanimi_etkin_fiyat()`'ı tekrar hesaplamıyor, gerçekleşmiş rakamlardan farkı alıyor — kademe override'ı da otomatik doğru çıkıyor); "İskonto" = resepsiyonun Ödeme Al ekranında elle girdiği ek indirim (`odeme.iskonto_tutari`), check-in borcunda hiç yok (o akışta manuel indirim alanı yok) → "—". **Kümülatif Bakiye sütunu için yeni bir mimari karar**: satırlar `created_at DESC` (en yeni ilk) ve sadece son 30 hareket çekiliyor — bu pencereden ileri doğru toplarsak (eski→yeni) pencere dışındaki geçmiş bilinmediği için yanlış bir "0'dan başlıyor" varsayımı yapılırdı; bunun yerine `v_hasta_ozet.bakiye`'den (o anki GERÇEK toplam bakiye, ayrı bir sorgu olarak eklendi) geriye doğru hesaplanıyor (`kredi` + / `borç` − / `ödeme`,`iade` bakiyeyi değiştirmez — `v_hasta_ozet`'teki mevcut kuralla birebir aynı), pencere boyutundan bağımsız her zaman doğru çıkıyor. Tablo, projede zaten yerleşik `<table>` deseniyle (bkz. Kamusal Giderler `odeme-tablosu.tsx`) yazıldı — yeni bir Table bileşeni eklenmedi. `types/hasta-detay.ts`'teki `HastaBakiyeHareket` tipi `randevu`/`odeme` embed alanlarıyla genişletildi (tek çağıran yer olduğu için kırılma riski yoktu). `npx tsc --noEmit`, `npm run lint` (9 hata önceden var olan ilgisiz baseline, dokunulan dosyalarda sıfır) ve `next build` temiz. Gerçek kullanıcıyla uçtan uca test edilmedi (giriş bilgisi yok).

Bakiye Hareketleri kart başlığına "Ödeme Ekle" butonu eklendi (klinik_admin/resepsiyon-only, kullanıcı kararı: ürün/paket seçimi olmayan, doğrudan `hasta_bakiye_hareket`'e manuel bir kayıt ekleyen ayrı/basit bir form — mevcut "Ödeme Al" kartından bilinçli olarak farklı, o kart ürün/kalem/parçalı ödeme akışını korumaya devam ediyor). Yeni `BakiyeHareketiEkleButonu` (`[id]/bakiye-hareketi-formu.tsx`, Personel Takip'teki "Ödeme Ekle" Dialog deseniyle birebir aynı — kendi açık/kapalı state'ini tutuyor, kayıt başarılı olunca otomatik kapanıyor) Tür (Ödeme/Kredi/Borç/İade — `BAKIYE_HAREKET_ETIKETLERI`'nden türetilen Select), Tutar, Tarih, Açıklama alanlarını alıyor. **Mimari karar**: `odeme_olustur`/`randevu_gelis_isaretle`'nin aksine bu bir RPC DEĞİL — `hasta_bakiye_hareket_ekle_resepsiyon_admin` RLS INSERT policy'si zaten klinik_admin/resepsiyon'a doğrudan izin verdiği için yeni action (`bakiyeHareketiEkle`, `[id]/actions.ts`) düz bir `.insert()` yapıyor; tabloda klinik_id'yi otomatik türeten bir trigger olmadığı için (diğer INSERT'lerin hepsi SECURITY DEFINER RPC içinden `current_klinik_id()` ile yazıyordu) `klinik_id` action içinde `kullanici.klinik_id`'den elle set ediliyor. `npx tsc --noEmit`, `npm run lint` (9 hata önceden var olan ilgisiz baseline, dokunulan dosyalarda sıfır) ve `next build` temiz. Gerçek kullanıcıyla uçtan uca test edilmedi (giriş bilgisi yok).

"Ödeme Ekle" (Bakiye Hareketleri) yeniden düzenlendi + yeni "Borç Kapatma" akışı eklendi (2026-08-11/12, kullanıcı isteği — Ödeme Al/Ödeme Geçmişi kartları bir önceki turda tamamen kaldırılmıştı, bu tur o boşluğun yerine geleni inşa ediyor). **"Ödeme Ekle" değişikliği**: buton emerald dolu (yeşil) yapıldı; bir önceki turdaki "Tür" dropdown'ı (Ödeme/Kredi/Borç/İade) kaldırıldı — bu buton artık SADECE ödeme ekliyor (`tur` gizli input'la `'odeme'` sabit gönderiliyor), yerine "Ödeme Tipi" adında kutu-seçmeli (dropdown değil) Nakit/Kredi Kartı/Havale seçici geldi — yeni paylaşılan `odeme-tipi-secici.tsx` bileşeni (`OdemeTipiSecici`, `ODEME_TIPI_ETIKETLERI`, `ODEME_TIPI_SECILI_SINIFI`) hem burada hem yeni borç kapatma dialogunda kullanılıyor. Seçilen ödeme tipi ayrı bir DB kolonu gerektirmeden açıklamaya etiket olarak ekleniyor (`"Kredi Kartı — not"`). **Yeni "Borç Kapatma" akışı**: Bakiye Hareketleri tablosunda açık (henüz ödenmemiş) bir borç satırına tıklanınca "Borç Satırını Düzenle" dialogu açılıyor (`bakiye-hareket-satiri.tsx`, yeni) — İskonto (₺), Faturalı checkbox, Ödeme Tipi (aynı kutu-seçici), Kaydet. **Kullanıcıya soruldu ve netleşen mimari karar**: fatura tablosu sadece tamamlanmış bir ödemeye bağlanabildiği için (`fatura.odeme_id NOT NULL`) Kaydet GERÇEK bir tahsilat sayılıyor — yeni SECURITY DEFINER RPC `hasta_bakiye_hareket_borc_kapat` (migration `20260812090000`, `odeme_olustur`/`randevu_gelis_isaretle` ile aynı desen) borcun ORİJİNAL tutarı kadar yeni bir `'kredi'` hareketi yazarak borcu tam olarak sıfırlıyor; girilen iskonto sadece FİİLEN TAHSİL EDİLEN tutarı (`odeme_satiri`) azaltıyor, bakiyede kalıntı borç BIRAKMIYOR (indirim klinik tarafından üstlenilen bir write-off olarak muhasebeleşiyor). Orijinal borç satırı hiç güncellenmiyor/silinmiyor (append-only ledger felsefesiyle tutarlı, bkz. önceki randevu_gelis_isaretle kararı) — sadece `odeme_id` alanı kapatan ödemeye işaret edecek şekilde dolduruluyor (idempotency: `odeme_id` doluysa RPC "zaten_kapatilmis" ile reddediyor, satır tabloda "Ödendi" rozetiyle artık tıklanamaz görünüyor). Faturalıysa aynı transaction'da `fatura` satırı da (`durum='bekliyor'`) ekleniyor. **Fatura eksik bilgi kontrolü**: Faturalı işaretlenince ad soyad/e-posta/adres/TC kimlik-pasaport no'dan hangileri eksikse dialogda anında uyarı listeleniyor (yeni paylaşılan `lib/fatura/eksik-bilgi.ts` — `faturaEksikAlanlariBul`/`FATURA_ALAN_ETIKETLERI`, hem client'ta canlı uyarı hem RPC'nin server-side aynı kontrolü — savunma amaçlı ikinci katman — için tek kaynak); eksikken Kaydet butonu devre dışı kalıyor. **Muhasebe > Faturalar** ("Bu ekran henüz hazır değil" placeholder'ıydı) gerçek bir listeye çevrildi (`/panel/muhasebe/faturalar`) — `fatura` tablosunu `odeme`/`hasta`/`odeme_satiri` ile join'leyip tarih/hasta/açıklama/tutar/durum gösteriyor, "Faturayı Kes" retry butonu (mevcut `faturaTetikle` action'ı, hastalar/[id]/actions.ts'ten cross-route import edildi — Paraşüt hesabı hâlâ yok, buton hâlâ "henüz bağlı değil" nazik mesajını dönüyor) her satırda. `types/hasta-detay.ts`'teki `HastaBakiyeHareket`'e `odeme_id` eklendi (borcun kapanıp kapanmadığını UI'da anlamak için). `npx tsc --noEmit`, `npm run lint` (9 hata önceden var olan ilgisiz baseline, dokunulan/yeni dosyalarda sıfır) ve `next build` (yeni `/panel/muhasebe/faturalar` içeriği dahil 49 route) temiz; migration pooler üzerinden push edildi (DDL hatasız uygulandı). **Doğrulanamayan**: gerçek bir klinik_admin oturumuyla tarayıcıda borç satırına tıklayıp Kaydet'in gerçekten borcu kapattığı, Muhasebe > Faturalar'da satırın göründüğü uçtan uca denenmedi (bu ortamda giriş bilgisi yok) — sadece migration push + tsc/lint/build ile doğrulandı.

Bakiye Hareketleri kartına "PDF İndir" eklendi (2026-08-12, kullanıcı isteği): "Ödeme Ekle"nin yanına, hastanın cari hareketlerini PDF olarak indiren bir buton geldi — kolonlar bilinçli olarak ekrandaki tablodan dar tutuldu (Tarih/Saat/İşlem Türü/Terapist/Bakiye), Kategori İskonto ve İskonto sütunları kullanıcı kararıyla PDF'e YANSITILMADI. Yeni `GET /api/hasta-cari-hareketler/pdf?hastaId=...` route'u, projenin kanıtlanmış Puppeteer+`@sparticuz/chromium` altyapısını (Kayıt Formu/İş Başvuru Formu PDF'leriyle aynı `lib/pdf/browser.ts`+`pdf-yardimcilari.ts`) üçüncü kez kullanıyor — html2canvas'a hiç dönülmedi. **Ekrandaki tablo ile PDF arasında rakam sapması riskini önlemek için** kümülatif bakiye/işlem adı/terapist hesaplama mantığı (`hareketleriGorunumeCevir`, önceden `cari-odeme-sekmesi.tsx` içine gömülüydü) `lib/hasta/bakiye-hareket-gorunum.ts`'e çıkarılıp iki tüketici arasında (ekran tablosu + yeni PDF route) paylaşıldı — tek kaynak, iki yerde ayrı ayrı hesaplanmıyor. PDF ekrandaki `.limit(30)` penceresine bağlı değil, son 1000 hareketi kapsıyor (gerçek bir dışa aktarım/export dokümanı olduğu için). Yeni `lib/pdf/cari-hareketler-sablon.ts`/`cari-hareketler-html.ts` — form şablonlarındaki (`form-sablon-cekirdegi.ts`) `formBasligi()` BİLİNÇLİ OLARAK kullanılmadı (o, elle doldurulacak boş formlar için "Tarih: ____/____/______" satırı içeriyor, otomatik üretilen bir rapor için anlamsız olurdu) — yerine gerçek oluşturulma tarihini gösteren kendi başlık bloğu yazıldı. `PdfIndirButonu` (`components/panel/pdf-indir-butonu.tsx`) genel bileşenine opsiyonel `size` prop'u eklendi (üçüncü kullanım yerinde ilk kez ihtiyaç oldu — "Ödeme Ekle" ile aynı `sm` boyutta hizalanması için). `next.config.ts`'teki `outputFileTracingIncludes` ve `vercel.json`'daki `functions` bloğuna yeni route eklenmesi UNUTULMADI (CLAUDE.md'nin önceki "kritik, unutulması kolay adım" notu) — `next build` sonrası `.next/server/app/api/hasta-cari-hareketler/pdf/route.js.nft.json` içinde 4/4 `@sparticuz/chromium/bin/*.br` dosyasının gerçekten listelendiği aynı ampirik yöntemle doğrulandı. `npx tsc --noEmit`, `npm run lint` (9 hata önceden var olan ilgisiz baseline, dokunulan/yeni dosyalarda sıfır) ve `next build` (`/api/hasta-cari-hareketler/pdf` dahil 50 route) temiz. **Doğrulanamayan**: gerçek bir klinik_admin oturumuyla tarayıcıda PDF'in gerçekten indirildiği ve Türkçe karakterlerin/tablo düzeninin doğru göründüğü test edilmedi (bu ortamda giriş bilgisi yok) — ilk gerçek Vercel deploy'unda denenmesi öneriliyor.

**Gerçek bir renk hatası bulunup düzeltildi + borç kapatma dialogu sadeleştirildi (2026-08-12, kullanıcı "seçili olan belli olmuyor" bildirdi).** Kullanıcının "Ödeme Tipi" kutucuklarında seçili olan renklenmiyor" bildirimi üzerine derlenmiş CSS byte-offset ile incelendi: `.bg-surface-2` (Button'ın `variant="outline"` sınıfı) derlenmiş dosyada `.bg-emerald-500`'den SONRA tanımlanıyordu — aynı özgüllükte "sonraki kazanır" kuralı yüzünden emerald arka plan override'ı sessizce kaybediyordu (border rengi kazanıyordu ama dolgu rengi kazanmıyordu, bu yüzden seçili/seçili-değil ayrımı görsel olarak belirsizdi). **Bu, projede daha önce "çalışıyor" diye dokümante edilmiş aynı desenin (`durum-butonlari.tsx`/Personel Listesi butonlarındaki "outline variant üstüne soft-tint className") de aynı riski taşıyabileceği anlamına geliyor — o dosyalara bu turda dokunulmadı (kapsam dışı), ama ileride benzer bir "renk gelmiyor" şikayeti gelirse ilk bakılacak yer burası olmalı.** Çözüm Tailwind v4'ün `!` (important) önekiyle garanti edildi — `ODEME_TIPI_SECILI_SINIFI` artık `!border-emerald-500 !bg-emerald-500 !text-white hover:!bg-emerald-600 dark:hover:!bg-emerald-500/90`; `next build` sonrası derlenmiş CSS'te `.\!bg-emerald-500{background-color:var(--color-emerald-500)!important}` olarak gerçekten üretildiği doğrulandı (varsayıma bırakılmadı, projenin "aracın gerçek çıktısını ölç" metodolojisiyle tutarlı). Bu tek paylaşılan sabit hem "Ödeme Ekle" tetikleyici butonunu hem `OdemeTipiSecici` kutularını besliyor, tek düzeltme iki yerde de geçerli oldu. **Ayrıca kullanıcı kararıyla**: Borç Satırını Düzenle dialogundan "Ödeme Tipi" seçici tamamen kaldırıldı (borç kapatma artık ödeme yöntemi sormuyor) — RPC'nin hâlâ zorunlu tuttuğu `p_yontem` parametresine gizli bir `"nakit"` sabit değeri gönderiliyor (RPC/migration'a dokunulmadı, sadece UI'dan kaldırıldı). İskonto (₺) alanı `0` değeriyle başlayıp kullanıcı silene kadar ekranda duran bir sayı state'inden, gerçekten boş başlayan bir metin state'ine (`iskontoMetni`, `useState("")`) çevrildi — `Number("") === 0` olduğu için boş bırakılırsa hesaplama/sunucu tarafı hâlâ 0 iskonto olarak doğru işliyor, sadece ekranda artık "0" görünmüyor. `npx tsc --noEmit`, `npm run lint` (9 hata önceden var olan ilgisiz baseline) ve temiz `.next` ile `next build` (50 route + 3 PDF route'un chromium bin tracing'i yeniden doğrulandı) temiz. **Doğrulanamayan**: gerçek tarayıcıda seçili kutunun görsel olarak yeşil dolduğu, iskonto alanının gerçekten boş göründüğü (bu ortamda giriş bilgisi yok) — sadece derlenmiş CSS içeriği ve kod mantığı doğrulandı.

**Gerçek bir hata bulunup düzeltildi: Kişisel Bilgiler'de girilen adres kaydedildikten sonra "kayboluyormuş" gibi görünüyordu (2026-08-13, kullanıcı bildirdi).** Kök neden veri kaybı DEĞİL, client-side cache tazeliğiydi: "Kişisel Bilgi Giriş Formu"ndaki `hassas` verisi (`kisisel-bilgiler-sekmesi.tsx`) sunucu tarafından değil TanStack Query (`useHastaHassas`, `queries.ts`) ile çekiliyor; `temelBilgileriGuncelle`/`anamnezGuncelle` birer Server Action olduğu için kayıt sonrası sadece `revalidatePath` çağırıyorlardı (Next.js RSC cache'ini etkiler) — TanStack Query'nin `["hasta_hassas", hastaId]` cache key'ini hiç invalidate etmiyorlardı. `QueryProvider`'da (`components/panel/query-provider.tsx`) `staleTime: 30_000` + `refetchOnWindowFocus: false` ayarlı olduğu için, kayıttan sonraki 30 saniye içinde başka bir sekmeye/sayfaya gidip geri dönüldüğünde (QueryClient route değişse bile bellekte kalıyor) React Query "hâlâ taze" sayıp kayıttan ÖNCEKİ veriyi göstermeye devam ediyordu — girilen adres silinmemişti, sadece ekranda henüz görünmüyordu. Projede aynı `hasta_hassas` sekmesindeki diğer mutasyonlar (`useMutation` ile yazılanlar, örn. `useVucutIsaretKaydet`) zaten `queryClient.invalidateQueries` çağırıyordu — bu iki form (Server Action + `useActionState` deseni) o adımı atlamıştı. Çözüm: `TemelBilgilerFormu` ve `AnamnezFormu`'na (`detayli-bilgiler-karti.tsx`) `durum?.success` olduğunda `queryClient.invalidateQueries({queryKey:["hasta_hassas", hasta.id]})` çağıran birer `useEffect` eklendi — ikisi de aynı `hasta_hassas` satırını paylaştığı için tek query key yeterli. `npx tsc --noEmit` ve `npm run lint` temiz. **Doğrulanamayan**: gerçek bir klinik_admin oturumuyla tarayıcıda adres girip kaydettikten hemen sonra başka bir sekmeye geçip geri dönüldüğünde verinin artık göründüğü uçtan uca denenmedi (bu ortamda giriş bilgisi yok).

**Gerçek bir görünürlük hatası bulunup düzeltildi: Borç Kapatma'da girilen iskonto Bakiye Hareketleri'nde hiç görünmüyordu (2026-08-13, kullanıcı "iskonto uygulanmadan fatura kaydı açtı" bildirdi).** Araştırma: Muhasebe > Faturalar'daki tutar hesaplaması (`odeme_satiri.tutar` toplamı) zaten net/iskonto düşülmüş tutarı doğru gösteriyordu — asıl kök neden `hasta_bakiye_hareket_borc_kapat` RPC'sinin borcu kapatmak için yazdığı yeni `'kredi'` satırıydı (kullanıcının "bakiye hareketlerinde de kredi diye bir şey ilave etti" dediği kayıt): bu satır HEM borcun bağlı olduğu `randevu`'ya HEM de kapatma işleminin `odeme`sine bağlı — `lib/hasta/bakiye-hareket-gorunum.ts`'teki `hareketleriGorunumeCevir` `if (h.randevu) {...} else if (h.odeme) {...}` şeklinde birbirini DIŞLAYAN dallara ayrıldığı için randevu dalı öncelik kazanıyor ve `odeme.iskonto_tutari` (tam da girilen iskonto) hiç okunmuyordu — tabloda "İskonto" sütunu bu satır için hep "—" görünüyordu, kullanıcı da bunu "iskonto uygulanmadı" diye yorumlamıştı (parayı gerçekte doğru şekilde eksik tahsil ediyordu, sadece ekranda görünmüyordu). **Kredi satırının kendisi kaldırılmadı** — bu, borcun tam tutarla sıfırlanması için kasıtlı bir tasarım (bkz. bir önceki turun notu); sadece görünürlük düzeltildi: `manuelIskontoTutari` artık `h.odeme` var olduğunda (randevu dalından bağımsız, ayrı bir `if`) her zaman `odeme.iskonto_tutari`'den okunuyor — hem sade "Ödeme Ekle" satırlarında hem hem-randevu-hem-odeme'ye bağlı borç-kapatma kredi satırlarında doğru çalışıyor, terapist/işlem adı/kategori iskontosu (randevu dalından geliyor) hâlâ değişmedi. Paylaşılan fonksiyon olduğu için ekran tablosu ve Cari Hareketler PDF'i aynı anda düzeldi. `npx tsc --noEmit` ve `npm run lint` temiz. **Doğrulanamayan**: gerçek bir borç kapatma + iskonto senaryosunda tarayıcıda "İskonto" sütununun artık doğru rakamı gösterdiği uçtan uca denenmedi (bu ortamda giriş bilgisi yok).

**Borç Kapatma'dan "kredi" satırı tamamen kaldırıldı (2026-08-13, kullanıcı kararı: "kredi diye bir şey yok, borç var ödeme var sadece").** Bir önceki turun tasarımı (borcu ayrı bir offsetting `'kredi'` satırıyla sıfırlama) kullanıcı tarafından açıkça reddedildi — bunun yerine İskonto girilince SADECE orijinal borç satırının kendisi güncelleniyor. `hasta_bakiye_hareket_borc_kapat` RPC'si (migration `20260813090000`, `CREATE OR REPLACE`, imza aynı) yeniden yazıldı: artık yeni bir `hasta_bakiye_hareket` satırı INSERT etmiyor, orijinal borç satırını doğrudan `UPDATE ... SET tur='odeme', tutar=net_tutar, odeme_id=v_odeme_id` ile ödemeye dönüştürüyor. **Bakiye matematiği hâlâ doğru** — `v_hasta_ozet`/`hareketleriGorunumeCevir`'in ikisinin de paylaştığı kural (`kredi:+, borc:-, else:0`) sayesinde `tur` 'borc'tan 'odeme'ye geçince satırın bakiyeye etkisi otomatik olarak `-tutar`'dan `0`'a düşüyor — ekstra bir hareket YAZILMADAN borç kapanmış oluyor (append-only ledger felsefesinden bu TEK etkileşimli akış için bilinçli olarak sapıldı, randevu_gelis_isaretle'nin otomatik borç yazma akışına dokunulmadı). `odeme`/`odeme_kalemi`/`odeme_satiri`/`fatura` oluşturma mantığı (faturalı işaretlenince "Faturayı Kes" akışına giriş yapması) AYNEN korundu — sadece ikinci bir ledger satırı yazılmıyor. Bir önceki turda eklenen "İskonto Bakiye Hareketleri'nde görünmüyordu" düzeltmesi (`hareketleriGorunumeCevir`'in `h.odeme` her zaman ayrıca okunması) bu yeni tasarımda da geçerli kalıyor — artık aynı (tek) satır hem randevu hem odeme'ye bağlı olduğu için hem terapist/işlem adı hem girilen iskonto doğru gösteriliyor. UI tarafında `bakiye-hareket-satiri.tsx`'teki `kapaliBorc`/"Ödendi" rozeti mantığı kaldırıldı (artık gereksiz — satır zaten `tur='odeme'` olduğu için normal "Ödeme" rozetiyle görünüyor ve `tikanabilir` sade `hareket.tur === "borc"` kontrolüne indirgendi). `npx tsc --noEmit`, `npm run lint` ve `next build` temiz; migration pooler üzerinden push edildi. **Doğrulanamayan**: gerçek bir borç kapatma senaryosunda tarayıcıda satırın "Borç"tan "Ödeme"ye dönüştüğü ve bakiyenin doğru güncellendiği uçtan uca denenmedi (bu ortamda giriş bilgisi yok).

Hastalar listesine cari borç noktası + eksik bilgi rozeti eklendi (2026-08-13, kullanıcı isteği): her satırın avatarının sağ alt köşesinde küçük bir nokta — `v_hasta_ozet.bakiye < 0` ise kırmızı (borcu var), değilse yeşil. "KVKK onaylı · tarih" metni kaldırıldı; KVKK onaylı hastalarda artık onun yerine (varsa) E-posta/Adres/Telefon'dan hangileri eksikse "E-posta, Adres eksik" gibi amber bir rozet gösteriliyor, hiçbiri eksik değilse rozet hiç görünmüyor (KVKK onaylanmamış hastalarda "KVKK onayı al" butonu değişmeden duruyor). Yeni `types/hasta.ts` → `HastaListeSatiri` tipi (`HastaSatir` + `eposta`/`hasta_hassas.adres`/`bakiye`) — `bakiye` `hasta` tablosundan gelmiyor, `v_hasta_ozet`'in ayrı bir sorgusundan (`hasta_id IN (...)`) çekilip sayfada `Map` ile birleştiriliyor (view'ın gerçek bir FK ilişkisi olmadığı için PostgREST embed'i çalışmıyor). `npx tsc --noEmit`, `npm run lint` ve `next build` temiz. **Doğrulanamayan**: gerçek tarayıcıda noktanın/rozetin doğru göründüğü test edilmedi (bu ortamda giriş bilgisi yok).

**Gerçek bir hesaplama hatası bulunup düzeltildi: Borç Kapatma sonrası "Kategori İskonto" sütunu manuel iskontoyu da içine alıp yanlış rakam gösteriyordu (2026-08-13, kullanıcı ekran görüntüsüyle bildirdi — Vita kategorisi %0 olduğu halde Kategori İskonto ₺500 görünüyordu).** Kök neden: `hasta_bakiye_hareket_borc_kapat` RPC'si borcu kapatırken satırın `tutar`ını NET tutara (manuel iskonto zaten düşülmüş) güncelliyor (bkz. bir önceki turun notu), ama `lib/hasta/bakiye-hareket-gorunum.ts`'teki `kategoriIskontoTutari` hesabı hâlâ `vita_fiyat - h.tutar` formülünü kullanıyordu — bu formül kategori (Vita/Plus/Elit/Prime) bazlı otomatik indirimi hesaplamak için yazılmıştı ama artık netleşmiş `h.tutar`'dan girilen manuel iskontoyu da (yanlışlıkla) "kategori indirimi" olarak sayıyordu, hem bu sütunda hem ayrıca "İskonto" sütununda (doğru şekilde `odeme.iskonto_tutari`'den) aynı tutar iki kez görünüyordu. Düzeltme: kategori iskontosu hesaplanmadan önce `h.tutar`'a `h.odeme.iskonto_tutari` geri eklenerek orijinal (kategori-indirimli ama manuel-indirimsiz) tutar yeniden kuruluyor, formül bu rekonstrükte edilmiş tutar üzerinden çalışıyor — henüz kapatılmamış (`borc` durumundaki) satırlarda `h.odeme` boş olduğu için davranış değişmiyor. Ekran tablosu ve Cari Hareketler PDF'i aynı fonksiyonu paylaştığı için düzeltme ikisinde birden geçerli oldu. `npx tsc --noEmit` ve `npm run lint` temiz. **Doğrulanamayan**: gerçek bir borç kapatma + manuel iskonto senaryosunda tarayıcıda "Kategori İskonto" sütununun artık doğru (0 veya sadece gerçek kategori indirimi kadar) gösterdiği uçtan uca denenmedi (bu ortamda giriş bilgisi yok) — kullanıcının bir sonraki denemede teyit etmesi gerekiyor.

Muhasebe > "Kesilen Faturalar" iki bölümlü "Gelirler Takibi" hub'ına dönüştürüldü + yeni "Cari Alacaklar Takibi" ekranı eklendi (2026-08-13, kullanıcı isteği). `MENU_GRUPLARI`'deki Muhasebe grubunda "Kesilen Faturalar" öğesi "Gelirler Takibi"e (`/panel/muhasebe/gelirler-takibi`, ikon `HandCoins`) çevrildi — Tedaviler/Personel'de zaten kullanılan iç-içe hub deseni tekrarlandı: yeni bir sayfa `MenuGrubuSayfasi`'nı bu turda MENU_GRUPLARI'ye kayıtlı olmayan, sadece yerel bir `MenuGrubu` objesiyle çağırıp 2 `ModuleCard` gösteriyor (Tedaviler'in kendisi MENU_GRUPLARI'den geliyordu, burada iç hub MENU_GRUPLARI dışında tutuldu çünkü sidebar'da ayrı bir üst seviye grup değil, mevcut Muhasebe grubunun bir alt-sayfası). Eski `/panel/muhasebe/faturalar` (liste + `FaturaDurumHucresi`) `git mv` ile `/panel/muhasebe/gelirler-takibi/faturalar`'a taşındı, davranış/görünüm değişmedi. **Yeni "Cari Alacaklar Takibi" (`/panel/muhasebe/gelirler-takibi/cari-alacaklar`)**: hasta başına Toplam Bakiye / Tahsil Edilen / Kalan Bakiye gösteren bir tablo, satırın (her hücresi ayrı `Link` olarak render edilip tüm satır tıklanabilir hale getirildi — `<tr>` doğrudan `Link` olamadığı için) tıklanması hastanın `/panel/hastalar/[id]/cari` (Cari & Ödeme) sayfasına götürüyor. Yeni `v_hasta_cari_ozet` view'ı (migration `20260813100000`, `v_hasta_ozet` ile aynı `security_invoker=true` deseni) — **mimari karar**: `hasta_bakiye_hareket_borc_kapat` RPC'si (bir önceki turdan) bir borcu kapatırken satırın kendisini `tur='borc'`'tan `tur='odeme'`'ye çevirip `randevu_id`'sini KORUYOR (ayrı bir satır eklemiyor); bu sayede "borç kaynaklı tahsilat" satırları `tur='odeme' AND randevu_id IS NOT NULL` ile, hem peşin tam ödemelerden (`odeme_id` var ama `randevu_id` yok) hem manuel "Ödeme Ekle" kayıtlarından (ikisi de yok) ayırt edilebiliyor. Kalan Bakiye = hâlâ açık (`tur='borc'`) toplamı, Tahsil Edilen = kapanmış borç satırlarının (yukarıdaki filtre) toplamı, Toplam Bakiye = ikisinin toplamı; view sadece hiç check-in kaynaklı borcu olmuş (açık veya kapanmış) hastaları listeliyor, `v_hasta_ozet.bakiye`'nin aksine kredi/iade türlerini bilinçli olarak dışarıda bırakıyor (bu ekran özellikle "borç → tahsilat" akışını takip ediyor). Migration pooler üzerinden push edilip view service-role ile gerçek veriyle (hem açık hem kapanmış borç örnekleri) sorgulanarak doğrulandı. `npx tsc --noEmit`, `npm run lint` (9 hata önceden var olan ilgisiz baseline) ve `next build` (53 route, `/panel/muhasebe/faturalar`'ın kaybolup `/panel/muhasebe/gelirler-takibi/*`'nin derlendiği doğrulandı) temiz. **Doğrulanamayan**: gerçek bir klinik_admin oturumuyla tarayıcıda hub kartlarının ve satır tıklamasının uçtan uca denenmesi (bu ortamda giriş bilgisi yok).

Bakiye Hareketleri "Tutar" sütunu, kullanıcının onayıyla net (manuel iskonto düşülmüş) yerine brüt/orijinal fiyat göstermeye çevrildi (2026-08-13, bir önceki Kategori İskonto düzeltmesinin devamı — kullanıcı ekran görüntüsüyle "Emscult Terapi Vita ₺1.500 iken Tutar ₺1.000 gösteriyor, yanlış" bildirdi). Karar: Tutar sütunu işlemin gerçek/tam fiyatını göstersin, manuel indirim zaten ayrı "İskonto" sütununda var, "Bakiye" zaten net etkiyi yansıtıyor. `lib/hasta/bakiye-hareket-gorunum.ts`'e yeni bir `tutarBrut` alanı eklendi (`h.tutar + manuelIskontoTutari` — bir önceki turda Kategori İskonto için yazılan "orijinal tutarı geri kur" mantığıyla aynı, artık tek yerde hesaplanıp hem kategori-iskonto hem tutarBrut için paylaşılıyor) — bu sadece randevu bağlantılı (borç kapatma) satırları değil, `odeme_olustur` RPC'sinden gelen (paket/işlem satışı) satırları da kapsıyor, çünkü o akışta da `hasta_bakiye_hareket.tutar` aynı şekilde net yazılıyor. `bakiye-hareket-satiri.tsx`'teki "Tutar" hücresi artık `hareket.tutar` yerine `tutarBrut` gösteriyor (manuel iskonto olmayan satırlarda ikisi zaten eşit, davranış değişmiyor). Cari Hareketler PDF'i bu sütunu hiç içermediği için (kullanıcı kararıyla zaten dışarıda bırakılmıştı) etkilenmedi. `npx tsc --noEmit` ve `npm run lint` (aynı 9 önceden var olan ilgisiz baseline, dokunulan dosyalarda sıfır) temiz. **Doğrulanamayan**: gerçek bir borç kapatma senaryosunda tarayıcıda Tutar sütununun artık orijinal fiyatı (₺1.500) gösterdiği uçtan uca denenmedi (bu ortamda giriş bilgisi yok).

Cari Alacaklar Takibi'ne isim arama eklendi (2026-08-13, kullanıcı isteği): tablo bir client bileşene (`cari-alacaklar-listesi.tsx`) taşındı — Personel Listesi'ndeki client-side arama deseniyle (`useState`+`useMemo`, `toLocaleLowerCase("tr")`) birebir aynı, ayrı bir sorgu/route parametresi eklenmedi (dataset zaten sadece cari borcu olan hastalarla sınırlı, büyük veri riski yok). Sayfa (server component) veriyi çekmeye devam ediyor, sadece render'ı bu yeni bileşene devrediyor.

Muhasebe grubundaki "Gelirler Takibi" öğesi "Gelirler Takibi ve Faturalandırma" olarak yeniden adlandırıldı (2026-08-13, kullanıcı isteği) — sadece görünen etiket (`lib/panel/menu-gruplari.ts` + `gelirler-takibi/page.tsx`'teki yerel `GRUP.label`), URL/route slug (`/panel/muhasebe/gelirler-takibi`) değişmedi.

Bakiye Hareketleri'nde kapatılmış borç satırları "İşlem" ve "Ödeme" olmak üzere iki ayrı tabloya bölündü (2026-08-13, kullanıcı kararı — bir önceki turdaki tek-satır tasarımı "hâlâ problem devam ediyor" diyerek reddetti: kapanan borç satırı hem tedavi bilgisini hem tahsilatı tek satırda karıştırıyordu). Netleşen tasarım (kullanıcıya soruldu): (1) İşlem satırı sadece açık (`tur='borc'`) durumdayken tıklanabilir kalsın, ödendikten sonra salt-okunur bir kayıt olsun; (2) bölünme SADECE görünüm katmanında olsun, veritabanında hâlâ tek kayıt kalsın (bir önceki "kredi satırı kaldırıldı" kararıyla tutarlı — ikinci bir ledger satırı YARATILMADI); (3) İşlem ve Ödeme satırlarının kendi tarih/saati olsun (aynı an değil), Ödeme satırında tedavi bilgisi hiç yazmasın. Uygulama: `hasta_bakiye_hareket_borc_kapat` RPC'sine DOKUNULMADI (hâlâ tek satırı `tur='odeme'`ye çeviriyor) — bölme tamamen `bakiye-hareket-satiri.tsx`'te: `hareket.tur === 'odeme' && hareket.randevu !== null` (kapatılmış borç sinyali, `v_hasta_cari_ozet`'in kullandığı filtreyle aynı) true olduğunda tek `<BakiyeHareketSatiri>` çağrısı iki `<tr>` render ediyor — İşlem satırı: orijinal randevu tarihi (`hareket.created_at`), tedavi adı, terapist, brüt tutar (`tutarBrut`, bkz. bir önceki turun notu), kategori iskontosu, İskonto/Bakiye hücreleri "—"; Ödeme satırı: kapatma anının tarihi (`hareket.odeme.created_at` — bunun için `odeme` embed sorgusuna `created_at` eklendi, `types/hasta-detay.ts`'teki `HastaBakiyeHareketOdeme`'ye de), terapist/işlem adı hücreleri "—", net tahsil edilen tutar, manuel iskonto, gerçek bakiye. Diğer tüm satır türleri (açık borç — hâlâ tıklanabilir, manuel "Ödeme Ekle" kayıtları, `odeme_olustur`'dan gelen paket/işlem satışı ödemeleri, kredi, iade) tek satır olarak eskisi gibi render edilmeye devam ediyor, sadece kapatılmış-borç durumu bölünüyor. Cari Hareketler PDF'i `hareketleriGorunumeCevir`'in çıktısını doğrudan (bölünmeden) kullandığı için bilinçli olarak DOKUNULMADI — bu tur sadece ekrandaki tabloyu kapsıyor. `npx tsc --noEmit`, `npm run lint` (aynı 9 önceden var olan ilgisiz baseline, dokunulan dosyalarda sıfır) ve `next build` temiz. **Doğrulanamayan**: gerçek bir borç kapatma sonrası tarayıcıda İşlem/Ödeme satırlarının ayrı tarih/saatle ve doğru içerikle göründüğü uçtan uca denenmedi (bu ortamda giriş bilgisi yok).

**İşlem/Ödeme bölünmesinde iki gerçek hata bulunup düzeltildi (2026-08-13, kullanıcı "özellikle Ayşe Kadın'ın carisinde karışıklık oluyor" diye bildirdi, ekran görüntüsüyle 3 nokta işaretledi).** (1) **Sıralama hatası**: İşlem ve Ödeme satırları o ana kadar tek bir `hareketGorunumleri.map()` içinde, alttaki `hasta_bakiye_hareket` kaydının (İşlem'in orijinal check-in tarihi) konumunda bitişik render ediliyordu — ama Ödeme'nin GÖSTERİLEN tarihi (`odeme.created_at`, kapatma anı) çoğu zaman çok daha geç bir tarihti; aralarında başka bir hareket olduğunda (örn. borç 31.07'de açılıp 10.08'de kapandıysa ve 05.08'de ayrı bir işlem daha varsa) sıra tutarsız görünüyordu. Çözüm: `lib/hasta/bakiye-hareket-gorunum.ts`'e yeni `bakiyeSatirlariOlustur()` eklendi — `hareketleriGorunumeCevir`'in bakiye matematiğine (o hâlâ orijinal DB sırasına bağlı, dokunulmadı) dokunmadan, çıktıyı düz bir "ekran satırı" listesine çevirip her satırı KENDİ gerçek tarihiyle (İşlem: `hareket.created_at`, Ödeme: `hareket.odeme.created_at`) yeniden sıralıyor — artık aralarına başka bir hareket giriyorsa kronolojik olarak doğru yerde beliriyor. (2) **Etiket tutarsızlığı**: kapatılmamış (açık) borç satırları kırmızı "Borç" rozetiyle görünürken, kapatılmış satırın İşlem yarısı gri "İşlem" rozetiyle gösteriliyordu — aynı kavram (bir tedavinin borcu) iki farklı görünüme sahipti, kullanıcı bunu karışıklık olarak işaretledi. İşlem satırının rozeti de "Borç" (rose) yapıldı — tek fark artık sadece tıklanabilir olmaması (ödeme zaten yapılmış). `bakiye-hareket-satiri.tsx` `hareket`/`islemAdi`/... gibi ayrı prop'lar yerine tek bir `satir: BakiyeSatirGorunum` (`tip: 'tekli'|'islem'|'odeme'`) prop'u alacak şekilde sadeleştirildi, `cari-odeme-sekmesi.tsx` artık `bakiyeSatirlariOlustur()` çıktısını (`bakiyeSatirlari`) map'liyor. Netleşen ve DEĞİŞMEYEN kararlar (kullanıcıya ayrıca soruldu, teyit edildi): İşlem satırı hâlâ sadece açık borçken tıklanabilir (ödendikten sonra salt-okunur kalması doğru bulundu, geri alınmadı). Cari Hareketler PDF'i hâlâ `hareketleriGorunumeCevir`'in ham (bölünmemiş) çıktısını kullanıyor — bu tur sadece ekran tablosunu kapsıyor. `npx tsc --noEmit`, `npm run lint` (aynı 9 önceden var olan ilgisiz baseline) ve `next build` temiz. **Doğrulanamayan**: gerçek bir hastada, İşlem ve Ödeme tarihleri arasına başka bir hareket giren senaryoda sıralamanın tarayıcıda doğru göründüğü uçtan uca denenmedi (bu ortamda giriş bilgisi yok) — kullanıcı "başka carilerden kontrol ederek devam ederiz" dedi, sonraki turda teyit bekleniyor.

İşlem/Ödeme bölünmesinde üçüncü bir gerçek hata düzeltildi (2026-08-13, kullanıcı ekran görüntüsüyle "iskonto ödeme bölümünde gözüküyor, iskonto bölümünde iskonto gösterebilecek bir kod olmaması gerekiyordu, iskonto işleme uygulanıyor" bildirdi). Manuel İskonto tedavinin/işlemin kendisine uygulanan bir indirim — kapatma anındaki tahsilat (Ödeme satırı) sadece ne kadar para alındığını göstermeli, indirimi değil. `bakiyeSatirlariOlustur()`'daki `manuelIskontoTutari` ataması İşlem/Ödeme satırları arasında yer değiştirdi: artık İşlem satırının İskonto sütununda gösteriliyor (Kategori İskonto ile aynı satırda, tıpkı ikisinin de aynı işleme uygulandığı gibi), Ödeme satırının İskonto sütunu her zaman "—". `bakiye-hareket-satiri.tsx`'teki İşlem dalının daha önce hardcoded "—" olan İskonto hücresi gerçek değeri gösterecek şekilde güncellendi. `npx tsc --noEmit`, `npm run lint` ve `next build` temiz. **Doğrulanamayan**: gerçek bir borç kapatma senaryosunda tarayıcıda İskonto'nun artık İşlem satırında göründüğü uçtan uca denenmedi (bu ortamda giriş bilgisi yok).

Bakiye Hareketleri Ödeme satırına ödeme tipi eklendi (2026-08-13, kullanıcı isteği: "ödeme satırında ödeme tipi bilgisi gözükmesi gerekiyor... ödeme (nakit) gibi"). `odeme_satiri.yontem` (parçalı ödemede birden fazlaysa hepsi, portal ödeme geçmişindeki `YONTEM_ETIKETLERI`+`" + "` deseniyle birleştirilerek) artık hem ekranda hem PDF'te gösteriliyor. Ekran: `hareketleriGorunumeCevir`'e yeni `odemeYontemMetni` alanı eklendi, `bakiyeSatirlariOlustur()` bunu "odeme" ve "tekli" satırlarına taşıyor (İşlem satırına taşımıyor — orada ödeme bilgisi yok), `bakiye-hareket-satiri.tsx`'te "Ödeme" rozetinin yanına `(Nakit)` gibi ekleniyor. **PDF de bu turda güncellendi** — önceki notlardaki "PDF bu turu kapsamıyor, hâlâ ham hareketleriGorunumeCevir çıktısını kullanıyor" artık kısmen eskidi: PDF hâlâ İşlem/Ödeme diye BÖLÜNMÜYOR (tek satır/hareket, ekrandaki gibi ayrı satırlara ayrılmadı — kapsam dışı bırakıldı, mevcut basit yapı korundu) ama `tur='odeme'` olan satırlarda İşlem Türü hücresine artık `(Yöntem)` parantez ekleniyor (`cari-hareketler-sablon.ts`'teki `CariHareketlerPdfSatir`'e yeni `yontemMetni` alanı). Sorgulara (`cari/page.tsx` + PDF route) `odeme_satiri(yontem)` eklendi, `types/hasta-detay.ts`'teki `HastaBakiyeHareketOdeme`'ye `odeme_satiri: {yontem: OdemeYontemi}[]` eklendi. Manuel "Ödeme Ekle" kayıtlarında `odeme_id` hiç oluşmadığı için (bkz. önceki turların notu) bu alan boş kalıyor — o akışta yöntem zaten `aciklama`'ya etiket olarak gömülü ("Nakit — not"), ayrıca çakışma yok. `npx tsc --noEmit`, `npm run lint` ve `next build` temiz. **Doğrulanamayan**: gerçek bir ödeme/borç kapatma sonrası tarayıcıda ve indirilen PDF'te yöntem etiketinin doğru göründüğü uçtan uca denenmedi (bu ortamda giriş bilgisi yok).

Son güncelleme: 2026-08-13 (Bakiye Hareketleri: Ödeme satırında ödeme tipi "Ödeme (Nakit)" gibi gösteriliyor, Cari Hareketler PDF'inde de parantez içinde)
Önceki güncelleme: 2026-08-13 (Bakiye Hareketleri: manuel İskonto artık Ödeme satırında değil İşlem satırında gösteriliyor)
Önceki güncelleme: 2026-08-13 (Bakiye Hareketleri: İşlem/Ödeme satırları artık kendi gerçek tarihleriyle sıralanıyor, İşlem rozeti "Borç" ile tutarlı hale getirildi)
Önceki güncelleme: 2026-08-13 (Bakiye Hareketleri: kapatılmış borç satırları görünümde İşlem + Ödeme diye iki ayrı satıra bölündü, veri modeli tek kayıt olarak kaldı)
Önceki güncelleme: 2026-08-13 (Muhasebe > "Gelirler Takibi" ismi "Gelirler Takibi ve Faturalandırma" olarak değiştirildi)
Önceki güncelleme: 2026-08-13 (Cari Alacaklar Takibi'ne isim arama kutusu eklendi)
Önceki güncelleme: 2026-08-13 (Bakiye Hareketleri "Tutar" sütunu net yerine brüt/orijinal fiyat gösterecek şekilde düzeltildi)
Önceki güncelleme: 2026-08-13 (Muhasebe > "Kesilen Faturalar" → "Gelirler Takibi" 2 bölümlü hub'a çevrildi: Kesilen Faturalar taşındı + yeni Cari Alacaklar Takibi ekranı, satır tıklayınca hasta Cari & Ödeme sayfası açılıyor)
Önceki güncelleme: 2026-08-13 (Hastalar listesi: cari borç kırmızı/yeşil nokta + KVKK onaylı metni yerine eksik bilgi (e-posta/adres/telefon) rozeti)
Önceki güncelleme: 2026-08-13 (Borç Kapatma'dan "kredi" satırı kaldırıldı — İskonto girilince artık ayrı bir hareket eklenmiyor, orijinal borç satırının kendisi tur='odeme'/tutar=net olarak güncelleniyor)
Önceki güncelleme: 2026-08-13 (Borç Kapatma'da girilen iskonto Bakiye Hareketleri'ndeki kredi satırında görünmüyordu — hareketleriGorunumeCevir'in randevu/odeme dallarının birbirini dışlaması düzeltildi)
Önceki güncelleme: 2026-08-13 (Kişisel Bilgiler formları kayıttan sonra TanStack Query cache'ini invalidate etmiyordu — 30sn boyunca eski veri görünüyordu, adres "kayboluyormuş" gibi algılanıyordu; düzeltildi)
Önceki güncelleme: 2026-08-12 (Ödeme Tipi kutularında seçili renk gelmiyordu — Tailwind cascade sırası hatası `!important` ile düzeltildi; Borç Kapatma'dan Ödeme Tipi kaldırıldı; İskonto alanı artık boş başlıyor)
Önceki güncelleme: 2026-08-12 (Bakiye Hareketleri: PDF İndir eklendi — Tarih/Saat/İşlem Türü/Terapist/Bakiye kolonlarıyla, Kategori İskonto/İskonto hariç)
Önceki güncelleme: 2026-08-12 ("Ödeme Ekle" yeniden tasarlandı + borç satırına tıklayıp iskonto/faturalı/ödeme tipiyle kapatma akışı + Muhasebe > Faturalar gerçek liste oldu)
Önceki güncelleme: 2026-08-11 (Bakiye Hareketleri kart başlığına "Ödeme Ekle" butonu — ürün/paket seçimi olmayan manuel bakiye hareketi ekleme formu)
Önceki güncelleme: 2026-08-11 (Hasta Detay > Cari & Ödeme: Bakiye Hareketleri tabloya çevrildi — tarih/saat/işlem türü/terapist/kategori iskonto/iskonto/bakiye sütunları, kümülatif bakiye v_hasta_ozet'ten geriye doğru hesaplanıyor)
Önceki güncelleme: 2026-08-11 (Hasta Portalı: kişisel giriş QR'ı, randevu talebi + resepsiyon onayı, kişisel bilgi değişiklik denetimi/"son değişiklik" satırı)
Önceki güncelleme: 2026-08-10 (QR Kodları yönetim listesine çevrildi: her QR için aktif/pasif checkbox, kapatılan QR'ın anonim formu "Kullanım Dışı" gösterip kayıt almıyor)
Önceki güncelleme: 2026-08-10 (QR Kodları genişletildi: Personel Puantaj PIN ile giriş/çıkış + Seans Sonu Anket ve Puanlama — randevuya bağlı dinamik QR, tablette otomatik gösterim, Randevu Detay Paneli'ne "Seansı Bitir" 6. buton)
Önceki güncelleme: 2026-08-10 (İş Başvurusu Personel bölümüne taşındı, PDF ile aynı alanlarla genişletildi, Personel Başvurusu akışıyla birleştirildi; Olumlu→Personel oluşturma sihirbazı prefill, Bilgiler Tamam/Eksik rozeti, ay/yıl arşiv)
Önceki güncelleme: 2026-08-09 (İş Başvuru Formu PDF: İş Deneyimi/Referanslar grid3 satırlarında bir hücrenin etiketi 2 satıra sarılınca (örn. "Çalışma Süresi (Başlangıç - Bitiş)") komşu hücrelerin çizgisiyle hizası bozuluyordu — kullanıcı ekran görüntüsüyle bildirdi, `alan`/`alanGenis` stillerine `justifyContent:flex-end` eklenerek çizgiler etiket uzunluğundan bağımsız satır boyunca aynı seviyeye hizalandı, gerçek PDF'in dar A4 genişliğini taklit eden viewport'ta render edilip görsel doğrulandı)
Önceki güncelleme: 2026-08-09 (Personel Listesi'ne "Yeni Personel Ekle" yanına "İş Başvuru Formu (PDF)" eklendi — Kayıt Formu PDF altyapısı (Puppeteer + @sparticuz/chromium) genelleştirilip yeniden kullanıldı)

**Emoji → inline SVG (2026-08-09).** Kayıt Formu'nun ekran önizlemesinde ⚠ (Alerji Durumu) ve 🩸 (Kan Sulandırıcı Kullanımı) emojileri düzgün görünüyordu ama gerçek Vercel PDF çıktısında boş kutuya dönüşüyordu — kök sebep glyph'in sistem emoji fontundan gelmesi, `@sparticuz/chromium`'un Lambda tarzı ortamında ise hiç emoji fontu bulunmaması. Font gömmek (Noto Color Emoji, ~10MB) bilinçli olarak tercih edilmedi (bundle şişer, zaten siyah-beyaz baskıda renkli emoji anlamsız) — bunun yerine projede zaten kurulu `lucide-react`'in `TriangleAlert`/`Droplet` ikonlarının path verisi (`node_modules/lucide-react/dist/esm/icons/*.mjs`'den alındı) hem PDF şablonuna (`lib/pdf/kayit-formu-sablon.ts`, yeni `svgIkon()` — inline `<svg>`, harici referans yok, `stroke="currentColor"` ile metin rengini takip ediyor) hem ekran önizlemesine (`page.tsx`, doğrudan `lucide-react`'ten `<TriangleAlert>`/`<Droplet>` import edilip kullanıldı) eklendi. İki şablon ayrı olduğu için (bkz. altdaki not — setContent kısıtı) düzeltme HER İKİSİNE de ayrı ayrı uygulandı. Tarama ile (unicode emoji regex) formda başka emoji kalmadığı doğrulandı; projenin başka ekranlarındaki (Hasta Detay Anamnez kartı, Portal Bilgilerim) aynı emojilere BİLİNÇLİ OLARAK dokunulmadı — onlar gerçek tarayıcıda (emoji fontu her zaman mevcut) render ediliyor, bu PDF'e özgü bir sorun. Gerçek PDF üretilip sayfa 2 PNG'ye render edilerek her iki ikonun da göründüğü görsel olarak doğrulandı. `npx tsc --noEmit`, `npm run lint` ve `next build` temiz.

**Asıl kök sebep bulundu (2026-08-09, versiyon pinlemesinden sonra hâlâ 500 alınınca).** Versiyon uyumsuzluğu (bkz. yukarısı) gerçek ve düzeltilmesi gereken bir sorundu ama tek başına yeterli değildi — kullanıcı 3 klasik Vercel+Puppeteer sorununu (bundle'a dahil olmama, süre limiti, bellek) sırayla kontrol ettirince 1. madde doğrulandı: `@sparticuz/chromium`'un `bin/*.br` dosyalarının (chromium.br, al2023.tar.br, fonts.tar.br, swiftshader.tar.br — toplam ~67MB) yolu paket içinde `paths.js`'te `import.meta.url` ile ÇALIŞMA ANINDA hesaplanıyor (`getBinPath()`), literal bir `require()`/`import()` değil — Next.js'in serverless fonksiyon dosya izleyicisi (`@vercel/nft`, statik AST analiziyle çalışır) bu deseni takip edemiyor, dosyalar sessizce fonksiyon bundle'ından düşüyor ve runtime'da "Could not find Chromium"/ENOENT ile patlıyordu. **Ampirik olarak kanıtlandı** (varsayıma bırakılmadı): `next.config.ts`'e `outputFileTracingIncludes: {"/api/hasta-kayit-formu/pdf": ["./node_modules/@sparticuz/chromium/bin/**"]}` eklenip build alındı, üretilen `.next/server/app/api/hasta-kayit-formu/pdf/route.js.nft.json` dosyası incelendi — önce (fix'siz) 0 adet `.br` dosyası, fix'le birlikte 4/4 tam olarak listede çıktı; nedenselliği doğrulamak için fix geçici kaldırılıp tekrar build alınarak fark ölçüldü. Aynı turda 2. ve 3. maddeler de uygulandı: `maxDuration` 30'dan 60'a çıkarıldı (soğuk başlatmada 67MB brotli decompress + Chromium açılışı 30s'yi zorlayabiliyordu), yeni `vercel.json` ile bu route'a özel `memory:1769` (Vercel'in 1 vCPU'ya denk gelen kademesi) + `maxDuration:60` ayarlandı — memory'nin kod içinden (route segment config) ayarlanabilen bir karşılığı yok, sadece vercel.json/proje ayarlarından verilebiliyor. **Hâlâ doğrulanamayan**: bu üç düzeltmenin GERÇEK Vercel deploy'unda `chromium.executablePath()`'i başarıyla döndürüp döndürmediği bu sandbox'tan (Windows) test edilemiyor — `.nft.json` kanıtı "dosyalar artık fonksiyona dahil ediliyor" olduğunu kesin gösteriyor, ama fonksiyonun end-to-end çalıştığını değil; kullanıcının canlı sitede tekrar denemesi gerekiyor.

**Versiyon uyumsuzluğu bulgusu (2026-08-09, canlıda "PDF oluşturulamadı" bildirimi üzerine).** Bir önceki turda Puppeteer'a geçiş yerel Chromium (Playwright cache, `PUPPETEER_EXECUTABLE_PATH`) ile doğrulanmıştı — bu, gerçek `@sparticuz/chromium` kod yolunu HİÇ egzersiz etmiyordu (Windows'ta o binary zaten çalışamaz, Linux ELF). Canlı Vercel'de buton hata verince kök sebep araştırıldı: `puppeteer-core`'un `revisions.js`'i her sürümün beklediği Chrome milestone'ını pinliyor — `npm install`'ın çektiği en güncel `puppeteer-core@25.5.0` Chrome 151 bekliyordu, ama yine en güncel `@sparticuz/chromium@147.0.0` Chrome 147 idi (4 milestone fark, CDP protokol uyumsuzluğu riski — paketin kendi README'si de sürümlerin eşleştirilmesi gerektiğini açıkça yazıyor). npm tarball'ları indirilip (`npm pack`) `revisions.js` karşılaştırılarak tam eşleşen çift bulundu: **`puppeteer-core@25.1.0` (Chrome 149.0.7827.22) + `@sparticuz/chromium@149.0.0` (Chrome 149)** — ikisi de `package.json`'a `--save-exact` ile SABİT (caret range değil) yazıldı, aksi halde bir sonraki `npm install` birini diğerinden bağımsız güncelleyip aynı hatayı sessizce geri getirebilirdi. Ayrı bir gerçek hata daha bulundu: `@sparticuz/chromium@149`'da artık `chromium.headless` getter'ı yok, `chromium.args` zaten `--headless='shell'` flag'ini içeriyor; kod `headless:true` (yeni headless mod) veriyordu — headless_shell binary'si sadece eski-stil "shell" modunu destekliyor, çakışan/gereksiz bir headless flag'i daha eklenmiş oluyordu; `lib/pdf/browser.ts`'te serverless dalı `headless:"shell"` olarak düzeltildi (yerel dal `headless:true` olarak kaldı, sistem Chrome'da sorun değil). Route'a (`app/api/hasta-kayit-formu/pdf/route.ts`) try/catch + `console.error` eklendi — önceden hata sessizce 500'e düşüyordu, Vercel fonksiyon loglarında hiçbir iz bırakmıyordu. **Hâlâ doğrulanamayan**: gerçek Vercel/Linux ortamında `@sparticuz/chromium`'un başarıyla başlayıp başlamadığı bu sandbox'tan (Windows, Linux ELF binary çalıştırılamıyor) test edilemedi — versiyon eşleşmesi ve headless düzeltmesi güçlü kanıta dayanıyor (paketin kendi README'sindeki açık uyarı + revisions.js karşılaştırması) ama kullanıcının canlı sitede tekrar denemesi ve/veya Vercel fonksiyon loglarının paylaşılması gerekiyor.

**Yöntem değişikliği: html2canvas → Puppeteer sunucu tarafı PDF (2026-08-09).** Bölüm A doğrulaması: Playwright ile gerçek Chromium'da sayfa ölçüldüğünde (login'li gerçek oturum, `/panel/hastalar/kayit-formu`) HİÇBİR elementte `scrollWidth > offsetWidth` yoktu, 3 sayfa da tam 792px — yani önceki 3 turun tamamı CSS'i değil html2canvas'ın kendi (eksik) layout motorunu düzeltiyordu; kök sebep hep buydu. Bunun üzerine PDF üretimi tamamen sunucuya taşındı: yeni `GET /api/hasta-kayit-formu/pdf` route'u (`app/api/hasta-kayit-formu/pdf/route.ts`, `runtime="nodejs"`, `maxDuration=30`) `puppeteer-core` + `@sparticuz/chromium` ile gerçek bir Chromium başlatıp `page.pdf({format:'A4', printBackground:true, margin:15mm})` üretiyor; `KayitFormuPdfButonu` artık html2canvas/jsPDF yerine bu route'u `fetch` edip blob'u indiriyor (`html2canvas-pro`+`jspdf` bağımlılıkları kaldırıldı). **`page.goto()` DEĞİL `page.setContent()` kullanıldı** (kullanıcı kararı/uyarısı) — form auth arkasında olduğu için Puppeteer bir URL'e gitseydi çerez taşıma derdi + login ekranını PDF'e basma riski olurdu; onun yerine route handler klinik verisini kendi (zaten authenticated) Supabase server client'ıyla çekip HTML'i STRING olarak üretiyor ve `setContent`'e veriyor — oturum/çerez taşımaya hiç gerek kalmadı. **setContent göreli yolları kırdığı için şablon tamamen kendi kendine yetiyor**: `lib/pdf/kayit-formu-sablon.ts` (yeni) Tailwind KULLANMIYOR — düz TS string fonksiyonları + inline `style=""` üretiyor (JSX/`renderToStaticMarkup` da denendi ama Next.js App Router'ın Turbopack build'i `react-dom/server` import'unu route handler'ların da dahil olduğu server modül grafiğinde reddediyor, "react-server" koşulu çakışması — bu yüzden düz string üretimine geçildi); dinamik metin (klinik adı) `kacir()` ile elle HTML-escape ediliyor (React'in aksine otomatik escape yok). Klinik logosu `fetch` edilip base64 data URI'ye çevriliyor (`lib/pdf/kayit-formu-html.ts`), Inter fontu (Google Fonts'tan indirilip `lib/pdf/fonts/inter-base64.ts`'e gömülü — latin + latin-ext altset, Türkçe ş/ğ/ı/İ/ö/ü/ç dahil) `@font-face` + base64 data URI ile inline `<style>` içinde gömülü — Lambda'da (@sparticuz/chromium) sistem fontu eksikliğinden Türkçe karakterlerin kutu/eksik glif çıkması riski böylece bertaraf edildi, ayrı bir `chromium.font()` ağ çağrısına gerek kalmadı. Sadece Regular (400) ağırlık gömülü — Bold istenen yerlerde (`fontWeight:700`) Chromium sentetik/faux kalınlaştırma uyguluyor, ayrı bold dosyası indirilmedi (bilinçli tradeoff). Sayfa kırılımı `@media print { .sayfa{break-after:page} .sayfa:last-child{break-after:auto} .bolum{break-inside:avoid} }` ile CSS'te; sabit px genişlikler tamamen kaldırıldı, A4 genişliğini `page.pdf({format:'A4'})` belirliyor. Yerel/prod ayrımı `lib/pdf/browser.ts`'te: Vercel'de (`process.env.VERCEL`) `@sparticuz/chromium`'un kendi binary'si, yerelde `PUPPETEER_EXECUTABLE_PATH` ortam değişkeni (veya yaygın Chrome/Edge kurulum yolları) kullanılıyor — tek kod yolu, sadece executablePath kaynağı farklı. `next.config.ts`'e `serverExternalPackages:["puppeteer-core","@sparticuz/chromium"]` eklendi (bundler bu native-binary-ağırlıklı paketleri paketlemeye çalışıp bozmasın diye). **Bölüm C doğrulaması atlanmadı**: geçici bir test klinik+kullanıcı (service role ile) oluşturulup Playwright'la gerçekten giriş yapıldı, route'tan gerçek PDF indirildi, `pdf-lib`+`pdf-parse` ile programatik olarak doğrulandı (3 sayfa, üçü de birebir 595.92×841.92pt — aynı genişlik, metin `getText()` ile çıkarılabildi ve "KVKK"/"İmza" bulundu, ş/ğ/ı/ö/ü/ç/İ/Ş/Ç/Ö karakterleri metinde mevcut) VE `getScreenshot()` ile sayfalar PNG'ye render edilip görsel olarak da incelendi — 3. sayfadaki KVKK paragrafları ve "İmza:" satırı dahil hiçbir yerde kesilme yok. Gerçek bir klinik logosu (Prosem) geçici test klinik'e atanıp logo gömme yolu da (base64 data URI, boyut artışı ~100KB) ayrıca doğrulandı. Test verileri (klinik/kullanıcı/auth user) ve geçici npm paketleri (playwright, pdf-parse, pdf-lib — sadece doğrulama amaçlı, `--no-save`) doğrulama sonrası temizlendi, kalıcı bağımlılık olarak sadece `puppeteer-core`+`@sparticuz/chromium` eklendi. **Bilinen sınır**: `@sparticuz/chromium` ile `puppeteer-core`'un Vercel/Lambda'daki gerçek uyumluluğu bu ortamdan (Windows sandbox, gerçek Vercel deploy yok) test edilemedi — sadece yerel Chromium (Playwright cache'inden `PUPPETEER_EXECUTABLE_PATH` ile) ile doğrulandı; ilk gerçek Vercel deploy'unda maxDuration/bundle boyutu ve chromium binary uyumluluğunun izlenmesi öneriliyor.
**İş Başvuru Formu PDF (2026-08-09, kullanıcı isteği: "Personel Listesi'nde 'Yeni Personel Ekle' yanına 'İş Başvuru Formu (PDF)' ekle").** Kayıt Formu PDF'in kanıtlanmış Puppeteer+`@sparticuz/chromium` altyapısı (`lib/pdf/browser.ts`) hiç değiştirilmeden yeniden kullanıldı, sadece şablon/route/buton katmanı genelleştirildi — yeni bir PDF motoru KURULMADI. `lib/pdf/kayit-formu-sablon.ts`'teki düz-HTML-string yardımcıları (`css`/`kacir`/`stil`/`bolum`/`alan`/`onay`/`sayfa`) ve `kayit-formu-html.ts`'teki font-embedding+print-CSS sarmalayıcısı (Inter base64, `@sparticuz/chromium`'da sistem fontu olmaması riskine karşı) artık iki formun ortak kaynağı: yeni `lib/pdf/form-sablon-cekirdegi.ts` (şablon yardımcıları, `grid3` düzeni ve `formBasligi()`/`imzaSatiri()` bu turda eklendi) ve `lib/pdf/pdf-yardimcilari.ts` (`gorseliDataUriyeCevir` — eski adı `logoyuDataUriyeCevir`, `pdfBelgesiOlustur`); Kayıt Formu'nun kendi dosyaları (`kayit-formu-sablon.ts`/`kayit-formu-html.ts`) bu ortak modülleri import edecek şekilde refactor edildi, ÇIKTISI DEĞİŞMEDİ (aynı stil objeleri birebir taşındı). Yeni `lib/pdf/is-basvuru-formu-sablon.ts`: Kişisel Bilgiler, Başvuru Bilgileri, Eğitim Bilgisi, İş Deneyimi (son 3 iş yeri, `grid3` düzeni), Referanslar (`grid3`) ve tek bir KVKK onay kutusu (hastanınkinden farklı, başvuru-sürecine-özgü metin — `ONAY_ACIKLAMALARI` reuse EDİLMEDİ çünkü o metinler "tedavi"/"sağlık verisi" bağlamlı) + imza satırı; içerik tek A4 sayfaya sığmayıp doğal olarak 2. sayfaya taştı (Kayıt Formu'nun aksine sabit `.sayfa` bölme YOK, sadece `breakInside:avoid` ile bölüm ortadan kesilmiyor — CLAUDE.md'nin önceki turlardaki "gerçek Chromium print layout motoruna güven" dersiyle tutarlı, browser'ın doğal sayfalamasına bırakıldı). Yeni `GET /api/is-basvuru-formu/pdf` route'u `hasta-kayit-formu/pdf` ile birebir aynı desende (`runtime="nodejs"`, `maxDuration=60`, sadece login kontrolü — `klinik_admin` kısıtı YOK, boş form bastırmak resepsiyon için de anlamlı görüldü). **Kritik, unutulması kolay adım**: yeni route `next.config.ts`'teki `outputFileTracingIncludes` ve `vercel.json`'daki `functions` bloğuna da eklendi — bunlar atlanırsa CLAUDE.md'nin "Asıl kök sebep bulundu" notundaki ENOENT/"Could not find Chromium" hatası bu route için de sessizce tekrarlardı; ekleme sonrası `next build` alınıp üretilen `.next/server/app/api/is-basvuru-formu/pdf/route.js.nft.json` içinde 4/4 `@sparticuz/chromium/bin/*.br` dosyasının gerçekten listelendiği doğrulandı (aynı ampirik yöntem). Buton tarafında da aynı genelleştirme yapıldı: yeni `components/panel/pdf-indir-butonu.tsx` (endpoint/dosyaAdı/etiket/variant prop'lu) — `KayitFormuPdfButonu` bu genel bileşenin ince bir sarmalayıcısına indirgendi (dışa açık API'si/görünümü DEĞİŞMEDİ), yeni `IsBasvuruFormuPdfButonu` aynı genel bileşeni `variant="outline"` ile kullanıyor. Personel Listesi header'ı (`app/(app)/panel/personel/liste/page.tsx`) iki buton aynı anda sığdığında (klinik_admin için "Yeni Personel Ekle" + bu buton) mobilde (`max-w-md`) taşabileceği için `flex-wrap`'e çevrildi (projede zaten yerleşik bir desen, bkz. `panel/muhasebe/kamusal-giderler/page.tsx`) — PDF butonu HERKESE (resepsiyon dahil) görünür, sadece `YeniPersonelDialog` klinik_admin'e özel kalmaya devam ediyor. **Doldurulan form panelde hiçbir yere KAYDEDİLMİYOR** (bilinçli kapsam dışı) — bu, QR ile self-servis `is_basvurusu`/`personel_basvuru_taslagi` akışlarından (bkz. yukarıdaki "RLS mimarisi" notu) tamamen ayrı: o akış adayın kendi telefonundan doldurduğu ve DB'ye yazılan bir form, bu ise resepsiyonun kapıya gelen adaya bastırıp elle doldurttuğu kağıt — ikisi arasında hiçbir veri bağı yok, bilinçli olarak kurulmadı. Gerçek Puppeteer render'ı bu ortamda da (yerel Chrome, `lib/pdf/browser.ts`'in yerel-Chrome-bulma dalıyla) çalıştırılıp doğrulandı: `page.evaluate`'te `scrollWidth > clientWidth` olan 0 eleman (taşma yok), üretilen PDF'in `/Pages` nesnesinde `Count 2` (2 sayfa, doğal taşma), ekran görüntüsü PNG'ye render edilip Türkçe karakterlerin (İ/ş/ğ) ve grid3 hizalamasının doğru göründüğü görsel olarak da teyit edildi; test dosyaları temizlendi. `npx tsc --noEmit`, `npm run lint` (aynı 9 önceden var olan ilgisiz baseline) ve `next build` temiz.

Önceki güncelleme: 2026-08-09 (Hasta Kayıt Formu PDF: Tıbbi Ön Geçmiş'teki ⚠/🩸 emojileri PDF'te boş kutu çıkıyordu — @sparticuz/chromium'da emoji fontu yok; ikisi de lucide-react TriangleAlert/Droplet path verisiyle birebir aynı inline SVG'ye çevrildi, hem PDF şablonunda hem ekran önizlemesinde, gerçek PDF render edilip görsel doğrulandı)
Önceki güncelleme: 2026-08-09 (Hasta Kayıt Formu PDF: 3. sayfadaki KVKK/onay paragrafları min-width:0 eksikliğinden dolayı sayfayı genişletip metni kesiyordu, Onay bileşenine min-w-0/break-words eklendi)
Önceki güncelleme: 2026-08-09 (Hasta Kayıt Formu PDF: html2canvas Tailwind v4'ün color-mix/oklch çıktısını parse edemiyordu, html2canvas-pro'ya geçirildi)
Önceki güncelleme: 2026-08-09 (Hasta Kayıt Formu PDF'i html2canvas+jsPDF'e geçirildi, 3 sayfa açık bölündü, bölüm üstü çizgiler kaldırıldı, logo eklendi)
Önceki güncelleme: 2026-08-09 (Kapı tableti kurumsal görünüme geçirildi: 3 durumlu sistem, doku/logo/ekran koruyucu/çevrimdışı göstergesi, WCAG ve hidrasyon düzeltmeleri)
Önceki güncelleme: 2026-08-09 (Şirket Bilgileri'ne Araçlar kartı eklendi — marka/model/plaka, her zaman açık ekleme satırı)
Önceki güncelleme: 2026-08-09 (Personel Listesi Giriş/Çıkış: tek tıkla anında kayıt yerine saat onay/düzenle akışı)
Önceki güncelleme: 2026-08-09 (Personel: hub kaldırıldı, doğrudan Personel Listesi'ne yönlendiriliyor; Maaş/Takip navigasyondan çıkarıldı; liste kutucuk butonları renklendirildi)
